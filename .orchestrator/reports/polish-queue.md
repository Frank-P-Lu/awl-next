⚠️ STANDING RULE (user, 2026-07-09): every subagent / workflow agent() call MUST set an explicit model — "sonnet" by default, "opus" for hard reasoning/verify stages. NEVER let a subagent inherit the session model (Fable) — EXCEPTION: the user may explicitly request a Fable agent for taste-grade audits (did so once, keybinding audit).

# 🌅 DRAIN COMPLETE — 2026-07-10 morning. 45 commits faaf3f7→d2ce571, suite 1672×green, LIVE @ https://awl-editor.fly.dev/editor/ (fresh-profile verified twice). READ FIRST: .claude/orchestrator/MORNING-REPORT-2026-07-10.md (the full ledger: arcs, fixes, galleries awaiting veto, the keybinding report's 8 questions, watch items). Awaiting the user: bullet/table/selection/ko galleries + keybinding picks + banked decisions (packs, in-grid editing, renumbering, web weight).

# 🌙 TONIGHT (2026-07-08→09 overnight, ultracode) — user asleep, wants a MORNING REPORT
LANDED since deploy: 3a9d509 Cmd-I stale-overlay · ef6f17c deploy config+wasm · 748bac8 setext-heading fix · 2e218fe outline-ink 2-state · d99c84d every-command smoke test (ZERO crashes, permanent no-wildcard guard, 1565 tests) · 717abcd web loading spinner (breathing amber caret, verified end-to-end via headless browser).
MORNING REPORT DELIVERED → .claude/orchestrator/COMMAND-REPORT-2026-07-09.md (command rename/taxonomy proposal + mini-panel expansion; 8 open questions for the user — renames NOT implemented, await user decisions).
IN FLIGHT: palette-nav-polish workflow (wqt3kfz5f) — hint-line up/down affordance + stronger selection highlight (taste capture → /Users/frank/.claude/jobs/3ce0c357/tmp/palette-polish.png).
🔴 REAL DEADLOCK FOUND (2026-07-09, supersedes the handoff's "not actually hung" claim): the stats-split's `lifetime::test_lock` + `about::test_lock` ABBA-invert — apply_core takes about(299)→lifetime(304), but `lifetime_stats_opens_and_any_key_dismisses_it` (tests.rs:2543) pre-holds lifetime then enters apply_core→about. Confirmed by live stack sample (~/.claude/jobs/891e5c3b/tmp/deadlock-sample.txt). Scheduling-dependent — THIS was last night's "hang" too. Opus agent fixing structurally (composite guard / canonical order about→lifetime, documented theme→fs→about→lifetime→page); bar = TWO consecutive full green runs.
🖼️ IMAGE ROUND (user, 2026-07-09 live — fires FIRST after the stats-split commit; user's active pain): (a) BUG: caret on an image line balloons to image-row height — cursor_scale (render/geometry.rs:1150) = row_h/line_h, right for headings (big glyphs) wrong for image rows (body-size source glyphs over a tall reserved row); fix = when image_heights[cursor_line].is_some(), caret sizes/anchors to the source TEXT glyph box (scale 1.0), positioned on the text. (b) REVEAL MODEL (user decided): caret-on-line shows SOURCE LINE + DIMMED image below it in the reserved row (Obsidian model; row grows by one text line while revealed; instant line-local reflow like other reveals) — replaces today's hide-image-show-source-only. (c) DRAG-RESIZE (user decided): any edge + corner, REUSING the page-resize component pattern (proximity hover → cursor_shape ↔ sides / ↕ top+bottom / ⤡ corners → drag → write-back on release); ALWAYS proportional, writes the single |W into ![|W](path) as ONE undoable edit; never WxH/distortion. Corner KEPT (rec accepted unless user objects). (d) asset files are NEVER deleted on reference removal (verified: zero deletion code; keep it that way — undo safety); BANKED: a summoned "Clean unused assets" command. (e) BUG (user, live): click onto a different line → WYSIWYG reveal reflows that line's glyph positions under the stationary pointer → release hit-tests a different column than press → phantom drag-SELECTION instead of a plain caret move. FIX: arm drag-selection on actual pointer TRAVEL (px slop threshold), never on hit-test-result drift; a reveal reflow under a still pointer must read as a plain click. (Also check the same drift can't misplace the caret itself: the press hit-test should win, or re-anchor after reflow.)
📋 COMMAND RENAME ROUND (user decided 2026-07-09 — queued AFTER focus removal so we never rename commands we're about to delete): implement the COMMAND-REPORT-2026-07-09.md proposal with the user's calls on §8: ALL recommendations ACCEPTED **except buffer-standardization, which is REVERSED** — user-facing labels stay/become the "file" family (`Last file`, `Finish file`); "buffer" is arcane emacs vocabulary and the audience is being widened beyond programmers (see IDENTITY CHAT below). Specifically: q1 ellipsis convention YES (with the trailing-`…`-stripping canonicalization + law test); q3 `Keep version` YES; q4 real `ToggleWritingNits` action YES (retire the Ignore sentinel); q5 focus renames MOOT (feature removed); q6 leave WYSIWYG/images/ligatures settings-only (subtraction); q7 retired `[keys]` keys go silently inert, NO alias back-compat — but recompute the retired-key list against the file-not-buffer direction. Sweep ALL user-facing surfaces for emacs-arcana while renaming (palette, menu, hints, notices, empty-states — "buffer" et al) but do NOT touch chords/keymap structure yet (that's the identity chat).
💬 IDENTITY CHAT — SETTLED (user 2026-07-09, "yeah sounds good… 1. 2. 3." + philosophy): the ROUND grows to an IDENTITY ROUND = rename round PLUS: (1) NATIVE-FIRST ADVERTISING — slot 1 (native ⌘/macOS-ctrl-nav) is THE advertised keymap; every user-facing surface speaks plain writer-English; palette shows the NATIVE binding as the binding (a command whose ONLY chord is emacs still shows it — it's the only door); (2) EMACS SLOT GOES QUIET — fully functional, both slots still fire + rebindable, rebind menu (Keybindings) still shows BOTH slots; it just stops being the advertised identity; (3) VOCABULARY PURGE (the rename round, buffer→file etc.); (4) NATIVE CHORD ASSIGNMENT + CONSOLIDATION PASS (user 2026-07-09, refined twice: "add macos defaults, you can pick all of them" + "since we have the lens pattern we can probably remove the total number of shortcuts"): a chord buys a TERRITORY (a general door + lenses), not a command. THE ADVERTISED DOORS: ⌘P do-something (palette) · ⌘O go-somewhere (Go to file; fold RECENT FILES + the planned HEADINGS lens in here — outline gets NO chord) · ⌘⇧P switch project (fold the RECENT-PROJECTS picker in as its Recent lens, retiring the standalone picker kind — the previously-banked fold is now DECIDED) · ⌘F find. Plus platform basics: ⌘N New note · ⌘T Switch theme · ⌃Tab Last file (native-only; web keeps palette) · ⌘-click Follow link (C-c C-o stays) · existing ⌘S/Q/Z/⇧Z/C/V/X/B/E/I/←→/↑↓. Browse files = summoned-by-name only (the wandering navigator; its "This folder" job is also a ⌘O lens). Move note = palette-only. REQUIREMENTS: free-key audit vs keymap.rs first; a collision = pick differently, never steal; extend the law tests.
(5) EMACS DEFAULT RETIREMENT (user 2026-07-09 "do we even still care about c-x? …yeah"): EMPTY the C-x/M- DEFAULTS from the catalog wherever a native chord or palette/lens door now covers the command — the emacs slot becomes just "slot 2", yours to fill. SURVIVORS (explicitly kept, platform-native — user refined 2026-07-09 "rip out meta, keep opt-left/right and that's about it"): bare-control nav C-n/C-p/C-a/C-e/C-k etc., C-s incremental search, ⌥←/→ word motion + ⌥⌫ word delete (AUDIT these fire natively before retiring anything). WORD-DELETE SPECIFIC (user 2026-07-09): awl currently binds Ctrl-Backspace for delete-word — a PC-ism, neither emacs (M-DEL = ⌥⌫ on a Mac) nor macOS idiom; make ⌥⌫ the advertised slot-1 word-delete, demote C-⌫ to quiet slot 2 (or drop); audit ⌘⌫ delete-to-line-start (the other macOS-standard deletion) exists too. The ENTIRE M-letter layer retires from defaults — M-b/M-f included (⌥←/→ supersedes; earlier "keep M-b/f" is superseded by this refinement) — because macOS reserves Option-letters for TYPING (dead keys é/ñ/ü, em-dash ⌥⇧-, •), which the writer audience needs; every M-letter chord awl claims steals a typographer character. Option-layer commands = word ops only, per the platform's own rule. KEEP THE MACHINERY — USER-DECIDED, PERMANENT (2026-07-09 "the prefix is actually good, power users would appreciate it — we shouldn't rip it out"): prefix-sequence keymap support + rebind-menu chord capture are a kept power feature (a [keys] line recovers any retired chord); whichkey panel stays and demotes naturally (fires only on prefixes that exist). Do NOT propose removing the prefix machinery in future sessions. PLUS the PHILOSOPHY.md (+SCOPE.md) AUDIENCE AMENDMENT, logged like the WYSIWYG-pivot amendment: audience: one → "for me, and for people who aren't programmers — people who like computers, and like writing, and like novelty, and beauty"; emacs demoted from identity to flavor ("progressively enhanced with Emacs" stays true mechanically, stops being the pitch). EXPLICITLY UNCHANGED: incremental search (Cmd-F front door, C-s flavor), all existing chords keep firing, NO Space leader, NO modes.
🎓 DISCOVERABILITY ROUND (user decided 2026-07-09 — queued AFTER the identity round, builds on the same catalog+stats plumbing): NO toasts/whispers/nudges EVER — user vetoed interruptions, awl stays calm; the data is recorded silently and surfaced only in PLACES YOU VISIT. (1) USAGE LEDGER: per-command invocation counts BY DOOR (chord vs palette vs menu — the apply seam already sees every dispatch; menu/palette pass a door tag), persisted in stats.toml beside the lifetime stats; a command GRADUATES once its chord has been used ~N times (then it never appears in tips). (2) HOLD-⌘ SHORTCUT PEEK (user: "i like 2!!!!"): hold BARE ⌘ a beat (~600ms, iPadOS convention; cancels instantly if any second key joins so ⌘S etc. are untouched) → the held-card pattern (Cmd-I HUD pipeline) shows a curated handful — the top UNLEARNED chords for commands you actually use, ranked from the ledger. Release = gone. Native-first chords shown, of course. (3) SHORTCUTS SCREEN: the existing Keybindings overlay gains a quiet TIPS footer — "your top 3" (most slow-door-used, chord-bearing, ungraduated commands). Same ledger feeds both surfaces; determinism: ledger is live-App-only (stats.toml precedent), capture shows fixed placeholders.
✅ STATS SPLIT LANDED — faaf3f7 (17 files: the 15 + about.rs/page.rs from the deadlock cure; suite double-green 1571×2; the ABBA fix is composite lifetime::test_lock + scoped apply_core guards, order theme→fs→about→lifetime→page documented).
✅ IMAGE ROUND LANDED — 136fdd0 (click-slop DRAG_ARM_SLOP_PX=4 · caret fix · all-8-zone proportional drag-resize, grab 12px, MIN_IMAGE_W 64, dim 0.55 — tunables flagged; suite 1583×green). ⚠️ VERIFY FOUND: the approved source-above-image reveal is geometrically impossible (cosmic-text centers a line's glyphs in its line_height — one baseline per line; gallery/image-reveal/*-revealed.png shows text centered ON the image). USER RE-DECIDED (2026-07-09): **CAPTION STYLE** — lean in deliberately: source line centered OVER the dimmed image, row height UNCHANGED on reveal (zero reflow, calmer than the original pick); polish legibility (tune dim, consider a soft scrim band behind the text). CAPTION-FIX round fires after the settings pass commits (same render files): revert the +base_lh row growth from 136fdd0's reveal, style the caption state, update the reveal tests/galleries.
➕ SETTINGS PASS ADDITION (user 2026-07-09): OUTLINE DEFAULT FLIPS TO ON (`outline` sticky default true — was off-by-default opt-in). Update: the default in config/outline seams, every test asserting the off default (dedicated outline default tests + any capture asserts), the config template comment, and the docs/queue lines that logged "opt-in, off-by-default keeps the calm default" — log this as a user-decided taste reversal, not a silent flip. An existing user config line `outline = false` keeps working (sticky pref honored either way).
✅ SETTINGS PASS LANDED — 5e3934c (suite 1584×green; CJK-Enter + toggle-state were ALREADY FIXED — verified headlessly both seams, no code change; §8.8 comments corrected + corpus count now a law at 23; OUTLINE DEFAULTS ON with proven outline=false override; DESIGN.md sub-amendment logged. Live-only flag: the outline's default-on margin pixels need the user's first-launch eyeball).
✅ CAPTION FIX LANDED — df773ba (zero-reflow reveal; IMAGE_REVEAL_DIM_ALPHA 0.4, scrim base_100 @ ~0.72 + pads 4/3 — TASTE, user eyeball pending; gallery/image-reveal/ regenerated; suite 1584×green).
✅ FOCUS REMOVAL LANDED — 54a5ce6 (pass 1, 30 files, +183/−1263, suite 1562×green) + 469af2b (pass 2: render/focus.rs deleted, reports.rs new home, color_override threading stripped; suite 1562×green; sidecar schema /145, no focus block; zero behavior change). Release binary rebuilt at 469af2b — user told to relaunch.
✅ IMAGE SIZING SANITY LANDED — 64cd3ec (paste stamps no |W · 0.65 viewport cap · revealed images arm handles; suite 1566×green; NOTE: the workflow agent parked on an untracked background suite AGAIN — orchestrator verified + committed by hand; future agent prompts carry a HARD foreground-suite rule).
🎨 WEB COLOR-SPACE BUG — ✅ FIXED ON BRANCH worktree-wf_082b96bb-b8b-1 @ 1291a9b (suite 1567×green; redeploying from the branch NOW): root cause = WebGPU spec forbids sRGB canvas formats → find(is_srgb) fell back to non-sRGB → linear light written raw. Fix = sRGB VIEW over the base format (view_formats + add_srgb_suffix), backend-agnostic; native SHA256-byte-identical (proven); web Tawny+Saltpan match native to 0 counts/channel. AWAITS MERGE TRAIN like the others. TWO SIDE-FINDINGS: (1) 🕸️ WEBGL2 INIT HAS ALWAYS BEEN BROKEN — request_device default Limits demand compute → 'max_compute_workgroups_per_dimension 65535 vs 0' → no-WebGPU browsers get NOTHING; fix = downlevel_webgl2_defaults()/adapter.limits() on wasm — QUEUED as its own small round (evidence web-webgl2-initfail.png in job tmp). (2) LATENT TASTE FLAG: the shipped native Tawny column (83,86,95) is a lifted rendering of base_100 (22,24,29) via theme.rs::to_wgpu's clear passthrough — if the user ever wants the literal darker column, that change moves EVERY native capture byte; parked for the user's eye, not made.
🇯🇵 JP TYPOGRAPHY — 🔵 IN FLIGHT (workflow w77bcn026, worktree lane; duhai's live findings 2026-07-09): phase 1 = BOLD/ITALIC BREAK JAPANESE (the documented weight-trap: bundled JP faces are 400-only, md bold requests 700 → cosmic drops the face; fix = CJK runs pin weight/style to the resolved face's registered values; no synthetic bold v1, logged); phase 2 = FACE VARIETY (user: kana barely changes across worlds — 2-3 new OFL faces via the documented instance+subset recipe, per-world ladder reassignment, THEMES.md table, veto galleries at gallery/jp-worlds/, report native+wasm size deltas; web-inclusion decision stays banked).
📦 THEME PACKS — THE SETTLED MODEL (user 2026-07-09, supersedes "font packs"; design-then-build arc, fires after the current lanes + merge train): the pack UNIT IS THE WORLD, COMPLETE OR ABSENT — a world ships WITH its faces (display/mono/CJK companions/ornaments); NO "enhanced theme" states ever (user: "i hate the idea of themes being enhanced"). WEB ships a curated set of COMPLETE built-in worlds (~5, picks need user veto) + the rest as downloadable packs; NATIVE gets the SAME user-initiated pack install (user: "if we support it on web we've gotta support it natively too") → the zero-network invariant amends NARROWLY: never unprompted, no telemetry/update-checks; the only network act = a world the user explicitly requested from awl's own host; drop-a-file-in-data-dir keeps working underneath. NUANCE: the CJK SCRIPT FLOORS (the never-tofu guard, ~10MB of the 16) are NOT flavor — they stay bundled native, LAZY-FETCH on web on first CJK use (identical in every world, so no contract breach). This is the road to "10 fonts per language": new faces arrive INSIDE new worlds, nobody's binary grows. Interplay: the in-flight JP round + queued CJK companions still bundle into native (fine — native has room); they migrate into pack definitions when this arc lands.
🈶 CJK COMPANION ROUND (user mused 2026-07-09, queued PENDING the JP round's galleries + veto): the OFL landscape is asymmetric — JP is deep (round in flight), zh/ko are thin. Proposal: KO gains **Gowun Batang** (OFL serif — closes the logged "no serif Korean companion" v1 gap; serif worlds stop sharing one Noto Sans KR); zh-Hans gains ONE face, **GenSenRounded** (justfont's OFL Source-Han derivative, rounded — pairs with the warm/rounded worlds); zh-Hant stays banked (Big5 weight); KingHwa OldSong stays DECLINED (license). Same recipe (instance+subset per script codec), same veto galleries, honest native+wasm deltas. NOTE: every added face pressures the 27MB wasm payload → the banked WEB CJK SLIMMING (lazy-load faces on web / Latin-first boot) is coming due — evaluate alongside.
⏪ BREADCRUMB BACK (duhai, 2026-07-09 — queued AFTER identity lands, overlay territory): Cmd-P → sub-picker → can't get back to the palette; generalize the Settings return_to breadcrumb — ANY overlay summoned from another overlay gets return_to = parent; Esc pops one level, Esc at top closes. Rides with/just after the discoverability round.
🍔 WEB/LINUX MENU BAR (duhai, 2026-07-09 — design-then-build, queued after identity+discoverability): web has ZERO chrome → nothing discoverable. An awl-RENDERED slim menu bar reusing menu::SECTIONS (one roster, macOS=NSMenu / web+Linux=in-app renderer), default SHOWN on web+Linux, ABSENT on macOS; "Toggle menu bar" command + sticky setting to hide; persistent-chrome philosophy amendment logged (outline precedent). User: "we'll need a command to hide that" — settled. THEME-DERIVED (user 2026-07-09): the bar's colors come from the ACTIVE WORLD — ground from the base ladder, labels faint/muted ink, hover = a value step, NEVER amber (DESIGN §3); re-tints on theme switch via the sync_theme_colors seam (O(1), like every other chrome); no fixed chrome color anywhere.
✅ IDENTITY ROUND FULLY LANDED — 8bcc361 + 66f720c + c638c96 (lens folds, suite 1564×green, both folds capture-verified, reference stash dropped) + c7fb259 (PHILOSOPHY/SCOPE/CLAUDE.md dated amendments). 🚂 MERGE TRAIN ✅ DONE (linear cherry-picks, each suite-gated): 8cb53c2 web sRGB fix (clean) · d4a21a3 CJK weight pin (clean) · asset cleaner (6 mechanical conflicts resolved per identity conventions — "Clean unused assets…" ellipsis'd + PALETTE_ONLY'd, ALL_KINDS 13, schema /148 capture-confirmed). ✅ MERGE 4 DONE — main @ 1bcd9b4, suite 1588×green, ZERO conflicts. JP FACE VETO ✅ BLESSED (user, 2026-07-09 evening: "they look good. ship please") — assignments final: Shippori Mincho (Gumtree/Bilby/Undertow) · Zen Maru Gothic (Galah/Kingfisher) · Klee One (Mopoke/Quokka). WORLDS.md update unblocked. (historical) JP FACE VETO PENDING (user + duhai): gallery/jp-worlds/ = 7 changed worlds × {characterful, floor} pairs — Gumtree/Bilby/Undertow→Shippori Mincho, Galah/Kingfisher→Zen Maru Gothic, Mopoke/Quokka→Klee One; mono+display worlds keep Noto by design. Undertow sanity-captured resolving Shippori Mincho bundled:true. ✅ DISCOVERABILITY LANDED — 12d423b (ledger: Door counts in stats.toml, GRADUATION_N=5) + efb89d5 (hold-⌘ peek, HOLD_PEEK_MS=600, starter-six fallback; Keybindings tips footer; suite 1613×green). FLAKE NOTED for next audit day: render::tests::wash_cache_and_geometry_contract flaked once under parallel load (passes isolated; pre-existing, reads theme/wash geometry — likely missing a test-lock). ⛔ SESSION LIMIT HIT (~15:00 JST, resets 21:50 JST) — the pre-deploy round's two agents died BEFORE ANY EDITS (tree verified clean at efb89d5). RESUME AFTER RESET (or after /login to API billing): re-run Workflow with scriptPath /Users/frank/.claude/projects/-Users-frank-code2026-awl-next/891e5c3b-b8da-4a62-868c-445c74336d4a/workflows/scripts/pre-deploy-fixes-wf_6f2b8fe4-4be.js + resumeFromRunId wf_6f2b8fe4-4be. ⚠️ DO NOT DEPLOY MAIN BEFORE THE WELCOME REWRITE LANDS — main's welcome doc still teaches the C-x chords the identity round RETIRED (actively wrong on the live site); the currently-deployed bundle is internally consistent, leave it until the fixes round lands.

📜 DRAFT FOR USER VETO — the packs-arc zero-network amendment (PHILOSOPHY.md voice, goes in when the arc builds): "Amendment (2026-07-09, theme packs): the native app never phones home — no telemetry, no update checks, nothing unprompted. The one network act awl may ever take is fetching a THEME PACK the user explicitly asked to install, from awl's own host, at that moment, and never again after. A world is complete or absent — packs add worlds, never alter them. The paranoid path remains: any pack is also a file you may drop into the data dir yourself; awl cannot tell the difference."
📜 DRAFT FOR USER VETO — the web's 5 built-in worlds (curation proposal): Tawny (the opener — awl's face, mono, dark-warm) · Saltpan (the light one — paper-calm, serif) · Undertow (dark serif, now Shippori Mincho ja — the literary showcase) · Currawong (sans, gothic ja — the modern clean one) · Mopoke (Klee One — the characterful oddball that makes people fall in love). Covers dark/light × serif/sans/mono × all three new JP faces… pending the JP veto itself.

🔵 PRE-DEPLOY FIXES (KILLED BY LIMIT, resume above) (wqmyb1vw4): breadcrumb-back (opus; pop-vs-close-all table per picker) → small batch (sonnet: no-path paste saves first · WebGL2 downlevel limits · welcome.md native-first rewrite). 🈚 CJK-SETTING UX FIX (user vetoed the config-as-text punt, 2026-07-09 evening): the "CJK priority" List row asks the wrong question — reframe as a PICKER: row reads "Ambiguous CJK reads as · Japanese" (writer-words); Enter opens the standard sub-picker (return_to breadcrumb, just landed) with 4 rows Japanese/Simplified Chinese/Traditional Chinese/Korean; picking PROMOTES that lang to the ladder front (rest keep relative order); config keeps the full ordered list (hand-edit + back-compat untouched); SettingKind::List RETIRES (this was its only row — no-wildcard sweeps drive the removal). Small round — ride alongside/after the menu bar.
✅ PRE-DEPLOY BATCH LANDED — e75df23 (no-path paste saves first · WebGL2 downlevel limits, wasm-compile-verified · welcome.md native-first; suite 1625×green; hermeticity law satisfied via justified count bump). ✅ FINAL BUILD QUEUE LANDED — 3eb3a03 lens clicks (mostly pre-existing, gaps filled) · 286ea20 CJK picker (List retired) · d68e7de menu bar (bespoke chrome reusing picker primitives, NOT an OverlayKind; suite 1654×green, wasm green). 🔵 PRE-SHIP SWEEP RUNNING (w09rqqieg, 4 phases): theme-pick blocker (opus) → selection pillar + contrast floor + pull-quote (opus; gallery/selection/) → menu-icon template + calt per-buffer (sonnet) → WORLDS.md JP faces (sonnet). ✅ PRE-SHIP SWEEP LANDED (dfe6070 theme-pick ValuePick fix — hypothesis DISPROVEN honestly · a5bb6d4 selection band+contrast+pull-quote · 568d1cb icons template+ligature seal · 18ea48c WORLDS.md) + ✅ e33cb8a Super-swallow guard (⌘H typed 'h' — resolve_char never saw Super). 🚀 SHIPPED 2026-07-10 ~00:20 JST — deployed @ e33cb8a, LIVE-VERIFIED fresh-profile: menu bar renders theme-derived, native-first welcome (⌘P/⌘O/⌘F + hold-⌘ line), Tawny warm, 14 worlds. NOTE: a returning browser restores its OWN scratch stash (old welcome text ≠ stale deploy). ✅ TRAIN+TABLES LANDED — Gowun Batang merged clean (39e2118) + 77071c3 tables (columns/pan/x-ray; suite 1669×green; schema /163; gallery/tables/). ✅ BULLETS LANDED — Undertow's manicule ☞ (EB Garamond-native; Junicode subset-extension DECLINED per zero-network — right call), 7 literary worlds hedera'd with flavour arguments, 7 technical worlds byte-identical; gallery/bullets/. 🔵 ORG PASS RUNNING (w7105g9xi, 3 phases, ~13 bisectable commits: test splits → product splits [render.rs exception re-affirmed] → structural quartet [ViewState::base · ONE test_serial (expected to cure the wash_cache flake) · SCHEMA const · card unification]). THEN: final redeploy (also commits the dirty site/editor artifact) + the morning report.
🌊 FULL DRAIN ORDERED (user, 2026-07-09 night: "we should just drain all of them!"): after the deploy — LANE A (main, sequential): TABLE ROUND (columns+scroll+x-ray) → BULLETS (veto gallery) → ORG PASS (alone on main, both halves; fold the wash_cache flake into structural item 2) → FINAL REDEPLOY. ❌ PACKS ARC PULLED FROM THE DRAIN (user, same night: "don't do the theme packs just yet… genuinely still uncertain… don't think we should do the downloads stuff") — BANKED, do not build, do not re-pitch; the earlier "settled model" block above is DESIGN-ONLY now. Consequence accepted: web stays heavy (bundled fonts, no lazy-fetch) — the web-weight decision banks with it. LANE B (worktree, fires immediately): CJK COMPANIONS (Gowun Batang ko-serif + GenSenRounded zh-rounded, veto galleries) — merge-trains in when ready. LANE C ✅ DONE (Fable): KEYBINDING-REPORT-2026-07-09.md delivered — 10 ranked recommendations, 8 open questions for the user's morning picks (⌘, · ⌘G/⇧⌘G + remembered-query · ⌘W · ⌘. · Hide block · tap/hold-⌘I · ⌘E-keep+⌘F-prefill · ⌘K-reserve · ⇧⌘L · refusals). ⚠️ BUG-GRADE FINDING PULLED INTO THE SHIP: unbound ⌘-chords SELF-INSERT their letter (⌘H types 'h'; a keymap test PINS it) — the SUPER-SWALLOW GUARD (unhandled Super chord → calm no-op, flip the pinning test) fires as a quick fix right after the pre-ship sweep lands, BEFORE the deploy. NO other report item implemented until the user picks. Morning deliverables: a report + the veto galleries (JP-style) + selection captures. 🐛 THEME-PICK LANDS IN RECENT FILES (user, 2026-07-09 evening — REGRESSION, pre-ship BLOCKER #1): Switch theme → select → the app opens what reads as the recent-files menu. PRIME SUSPECT: the breadcrumb round (b7cf4f7 AcceptDisposition/return_to) × lens folds (c638c96 REMOVED OverlayKind variants) — if the parent kind is stored/rebuilt by enum POSITION anywhere (ALL/ALL_KINDS arrays, an index into a kinds table, sidecar mode mapping), every kind after the removed ones SHIFTED: "return to palette" decodes as "return to Goto" (opens on its Recent lens = a recent-files menu). Diagnose the actual mechanism before fixing (check: return_to stamping on ⌘T-direct summon — no parent should mean CLOSE-ALL; the dispose_after_accept re-summon path; any kind-by-index code). Fix + a law test making kind round-trips value-based (an enum removal must never re-aim a breadcrumb). Repro headlessly: --keys palette→theme→Enter AND ⌘T→Enter, assert final overlay state.
🗂️ CODE ORGANIZATION PASS (user, 2026-07-09 evening — post-deploy, right after the table round; sequential on main, NO parallel lanes during it): re-apply the ~500-line ceiling to the monsters. BEHAVIOR-ZERO — moves + module plumbing only, no logic edits; the suite must end with the IDENTICAL test count, all green. Targets (biggest first): render/tests.rs 9.3k → render/tests/ dir split by feature area (caret/wysiwyg/images/syntax/cjk/geometry/…); actions/tests.rs 3.5k + capture/tests.rs 2.6k + caret/tests.rs 1.8k same treatment; product files: overlay.rs 3.4k → overlay/ dir (state/build/capture-substates), markdown.rs 2.7k, theme.rs 2.2k, config.rs 2.2k, app/input.rs 1.9k, history.rs 1.8k — each along its natural seams. render.rs 3.6k = the DECLARED GPU-core exception (re-affirm or narrow the declaration, don't blind-split). CRITICAL SWEEPS: (1) CLAUDE.md + docs cite dozens of test paths (render::tests::foo) — every moved test's new module path must be swept through the docs; (2) module-privacy seams (the single-owner bypass-proofing) must survive the moves — re-verify pub(super)/pub(crate) boundaries didn't widen; (3) the law tests enumerating files/initializers (ViewState's six sites) get their path lists updated. One commit per target file/dir, suite-gated each, so any regression bisects to one move. STRUCTURAL HALF (user asked "what else makes it cleaner", 2026-07-09 — each earned by a real bite today): (1) ViewState::base() canonical constructor + per-site overrides — kills the six-initializer merge footgun + its law-test ritual; (2) ONE test_serial() mutex replacing the about/lifetime/page/theme/fs ordered-lock family — makes the ABBA class (today's 3-way deadlock, likely the wash_cache flake too) unrepresentable; slightly coarser test parallelism accepted; (3) sidecar SCHEMA as one const + append-only history table — kills the schema-chain merge conflicts (bit the merge train today); (4) unify about/lifetime/peek into ONE summoned-card owner (three copies of open-flag + any-key-dismiss + float-card gate — same-behavior-same-code law). Each structural item = its own suite-gated commit; behavior-zero except where the change IS the contract (test-lock collapse).
• PER-WORLD LIST BULLETS (user, 2026-07-09 evening — "more fun icons… hand pointer glyphs… maybe per world?"; post-deploy taste round, the ornament-trio pattern one level down): the list-bullet conceal glyphs (• level-1 / ◦ level-2) become PER-WORLD DATA in the world's own ornament face — antique/literary worlds draw from hederas ❧ / small florets / the JUNICODE MANICULES ☞ (the medieval margin-pointing hand — deeply in-character for the antique worlds); technical/modern worlds KEEP geometric •/◦ (restraint IS their character). Calm rule: bullets are rhythm not punctuation — quieter than the section ornaments, faint ink unchanged, bullet-sized. Law test per the ornament-trio pattern (every world's bullet pair present in its face); veto gallery. ALSO NOTED from the same screenshot's own text: nested ordered items render the marker AS TYPED ("2." under "1.") — whether WYSIWYG should DISPLAY renumbered (1.1 / proper sublist restart) is an open design question, banked (Obsidian shows as-typed too; renumber-on-render is a deeper semantic move).
📐 TABLE COLUMN ALLOCATION (user + WORLDS.md-table screenshot, 2026-07-09 evening — "Da wn"/"Tim e" mid-word breaks; fires as its own round RIGHT AFTER the deploy, redeploy is cheap): adopt the CSS auto-layout shape in the tables-v2 engine — (1) MIN-CONTENT FLOOR per column: ≥ the longest unbreakable word incl. the header; a word NEVER breaks mid-word; (2) surplus distributed ∝ (max-content − min-content) so phrase columns (Ground/Ornament) absorb squeeze by WORD-boundary wrapping while token columns stay rigid; (3) floors > measure → modest growth into margins first, THEN (user revised the tables-v2 no-scroll call, 2026-07-09): the grid PANS HORIZONTALLY within the table block — trackpad horizontal scroll over the table; indicator = a THIN transient dim bar at the table's bottom edge (appears on hover/while scrolling, fades; value-step tint, never amber, never persistent chrome). KEYBOARD STORY (settled, already true): the grid is the READING view — keyboard users drop to SOURCE by caret-entry (the WYSIWYG rule), where the soft-wrapped raw line shows everything; grid panning is a reading gesture like ⌘-click, mouse/trackpad-only by design. Mid-word breaks only in the degenerate single-word case. Tests: WORLDS.md table as fixture (Dawn/Time never split); pan state live-only, geometry unit-tested; law: no cell wraps mid-word. PLUS (user, same evening — keyboard walk balloons the table: each caret-entered row's long source line soft-wraps to 3-4 lines, table dances): ZERO-REFLOW ROW REVEAL — "THE X-RAY" (user's own metaphor, 2026-07-09, canonized: the caret is an x-ray into the standing structure — the grid/render NEVER collapses; the caret's row goes translucent showing the markdown bones in place, heals on exit; nothing else ever shifts) — the image-caption decision generalized — caret on a table row keeps the GRID ROW HEIGHT, dims the rendered row, floats the raw source as ONE NON-WRAPPING line over it, PANNING horizontally to follow the caret (the find-field single-line pan model); the table never changes height during a keyboard walk. BANKED (the endgame, not now): in-grid cell editing (Obsidian/Typora model — caret lives in a cell, Tab hops cells, write-back edits; a structured-sub-editor arc + the first exception to drop-to-source).
≠ PROSE LIGATURE LEAK (user + ==highlight!!== screenshot, 2026-07-09 evening — rides pre-ship): in a mono-display world (Potoroo/Monaspace), PROSE keeps calt on (f282df8's "never disabled calt, made explicit" hole) → Monaspace's programming ligatures fire in markdown: `!!` + the `==` highlight delimiter forms `!=` ACROSS the content/markup span boundary (spans differ only by color → one shaping run) and renders ≠, fusing prose with markup. FIX: the policy goes strictly PER-BUFFER — prose/markdown = liga only, calt OFF even on mono faces (texture healing is a code feature; forfeit it in prose); code buffers unchanged. Test: ==x!!== in md renders two dim trailing equals, never ≠ (advance/glyph assertion); mono_world_shapes_uniform_pitch stays green.
🎨 MENU ICON TEMPLATE FIX (user + File-vs-system-Edit screenshots, 2026-07-09 evening — rides pre-ship): awl's File-menu icons are flat mid-gray bitmaps (the icon round's documented muda limitation: no template-image constructor) — they can't adapt to appearance/vibrancy/highlight, so they read off-color vs the system's template icons. FIX via the in-tree objc2 door (mac_chrome.rs precedent): after menu install, walk the NSMenuItems and set each icon NSImage's isTemplate = YES → AppKit tints to label ink in every state. Native/live-only (smoke-script tier + a user eyeball); no capture impact.
💬 PULL-QUOTE OFF THE MARGIN (user + screenshot, 2026-07-09 evening: "the quote shouldn't be in the gutter right?"): the blockquote's big dim hanging " renders in the LEFT MARGIN — which the outline now owns BY DEFAULT (today's flip made the collision the default experience; the glyph reads as a stray outline row). FIX: anchor the pull-quote glyph INSIDE the writing column at the quote block's own indent (drop-cap logic — the mark belongs to its paragraph, not the page edge); margin = outline's alone. Blockquote-round render files; rides the pre-ship round.
🟩 SELECTION PILLAR ON IMAGE LINES (user + screenshot, 2026-07-09 evening — the giant-caret bug's sibling, rides pre-ship): selecting chars in a revealed image line draws the selection quad at FULL IMAGE-ROW HEIGHT (char-width × ~2000px pillar) — the caret got pinned to the text glyph box (136fdd0) but selection/squiggle row-bands still use the row height. FIX: on an image line, selection + squiggle bands size to the CAPTION TEXT BAND (same anchor the caret and scrim plate use — one owner), never the reserved image height. Test alongside the caret one.
🖍️ SELECTION VISIBILITY (user + Undertow screenshot, 2026-07-09 evening: "you really can't tell it's highlighted" — DOCUMENT text-selection quad, NOT the picker band 0312d4a already fixed): the selection tint on dark worlds sits a hair above ground — fails the glance test. FIX measurably: raise the per-world selection-vs-ground contrast floor (law-test it with composited ΔL/redmean bounds, the wash-law pattern — if a selection law exists its threshold is too low, else add one), re-derive so EVERY world clears it (worst: Undertow/Currawong/Mangrove; check light worlds don't overshoot), stay in-hue (calm, no new color, never amber), keep the copy-pulse brighten composing on top. Before/after captures for the taste call. Fires AFTER the build queue lands (render/theme territory — no race), BEFORE deploy.
THEN: WORLDS.md CJK-section update (user spotted it stale 2026-07-09: missing Shippori Mincho/Zen Maru Gothic/Klee One + the per-world ja assignments — Klee One was missing even pre-today; update the fonts-we-ship CJK list + name each changed world's JP face in its blurb/table, AFTER the JP veto settles so the doc describes what survives) → FINAL wasm rebuild + fly deploy (welcome rewrite is in — deploy unblocked once the queue lands). Post-deploy: theme-packs arc (user hasn't pulled it forward), CJK companions (pending JP veto), image caption taste items. JP FACES ✅ DONE on branch jp-typography-faces @ ccb6e20 (suite 1568×green; 3 new OFL faces incl. Shippori Mincho; veto galleries at gallery/jp-worlds/; ANCESTRY: parent = 3539807 = the train's step-2 branch, so it SUPERSETS the bold fix — after the train lands, MERGE 4 = ccb6e20 brings only the faces commit; if step 2 hadn't merged, ccb6e20 alone covers both). Merge 4 dispatches when the train reports. THEN: discoverability round → breadcrumb-back → web/linux menu bar (+ welcome-copy native-first rewrite + WebGL2 init fix + no-path paste) → FINAL wasm rebuild + fly deploy.
(historical) 🔵 IDENTITY ROUND — phases 1-2 ✅ LANDED: 8bcc361 (renames/vocab purge, suite 1567×green) + 66f720c (native-first chords: ⌘O verified opening Goto end-to-end; emacs defaults retired; suite 1563×green). AUTH OUTAGE killed phases 3-4 mid-flight (+ the JP faces phase); the dying lens-folds agent left non-compiling WIP in the shared tree — RECOVERED: WIP banked to stash@{0} + ~/.claude/jobs/891e5c3b/tmp/lens-folds-partial.diff (reference only, known gaps: FacetItem.heading missing, non-exhaustive OverlayKind match), tree reset clean at 66f720c. RESUMED from cache (run wf_816e2999-a10 → task wq0c6f3t2): lens folds re-running with the reference-diff note (must git stash drop on success), then docs. JP workflow also resumed (wf_e0ef188f-a90 → wz8ihqb9b): phase-1 bold fix ✅ on branch jp-typography-fix @ 3539807 (cached), faces phase re-running. LESSON: worktree isolation requested for JP phase 1 did NOT apply (agent launched in the shared checkout — same class as the Explore/haiku miss); its self-rescue was clean, but watch workflow isolation flags.
🖼️ IMAGE SIZING SANITY (user hit it live 2026-07-09, "man this is really weird" — a native-retina paste |2241 becomes a full-bleed wall and the caption reveal dims the whole viewport; fires RIGHT AFTER focus removal): (1) PASTE WIDTH DEFAULT — stop stamping native pixel width on paste; stamp nothing (fit-to-column decides) or the displayed fit width (orchestrator pick: stamp NOTHING — the file keeps full res, |W stays a user gesture via drag-resize); (2) VIEWPORT-HEIGHT CAP — display size never exceeds ~65% of window height (named taste const, proportional, applies over any |W; drag-resize respects the same ceiling). Both flagged taste, user will tune. (3) REVEALED IMAGES MUST ARM RESIZE HANDLES (user hit it: "the handles are gone") — image_hit_rects (render/text.rs:515) skips `im.revealed`, a rule from the OLD hidden-image reveal model; post-caption the image is always drawn, and the trap is that paste/click puts the caret ON the line → revealed → resize unreachable. Drop `revealed` from the exclusion (keep missing + ornament-visible gates), flip the test contract that pinned the old rule.
🧹 ASSET CLEANER — ✅ BUILT ON BRANCH, AWAITING MERGE TRAIN: branch worktree-wf_fa4f8135-be7-1 @ a3e0feb (base 64cd3ec; suite 1585×green in worktree; ZERO new crates — Trash via in-tree objc2 NSFileManager trashItemAtURL, 2 additive feature flags, Cargo.lock unchanged; schema /145→/148 in-branch — EXPECT schema-number + commands.rs/overlay.rs conflicts vs the identity round; merge AFTER identity lands, merge agent if non-trivial, full suite gates the merge): summoned "Clean unused assets" palette command — scan every markdown file in the project (NESTED included), resolve every image reference against THAT doc's own dir (same resolve_image_path semantics the renderer trusts), orphan = asset no resolved reference lands on; picker lists orphans (name/size), deletion = move to macOS TRASH, never rm (recoverable by design; also the mitigation for out-of-project references). NEVER delete as an edit side-effect (undo safety — settled). SETTLED 2026-07-09 (user confirmed queue + design): scan scope = the ENTIRE project, nested included; asset-folder convention = per-doc-dir sibling assets/ (current behavior, now BLESSED — folders stay self-contained/portable, references stay short; NO config knob v1, Obsidian-style location modes explicitly rejected as knob-creep). NO-PATH PASTE — SETTLED (user 2026-07-09 "just save the file"): pasting an image into a no-path buffer TRIGGERS THE EXISTING quick-note auto-name save FIRST (the note becomes real under notes_root), then the paste proceeds normally (sibling assets/, relative ref). Rejected: a global data-dir holding assets/ — it sits outside every project, so the cleaner could never collect it (the user's own "cleanup later" criterion kills it) and the relative ref breaks on save-as. SMALL ROUND: lands with/after the asset-cleaner merge (paste_image.rs + the auto-name seam; the cleaner agent is reporting the current no-path behavior as input).
REMAINING main-lane order (2026-07-09 morning session): focus removal (running) → image sizing sanity → IDENTITY ROUND → DISCOVERABILITY ROUND → asset cleaner → wasm rebuild + fly deploy → IDENTITY ROUND (renames + native-first + 4-door chord consolidation + emacs-default retirement + philosophy amendment) → DISCOVERABILITY ROUND (hold-⌘ peek + tips footer + ledger) → wasm rebuild + fly deploy → settings pass (diagnose-first: CJK-Enter + toggle-state look already wired in-tree; §8.8 stale-comment fixes are real) → focus removal pass 1 → pass 2 → FINAL wasm rebuild + fly redeploy. USER CONFIRMED AWAKE 07-09: noticed the live web build looks stale (themes differ) — deployed bundle = ef6f17c, missing outline-ink 2-state + selection band + setext + hint line; the final redeploy fixes it (offered an early deploy, default is end-of-lane).
RUNNING (background): (a) command-organization-report workflow → a MORNING markdown doc: full command inventory + RENAME proposal (fix the toggle-vs-not inconsistency) + taxonomy + MINI-PANEL (caret-style picker) expansion opportunities. Deliver as markdown (NOT a web artifact). (b) web-palette-nav bug diagnosis (Ctrl-Up/Down doesn't move the palette highlight on WASM) → fix after. (c) command-smoke-test workflow → execute EVERY command (apply-seam + full-stack --keys), fix crashes, add a permanent no-wildcard smoke guard, commit.
MAIN LANE (sequential on main, one at a time — smoke-test commits first, then): 
  1. WEB PALETTE NAV — DIAGNOSED + BROWSER-VERIFIED (Playwright on the live site). NOT a code bug: Ctrl-Up/Down works in headless Chromium (moved the highlight 2 rows); on the user's real macOS, Mission Control / App Exposé EATS Ctrl-Up/Down before the browser sees it. Plain ↑/↓ and C-n/C-p work fine on web (verified). TWO real fixes: (a) the palette HINT LINE ("↵ run  ←/→ lens") never teaches the up/down move keys — ADD "↑/↓ move" (or C-n/C-p) so nav is discoverable. Owner: overlay.rs hint-line builder (must wait for smoke-test — same file region a command-crash fix might touch). (b) the selection HIGHLIGHT band is faint/low-contrast (user: "i don't see the highlights") — strengthen it; TASTE, capture for review.
  2. LOADING SPINNER — DESIGN LOCKED + browser-tested. Currently the cold load is a blank #1a1a1a void (no loader). Loader = a breathing amber block CARET (#e8a04c, awl's one-warm-thing) + muted "awl" wordmark, on #1a1a1a; pure CSS breathe (1.5s), removed on the TrunkApplicationStarted event (fade 420ms), pointer-events:none, reduced-motion guard. Ready snippet: /Users/frank/.claude/jobs/3ce0c357/tmp/spinner-preview.html. Integrate into BOTH /index.html (trunk SOURCE, data-trunk) AND site/editor/index.html (built, for the live deploy without a wasm rebuild). Web-only. After: fly deploy from site/ (static-only, no wasm rebuild needed) so duhai gets it.
  3. STATS SCOPE SPLIT (Cmd-I HUD = per-doc/session only; lifetime odometer → a NEW summoned "Lifetime Stats" view, reuse About/HUD float-card).
  4. SETTINGS PASS (CJK-priority Enter no-op fix + show toggle state).
  5. FOCUS MODE REMOVAL pass 1 (remove CycleFocusMode/focus state/render/focus.rs/dimming) then pass 2 (rearchitect the span system).
After the queue drains: consider a fresh wasm rebuild + redeploy so duhai gets the polished build.
NOTE: report is read-only (parallel-safe); all code lanes serialize on main to avoid commit races.

# 🔧 OUTLINE & HUD ROUND (2026-07-08) — combined (shared outline.rs/hud.rs/render.rs), fire AFTER artifact diagnosis returns
1. **Outline ink → 2-state (user chose "all faint, current dark").** Simplify render/chrome/outline.rs row_rung: DROP depth-floor + ancestor-lift. Every heading = FAINT; the CURRENT heading = CONTENT ink. Nothing else. Depth still shows via indent. Reconcile the edge-fade (no rung below faint → use alpha) + keep group gaps. (The current 4-shade depth×lit design read muddy on light grounds.)
2. **Cmd-I artifact bug** (diagnosis RUNNING) — margin chrome (outline + blockquote pull-quote marks + gutter) draws SHARP+GARBLED over the HUD frosted-blur (they're persistent chrome, not overlays, so the hud_showing() overlay-exclusion doesn't suppress them). Likely fix: gate the margin chrome on !hud_held() (a "hold to peek" dims the whole room to the card), OR draw them into the blurred texture. Await diagnosis for the exact cause.
3. **Stats scope split (user).** Cmd-I mixes per-DOC + LIFETIME stats → lifetime reads as session-scoped. FIX: Cmd-I held HUD = per-doc/session ONLY (Word Count, Through Doc, Line Endings, + session/file-created if present). MOVE the lifetime odometer (Characters, Time Writing, Files Touched, Caret Travel, Your World — already persisted in stats.toml, confirmed lifetime) to a NEW dedicated **"Lifetime Stats"** SUMMONED view (palette command, reuse the About/HUD float-card pipeline; summoned+dismissable, NOT held — you read lifetime stats, not peek). 
4. **SETEXT HEADING BUG (user).** Typing a `-` on the line below a paragraph made that paragraph a heading (in the OUTLINE only — not heading-size, since size uses #-count). Root cause: `markdown::headings` (outline source) uses pulldown's Tag::Heading which INCLUDES setext (===/--- underlines); heading-SIZE (md_line_scale/heading_scale) only counts ATX #. Inconsistent + surprising (the - underline collides with lists/hr). FIX: only ATX #-prefix headings count EVERYWHERE — filter setext out of markdown::headings/headings_from_spans (a setext heading = a Tag::Heading whose line doesn't start with #). Makes the outline consistent with heading-size + kills the surprise. Obsidian-style. Test: a paragraph followed by a `-`/`---`/`===` line is NOT a heading in the outline.
DONE this session recently: debug right-align (3d6e5c7). Ornament per-world scale (ornscale branch, running → merge). 

# 🗺️ ROADMAP (user's tiers, 2026-07-08)
## Tier 1 — promised/unfinished
1. **INLINE IMAGES — VERIFIED RENDERS ✅ (2026-07-08).** Confirmed with a real capture (imgcheck.png): tiny.png draws as real pixels inline (missing:false, 120x48), nope.png shows the calm placeholder card. So NOT "no pixels" — v1 draws. If the user sees blank live it's a relative-PATH-RESOLUTION bug (needs their exact ![](path) to repro). REMAINING image work = v2 drag-resize (page-resize + write-back templated, Obsidian ![alt|300]) + v3 web OPFS storage. The drawing is DONE.
2. **LINKS v2 (the last markup family showing plumbing).** Conceal [brackets](url) → just the link text off-caret (the same reveal-on-cursor rule as heading/emphasis; links were explicitly OUT of the WYSIWYG-conceal v1). PLUS a "follow link at point" command (open the URL under the caret) — palette + chord (org-mode C-c C-o is the emacs-slot precedent). Touches markdown.rs (link spans already parsed as Markup) + render/spans (conceal) + a new Action/command.
## Tier 2 — small, cheap, in-philosophy
3. **PASTE-URL-OVER-SELECTION → LINK.** Select "the essay", paste a URL (clipboard content is a URL) → [the essay](url). One undoable edit, zero chrome. Touches the paste path (app/input or actions/edit) — detect URL-shaped clipboard + non-empty selection → wrap.
4. **TYPEWRITER SCROLL** (sticky toggle, default off). The caret line stays vertically PINNED while you type — the room stays still, you stay centered (scrolling counterpart of focus mode / iA Writer). Deterministic + capture-testable. Touches the scroll math (render/rowgeom / app/viewstate scroll-follow) + a sticky config toggle (→ settings menu) + palette command.
5. **CONVERT LINE ENDINGS palette command.** Buffer::set_eol ALREADY EXISTS (built for exactly this). A palette command (LF ↔ CRLF) → set_eol → the autosave engine rewrites with the new ending. One-command loop-closer. Small.
6. **THE CONSCIOUS MARK.** A pinned, prune-EXEMPT snapshot ("keep this version") via palette — banked in the history design (prune_ladder/snapshot_after_save seam comments). Completes local-history: awl remembers everything EXCEPT the versions you cared about. Touches history.rs (a pinned flag exempt from prune_ladder) + a palette command + the history picker (show pinned).

# 🧭 OUTLINE V1.5 (2026-07-08 — user's detailed design crit) — orientation, not a list
Touches render/chrome/outline.rs + rowlayout.rs + cursor_shape.rs + app/input.rs + outline data (ancestor chain). SURE WINS (do directly): 
1. **ANCESTOR TRAIL** — the 3-rung ladder as a you-are-here breadcrumb: current heading MUTED (as now), its ANCESTOR CHAIN (the H1/H2 you're inside) ALSO lifted one rung, everything else FAINT. Caret in "Mopoke" → outline reads WORLDS.md → Each world → Mopoke as a lit path through dim surroundings. Needs: compute the ancestor chain of the current heading (walk up by level). Pure figure/ground, no new hue.
2. **INK BY DEPTH** — H1/H2 rows one rung ABOVE H3+ rows (value + indent, not just the near-invisible 2-space indent at LABEL scale). Structure survives even without the caret. Echoes how the doc itself does figure/ground.
3. **GROUP RHYTHM** — a half-row blank gap before each top-level (H1/H2) section except the first (the outline is a standalone glyph buffer → a short blank line does it). Breaks the wall into visual paragraphs.
4. **TITLE ELISION FIX** — heading text is front-loaded → END-elision ("WORLDS.md — the themes…"), NOT fit_primary's middle-elide (which is right for filenames). Even better: progressive truncation for em-dash-subtitle titles — drop the subtitle first, elide the head only if it still doesn't fit. Belongs IN rowlayout as a documented VARIANT (per same-owner rule), not a bypass.
6. **EDGE FADE on the follow-window** — when the slice slides on a long doc, render the first/last CLIPPED row one rung fainter (or a short alpha ramp) → quietly says "more above / more below", no scrollbar chrome.
7. **CLICKABLE + POINTER CURSOR** (user asked) — click an outline row → jump to that heading (jump_to_line); pointing-hand cursor on hover. NOTE: the outline is persistent MARGIN CHROME (not an overlay), so it needs its OWN cursor_shape flag (composes with the cursor pass). PHILOSOPHY: amend the just-logged "never interactive" outline clause → "click-to-jump only" (benign, user-approved; still not resizable/focus-target IDE furniture).
8. **ANCHORING (user follow-up).** VERTICAL: keep TOP-ANCHORED, do NOT center (centering DANCES as headings are added/deleted — cardinal sin for ambient chrome; also preserves the outline-top / gutter-bottom opposition). HORIZONTAL: right-anchor the block to the COLUMN, not the window edge — compute the block's natural width (widest fitted LABEL line), place it so its RIGHT edge sits at `column_left − gap` (the SAME gap/axis the gutter uses), so it hugs the text column + moves with it. Lines stay internally LEFT-aligned (indentation is a left-to-right signal — don't right-align the text). This REPLACES the v1 "left margin band from TEXT_LEFT" left-anchoring.
9. **DEBUG PANEL → TOP-RIGHT (user).** Debug panel is currently TOP-LEFT (debug.rs); the outline now owns the top-left margin → clash. Move the debug panel to the TOP-RIGHT corner. Small corner-placement change in debug.rs; folds into this chrome round. (Debug is opt-in DEBUG-only, so no determinism impact beyond the existing gate.)

TASTE CALL — CAPTURE FOR REVIEW (build but default-conservative, don't silently enable):
5. **REVEAL-ON-CARET DEPTH** — always show H1+H2; show H3+ ONLY inside the section the caret is in (the WYSIWYG conceal rule generalized to the outline: "detail where you are, preview elsewhere"). Composes with / mostly replaces the follow-window on medium docs. User flagged "prototype before committing" → SHIP AS A CAPTURE the user eyeballs; default off or note it.

# 🎛️ SETTINGS V2 (2026-07-08 — user hit the dead rows) — inline editors for the deferred value/path/list rows
The 7 v1-no-op rows: Page width prose/code + Zoom (Value), CJK priority (List), Notes/Workspace/Project root (Path). v1 shipped Toggle+Picker; these no-op on Enter.
- **Values (page widths, zoom):** inline number edit / ±step on Enter, write via persist_page_width/persist_zoom.
- **Paths (notes/workspace/project):** inline text edit OR route to the Browse navigator to pick a folder.
- **CJK priority (List) + optionally the paths:** a bespoke inline-reorder UI is over-engineering for a rare setting → route Enter to "Edit config as text" at that key (open config.toml). Pragmatic.
- Touches settings.rs/overlay.rs/actions/overlay_nav.rs/app/apply.rs — overlay/settings territory, disjoint from the render chain (blockquote/ligatures) + stats (hud). Fire when a lane frees.

# 🔨 WYSIWYG POLISH ROUND (2026-07-08) — 3 cooks, sequential on main (shared render/markdown files)
1. **Bold weights** 🔵 RUNNING — bundle a real Bold(700) for the 10 proportional display faces (instance+subset, same CJK recipe); wire the Bold arm so `**bold**` shapes real bold not the mono fallback. Monos stay Regular-only; headings still size-not-weight. Probe-first (confirm same-family-700 resolves).
2. **Tables v2** ⏳ (fires when bold commits) — (a) INLINE MARKDOWN in cells (conceal `**`/`*`/`` ` `` markers + apply styling via the existing md_attrs seam — bold headers now real after cook 1; (b) WRAP-NOT-CLIP: too-wide table wraps cell text → grows the row (calmest, no scroll chrome), instead of hard-clipping mid-word ("Monaspace Xen…"). Reuses the tables-grid cell TextAreas.
3. **Blockquote WYSIWYG** ⏳ (fires after tables v2) — conceal the `>` markers off-cursor (reveal on-cursor, like heading/emphasis); render a BIG DIM HANGING QUOTATION MARK in the world's own display serif (pull-quote), text indented beneath. Rejected: §/¶ (semantically muddy — mean section/pilcrow, not quote), signature line (no author in md `>`). The `"` is semantically honest (a real quote, Butterick's no-quotes-for-emphasis doesn't apply). Optional gentle dim of quote text — capture for the taste call.
4. **Ligature policy** ⏳ (fires after blockquote) — awl currently DISABLES all 3 ligature classes (render.rs:939 + render/text.rs:90). Change to a THREE-WAY SPLIT: PROSE (proportional faces) → STANDARD ligatures ON (fi/fl collision-fixers, Butterick-approved); CODE (mono, code buffer) → PROGRAMMING/contextual ligatures ON (=> != -> >= :: — what these monos were built for); DISCRETIONARY → OFF everywhere (the quaint st/ct). Config toggle `code_ligatures` (default ON, → settings menu). **PROBE-FIRST for the Monaspace Xenon risk** — its "texture healing" (calt advance adjustment) may break `mono_world_shapes_uniform_pitch`; JetBrains Mono/Iosevka preserve strict advance. If Monaspace breaks the pitch grid, enable selectively or leave it plain. VERIFY caret/selection/hit-test INSIDE a ligature cluster stay correct (the `caret_ink_box_off_for_mono_and_ligature_cluster` test already handles some of this). Touches render.rs/text.rs FontFeatures — sequential after the others.

5. **Project picker → Recent lens** ⏳ (queued) — the switch-project picker (C-x p, OverlayKind::Project) currently has NO lens strip (flat workspace-folder list, →open/←up navigation). Add an `All · Recent` faceted lens (Recent = the EXISTING recents.rs store, MRU order — same data the standalone RecentProjects picker + File menu use). Requires the BROWSE-STYLE arrow reassignment: ←/→ → lens cycling, Enter → open/descend, Backspace → up (makes Project consistent with Browse — both navigators). BANK: could later fold the standalone OverlayKind::RecentProjects picker into this (File-menu "Recent projects" → opens Project on the Recent lens), retiring a redundant picker kind.

# ⭐ THE WYSIWYG PIVOT (2026-07-07) — awl's new north star

**Decided by the user:** awl is now a **WYSIWYG editor** (was: "source editor, dim-the-markup"). Framing: awl's reveal-on-cursor conceal ALREADY IS the WYSIWYG model (Obsidian Live Preview) — this commits to finishing it by rendering the block content that still shows as text. Keeps the calm aesthetic + mg keybindings + reveal-on-cursor (drop-to-source). NOT a Word clone; "Obsidian Live Preview with awl's taste."

**The WYSIWYG initiative (next big arc):**
- [ ] **Inline images** (design spec DONE — deeply grounded, low-risk). v1 native render (reuses heading rows + fence-conceal + a caret_glyph-templated ImageQuadPipeline) → v2 drag-resize (page-resize + write-back templated, Obsidian `![alt|300]`) → v3 web OPFS blobs. Open Qs: fence-vs-heading reveal model (rec: fence), size syntax (rec: Obsidian pipe), PNG-only v1 (rec: yes), native-only v1 (rec: yes), DESIGN.md amendment (rec: yes).
- [ ] **Tables as GRIDS** ← USER PICKED THIS FIRST. Supersedes the mono-source align fix. A real mini table-layout engine (shape cells → per-column pixel widths → place cell text at column x-offsets → separators → reveal-on-cursor source via the fence/image block model). Comparable scope to images → design-then-build workflow. Taste default: CALM-MINIMAL (aligned columns + one faint header rule + subtle gaps, NO heavy ┌┬┐ borders) → capture for review, box-grid is an easy flip. FIRES when the picker-bugs workflow commits (sequential on main).
- [ ] **PHILOSOPHY.md + SCOPE.md + DESIGN.md amendment** — name the pivot. THE HONEST COST to log: an image is "the first element awl renders whose palette it does not control" — breaks the one-warm-thing discipline; unmitigable; a conscious accepted exception (like copy-pulse).
- Web storage RESOLVED: native→disk, web→**OPFS/IndexedDB binary blobs** (the banked sandboxed-web-storage seam; images are its forcing function).
- [ ] **Formatting commands** (block + inline toggles, full set, palette + Cmd-B/I) — 🔵 RUNNING on main. Block: quote/bullet/numbered/task/heading/code-block on selected lines; inline: bold/italic/code/highlight/strike on a selection. All toggle + undoable.

**WYSIWYG build order (order doesn't matter per user; sequential on main):** formatting-commands (running) → **UI-polish batch (NEXT — user's actively looking at these)** → tables-grids (design-then-build, calm-minimal default) → images v1 → philosophy amendment. Picker-bugs DONE (`a19e621`).

## ⏳ OUTLINE (new feature — design pass then build)
DECIDED: persistent margin table-of-contents = "Outline"; the old summoned jump-to-heading FOLDS INTO Go-to as a "Headings" lens (heading = a jump target; fits the lens machinery); DROP the old summoned `OverlayKind::Outline` picker.
- **Persistent margin TOC** — faint, dim, in the LEFT MARGIN (page-mode margin space), ambient marginalia like the existing bottom-left gutter. GRACEFULLY HIDES when the window's too narrow (same degradation as the filename/project gutter — rowlayout::gutter_plan precedent).
- **OFF BY DEFAULT, opt-in** — a sticky config toggle (`outline`, like wysiwyg/autosave). The toggle lives in the new settings menu. Calm-minimal default holds.
- **SHOWS CURRENT POSITION (the whole point — "don't get lost in a long doc")** — highlights the section the caret is currently in (the heading you're under); tracks live as you scroll/type; maybe a faint progress marker. Sidecar-assertable current-heading.
- Philosophy: persistent chrome — needs a logged DESIGN amendment (like the WYSIWYG pivot). Defensible as ambient-marginalia-that-hides (not a fixed IDE sidebar), + opt-in-off-by-default keeps the calm default.
- Go-to gains a "Headings" lens (when in a doc): ←/→ to Headings, jump to a heading. Reuses the lens machinery.
- DESIGN DONE (ready to build). Data reuses `markdown::headings` (parse already happens each reshape); render reuses the GUTTER machinery (a TextRenderer in the left margin TOP-anchored — gutter is bottom-anchored — coexist by splitting the band vertically); graceful-hide reuses the gutter floor (page-mode-only, hides when margin too narrow); current heading = nearest at/above caret, highlighted ONE value-rung up (muted vs faint, NO amber); cursor-move recompute is O(headings) gated like refresh_rule_conceal (0% idle CPU). Off-by-default `outline` sticky global (outline.rs, wysiwyg pattern). Sidecar `outline {on,headings,current}` (NO new ViewState field — reuses cursor_line/md_spans). DESIGN.md amendment logged (the "orientation lingers in TWO margin surfaces" widening).
- **DEFAULTS CHOSEN (autonomous drain):** current=muted · long-doc=FOLLOW-to-keep-current-visible · progress-marker=defer to v1.5 · Cmd-Shift-O→Toggle Outline · click-to-jump=defer (v1 strictly non-interactive per philosophy). All flagged for morning eyeball (ink taste + follow feel are live-only).
- **v1** = persistent margin outline (render+current+toggle+sidecar). **v2** = the Go-to Headings-lens fold (needs a real FacetScheme generalization — corpus+accept swap; retire the summoned OverlayKind::Outline; the no-wildcard law tests drive the removal sweep). Build v1 first; v2 after.

## ⏳ SETTINGS MENU (design DONE — ready to build v1)
Navigable "game-style" settings menu. DESIGN: reuse the faceted-lens machinery — **categories AS lenses** (All·Editor·Appearance·Writing·Files·Keybindings·Advanced), settings a flat corpus, VALUE in the rowlayout SECONDARY column (like the palette binding cell → NO new render primitive). Summoned overlay (not persistent chrome). config.toml stays truth; write via existing format-preserving `write_pref`. New seam: `return_to` breadcrumb (open a sub-picker → return to Settings). 6 categories/18 settings audited (see the design transcript).
- **v1 build:** `OverlayKind::Settings` + `SETTINGS_FACETS` (facets::scheme arm) + `SettingRow` table + build arm. TOGGLES fully working (page_mode/spellcheck/writing_nits/autosave/history/session_restore/wysiwyg — flip via setters+persist_pref; EXTEND the persist_pref mirror-match with the 4 missing keys; ADD markdown::set_wysiwyg_on live call + persist_wysiwyg — closes the wysiwyg-live-apply gap). PICKERS reuse existing Theme/Caret/Dictionary sub-pickers via the return_to breadcrumb. Keybindings row → opens existing OverlayKind::Keybindings (the fold-in). "Edit as text" row → Effect::OpenSettings. Value cells read effective values from the same owners the renderer reads. Sidecar mode:"settings" (rides existing bindings/lens fields; schema bump /121→/124 conservative). --keys tests.
- **v2 (defer):** inline number edit (page widths/zoom), path edit (notes_root/workspace), cjk_priority reorder, reload_config parity.
- The Outline opt-in toggle lands under Appearance/Editor once both ship.

## SETTINGS BUILD ORDER: v1 fires in the main chain after images (or wherever its slot lands). Design is ready — no more decisions needed; use the v1 slice above.

## UI-POLISH BATCH (fires after formatting-commands):
- [ ] REMOVE the overlay rise animation (user: appears instantly). Undo/simplify the Batch-3 overlay motion (10px/150ms rise) — overlay just appears.
- [ ] DROP the "All" lens tag — strip shows only the facets (Recent·This folder·By type / Time·Register·Voice·Temp); flat/unfiltered = NO facet highlighted; ← from the first facet returns to flat. Update the strip render + the "All is index 0 home" facets convention (All is still the underlying flat state, just not LABELED on the strip).
- [ ] OVERLAY FONT HIERARCHY (taste, capture) — the picker "looks heavy": everything renders in the world's reading serif at body size. Pull the list to a UI size (smaller than reading-body), build real hierarchy via the size + ink ladders (DESIGN §4): query (medium) › items (UI size, content ink) › lens strip + hints (small, faint) › section headers (small-caps faint). Draft + capture.
- [ ] BIGGER SECTION-BREAK ORNAMENTS (user loves them, wants bigger) — scale the `---`/`***`/`___` ornament glyph up ~2–2.5× (heading-scale machinery / variable row height already exists; the tall ornament row just grows). Tunable const + capture.
- [ ] EMPTY-STATE COPY CONSISTENCY — all "no ___ (yet)" family. Recent files: "nothing opened yet — files you visit gather here" → **"no recent files yet"**. Keep: no recent projects yet / no history yet / no headings yet / no suggestions / no matches / no files here / this folder is empty. (overlay.rs empty_corpus_message + empty_lens_message.)
- [ ] REMOVE THEME-PICKER SWATCHES (user: look horrible + redundant — the live doc preview already shows each world). Revert the Batch-2 swatch work: `theme_swatch_quads`/`overlay_swatches` pipeline in theme_picker.rs, `theme::swatch_for`, the `overlay.swatches` sidecar field, the SWATCH_GUTTER indent on theme rows (names go back flush-left). Keep the sidecar schema stable or drop the field cleanly.

---

# Polish queue (2026-07-07 session)

Live UI/polish backlog from the interactive testing session. Everything chains
sequentially on `main` (one cook at a time, gated on `cargo build && cargo test`
green, staging only its own files). Grouped into cook-able batches; tightly-related
items batched to cut cycles. Taste items ship a first-draft + review captures.

## ✅ Done tonight (the overnight drain)
color-fix · menu-fix (repaint + native panels) · GPLv3 · web-smoke L1 · font-swaps ·
ornament-faces (v1) · pickers (dotfiles+cursor) · tables · grounds

## ✅ Batch 1 (bugs) — `7acbf56`
SF-icon premul (CONFIRMED via `--dump-menu-icon` — real SF glyphs) · replace-caret-x · per-world highlight (accent+165°, captures `/tmp/hl2_*`).

## ✅ Batch 6 (lens pattern) — `1f81a92` — generic `facets` module; go-to/command/history/browse all faceted, All-first. (auth-blip mid-run, recovered via resume.)

## ✅ Batch 2 (overlay visual) — `83ab6da` — theme swatches, hint-line formatter, empty-states. (audit found chrome already unified — single-owner held.)
## ✅ Website lane — `4a4e43c` (landing+llms.txt) + `b4b40ca` (/editor mount + serve.sh, "try it" live). PARKED on `website` branch for final pass.

## ✅ Batch 3 (cursor+click+motion) — `c84bfe2` — hand on all picker rows + query-input I-beam; replace click-to-switch; overlay rise-in/sink-out motion (pure geometry, capture-safe). Follow-up banked: the find/replace PANEL (self.search, not an overlay) doesn't get the pointing-hand yet — wire panel_hit into cursor_shape (small).

## ✅ Batch 4 (menu/File/project) — `be775b7` — File SF icons (confirmed via --dump-menu-icon), Switch+Recent projects, project dotfolders hidden, git text-tag, Finish File, About credits.

## 🔵 In flight
- **Batch 5 (ornament re-pick)** — `main` — running (3 distinct symbols/world; gallery to veto)

## ⏳ NEEDS USER INPUT for the closing (Batch 7 + final website pass):
1. **Plausible infra** — self-host on Fly.io vs Plausible Cloud → gives the beacon's script-src/domain. (rec: Cloud unless data-ownership is the point.)
2. **Final repo URL** — `Frank-P-Lu/awl-next` vs a rename (e.g. `/awl`) → set across site index.html + llms.txt in the final pass.

## ▶ Order: 3 → 4 → 7 → 5 (main, sequential) → FINAL WEBSITE PASS (regen shots + rebuild /editor + set URL + merge website→main). Website lane already done.

## ⏳ BATCH 1 — Bugs (land first)
- [ ] **SF-icon premul fix** — `render_symbol_rgba` builds a non-premultiplied bitmap; `graphicsContextWithBitmapImageRep` needs premultiplied → returns nil → falls back to procedural. Fix the format (extraction only reads alpha). LIVE-only to confirm.
- [ ] **Find/replace caret-x** — the replace field's reserved-caret cell x is miscomputed (floats mid-panel). `render/chrome/panel.rs`.
- [ ] **Highlight → world-derived** — currently a fixed violet on every world (feels foreign). Derive a per-world highlight hue that still pops + clears the amber guard.

## ⏳ BATCH 2 — Overlay/picker visual pass
- [ ] **Theme picker swatches** — keep the list, add a ground band + accent dot per row.
- [ ] **Picker-visual-consistency** — all summoned overlays (theme/go-to/command/browse/spell/history) feel like one system.
- [ ] **Hint-line pass** — unify the `⏎ keep  ←/→ lens …` teach-line wording/style across overlays.
- [ ] **Empty-state pass** — what each picker shows with no matches / no history.

## ⏳ BATCH 3 — Cursor + click + motion
- [ ] **Cursor pass** — generalize the pointing-hand to ALL clickable overlay rows (Command-P etc.), full priority-table audit (text=I-beam, edge=resize, rows=pointer, scrim/margins=arrow, query field=I-beam).
- [ ] **Find/replace click-to-switch** — click the replace row to focus it (mouse affordance).
- [ ] **Motion pass** — consistent summon/dismiss feel for overlays.

## ⏳ BATCH 4 — Menu / File / project
- [ ] **Full File icons** — icon Open… + Finish Buffer (+ the new Switch project); depends on the SF-icon fix (batch 1).
- [ ] **"Switch project" in File menu** — after Open… (New note · Open… · Switch project · ─ · Save · Finish Buffer).
- [ ] **Project picker: hide dotfolders** — extend dotfile-hiding to `OverlayKind::Project` (keep the synthetic `.` "pick this folder" row + `.env`).
- [ ] **Git marker → "git" text tag** — replace the `•` (no good branch glyph in a text font) with a faint right-column "git" label.
- [ ] **Recent projects** — a recent-project-roots store + a picker, AND a File-menu **"Recent projects" submenu** (Open-Recent-style, macOS convention).
- [ ] **buffer → file terminology** — rename user-facing "Finish Buffer" → "Finish File"; audit other user-facing "buffer" labels → "file" (awl is a file editor; "buffer" is internal). ("Last file"/`LastBuffer` already reads "file" — leave the internal action names.)
- [ ] **About: license + author** — the About card/panel shows `GPL-3.0` + "by Frank Lu" alongside `CARGO_PKG_VERSION` (native NSAboutPanel credits dict + the in-app card on Linux/web).

## ⏳ BATCH 7 — Web telemetry (web ONLY — native stays zero-network)
DECISION (settled): telemetry on the WEB build only; the NATIVE binary keeps its verifiable zero-network promise. Amend PHILOSOPHY.md's invariant to "the NATIVE app never phones home" (web app, a normal website, may have analytics).
ANALYTICS = **GoatCounter (hosted)** (settled — free, ~3.5KB, cookieless, kindred-spirit tool; better fit than Plausible for a calm personal tool). NEEDS: user signs up at goatcounter.com → gives the site code (e.g. `awl.goatcounter.com`) for the beacon.
- [ ] **GoatCounter beacon** — pure `<script>` tag (no Rust code!) in the site's `index.html` + `site/editor/index.html` `<head>` (the marked PLAUSIBLE-BEACON slot → rename GOATCOUNTER). Since it's HTML-only on the web pages, native has NO telemetry code AT ALL — the zero-network-native invariant is trivially/verifiably preserved (nothing to gate). Caveat filed: GoatCounter free tier is non-commercial — revisit if awl sells the build.
- [ ] **Amend the zero-network docs** — PHILOSOPHY.md + CLAUDE.md "zero-network invariant" → scope it to NATIVE explicitly; README privacy line updated (native = zero-network, verifiable; web = cookieless Plausible). Keep it honest, not buried.
- [ ] Native `cargo build` + a native `--screenshot` must be byte-identical (the beacon is structurally wasm-only). Add a guard/test that no telemetry symbol is reachable on native.

## ⏳ BATCH 5 — Ornament re-pick (the proposal)
- [ ] Implement the 14-world SYMBOL proposal (sprig/fleuron/quatrefoil/vine/floret/daisy/6-star/damask/reversed-fleuron/asterism/rosette/diamond-cluster/4-star/8-star) for the flagship `---` dash.
- [ ] **THREE DISTINCT symbols per world** — `---`/`***`/`___` each get a DIFFERENT glyph (not a shared star=⁂/underscore=❦), all in that world's ornament face, all fitting its character, varying across worlds. (EBG worlds use their 3 fleurons ❧/❦/☙; Junicode/AwlMarks worlds pick 3 distinct from the wider palette → subset ~15-20 NS2 stars/florets into AwlMarks so the 7 AwlMarks worlds each have 3 distinct picks.)
- [ ] Update WORLDS.md ornament column (all 3 per world). Gallery for final glyph veto.

## ⏳ BATCH 6 — Lens pattern for other pickers
CONVENTION (settled): **"All" is HOME, not a facet → it's FIRST (leftmost) on every strip, and the picker LANDS there** (opens on the flat, type-to-filter list; facets are ←/→ refinements). Left→right = general→specific. Matches the theme picker.
- [ ] **Go-to file** — lenses `All · Recent · This folder · By type`.
- [ ] **Command palette** — lenses `All · File · Edit · View · Recent`.
- [ ] **History timeline** — lenses `All · Session · Today · Milestones`.
- [ ] **Browse / Recent projects** — lenses `All · Folders · Files · Git repos` (or `All · Recent · Pinned`).
Reuse the existing `Lens` machinery + strip; per picker it's lens data + facet grouping. (Fits with Batch 2's picker-consistency pass.)

## ⏳ BATCH 8 — Website (in-repo `/site`, hand-rolled static HTML/CSS)
DISJOINT from the editor-polish batches (writes `/site/`, only READS the editor via the harness) → a clean PARALLEL lane; merges trivially. Screenshots are REAL harness captures (`cargo run -- --screenshot`), not mockups (sidesteps the no-web-mockups rule). Aesthetic = awl itself: bundled fonts, amber accent, Swiss calm.
- [ ] **Landing `/`** — hero (screenshot + "awl, a calm editor" + primary CTA **[ Try it → /editor ]**); theme-worlds showcase strip (harness shots across the 14 worlds); the pitch (simple/beautiful/fun, one warm element); footer (GitHub · llms.txt · GPL-3.0 · **by Frank Lu**).
- [ ] **`/editor`** — the Trunk-built wasm editor, served at the sub-path.
- [ ] **`llms.txt`** at site root — markdown-content-at-`.txt` (the Answer.AI standard): `# awl`, `> one-liner`, `##` sections linking PHILOSOPHY/SCOPE/DESIGN/etc. Optional `llms-full.txt`.
- [ ] **DOWNLOAD: DEFERRED** (user: "skip it for now, fix the landing page later") — no download section in v1; "Try it" is the only CTA. Revisit once the `.app` bundle + release pipeline exists.
- [ ] **Screenshots regen LAST** — build structure/copy now, but regenerate the hero + world-strip captures AFTER the theme batches (5) land, so they show the final ornaments/grounds/highlight.
- [ ] Plausible beacon (Batch 7) drops into the site `<head>` (web-only telemetry).

### Website — DONE on `website` branch (`4a4e43c`), PARKED for the final pass
Landing + llms.txt + 14-world showcase built & committed to the `website` worktree branch. Preview: `open .claude/worktrees/website/site/index.html`. NOT merged yet.
**FINAL WEBSITE PASS (do LAST, before merge):** (1) regen all 15 screenshots AND rebuild the `site/editor/` wasm bundle (`trunk build`) after theme batches 5 land; (2) set the real repo URL — currently `github.com/Frank-P-Lu/awl-next` but USER MAY RENAME (possibly to `/awl`) — CONFIRM the final URL + branch before find-replacing across index.html + llms.txt; (3) merge `website` → main (trivial, `site/`-only, no src overlap).
- `/editor` mount + `site/serve.sh` built (separate pass) — "Try it" works over HTTP (`bash site/serve.sh` → localhost:8080). file:// can't serve wasm.

## 🏦 Banked ideas (not yet build items)
- **Auto-align-on-type** for markdown tables (deferred from the tables round).
- **`.app` bundle + release pipeline** — unblocks real Download artifacts on the site + the "Awl" menu title + About-panel icon.
- **Web payload slimming** — the wasm bundle is ~27 MB (was ~7.2 MB) because ALL CJK fonts (Noto JP/SC/KR + WenKai, ~16 MB) are compiled in. Levers: don't bundle CJK into the WEB build (Latin-only web build, or lazy-load CJK faces on demand — the drop-into-data-dir pattern, but for the browser), + a `wasm-opt` pass. Big first-load win for the web try-it.
- **`.app` bundle** — fixes the "Awl" menu title + the About-panel icon (packaging chore).
