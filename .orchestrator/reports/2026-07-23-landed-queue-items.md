# Landed queue items, archived 2026-07-23 (board diet)

> Moved VERBATIM from `queue.md` per its own header rule: completed work lives
> in git history and reports/, not the live queue. Landing shas are on each
> line; `git log --grep=decisions` replays the design sessions. Live items
> citing these (e.g. item 28 → 3(a), item 41 → 4(c)) resolve here.

Items 3–9 are the 2026-07-20 triage of the user's improvement notes (private, outside the repo; the screenshots live alongside them — "Notes lines N" below refer to that document). Bugs get regression tests — that is the round's exit criterion, not a nicety. Items needing the user's word first are under Held.


1. **Poster-bars overlays preserve the live page — land it.** ✅ LANDED @ eceea5a (claude, 2026-07-22; merged codex/poster-bars-preserve-page onto main atop frost-dpi, 8 files 212+/184−, no conflict). The recovered tree DID re-compile against current main (dev + release clean) — the "never re-compiled" risk did not materialize. Real change is `overlay_bars_room()`→`overlay_bars_scrim()` (both `base_100()`) so the page renders as a blur+dim backdrop instead of a flat plane. **Combined suite (frost+poster-bars together): 2498 passed / 0 failed / 11 ignored.** OUTCOME captures across Mangrove/Firetail/Cassowary (Bars) + Tawny (Pane): page STRUCTURE survives behind every overlay (tens of thousands of blurred-page px in a page-only region that the old plane rendered flat, std≈0), selected vs unselected plates distinct (fill dist 36–79) with 2400–2900 real ink px per selected row — refutes the Wagtail invisible-row mode. Orchestrator vision-smoke re-confirmed all four (selection locatable, page legible behind, no clipping). Same two `frost_rail_pixels` headed-GPU tests fail environmentally (occlusion gate, identical on bare main) — see item 2's caveat. Live feel still human-confirm.

2. **Frost DPI sibling fix — finish the test repair.** ✅ LANDED @ 2e3f681 (claude, 2026-07-22). The `frost_pill_geometry_is_dpi_invariant_in_logical_space` law now asserts pill EXTENTS (width `x1−x0`, height `y1−y0`) double at 2× rather than absolute edges — absolute origins fold in the adaptive rail's fixed physical floor, which does NOT scale, so edge-doubling was the wrong law; count/non-empty/padding controls kept, 0.75px tolerance. Test RUNS in release (1.12s, real Metal adapter) and passes. **Board's "remove the temporary eprintln!" was a misdiagnosis** — the `eprintln!("skipping …: no wgpu adapter")` skip-notice is the UNIVERSAL render-test convention (≈28 files); it was KEPT for consistency (merge, don't align). Unit suite 2498 passed / 0 failed / 11 ignored; compile clean. **Caveat:** the two `tests/frost_rail_pixels.rs` HEADED tests (`frost_dims_the_gutter_corner…`, `frost_pills_dim_real_gpu_pixels…`) fail "changed only 0 px" in a background/non-interactive shell — the wgpu macOS occlusion gate; confirmed IDENTICAL failure on clean base main (5b09b4f), pre-existing + environmental, not from this branch. They belong to "Live confirmation still useful → Gutter frost on Retina". Live Retina taste still human-confirm.


3. **Word-ops correctness round.** ✅ LANDED @ ca03d7e (claude, 2026-07-22; all three parts complete). Combined suite 2505 passed / 0 failed / 11 ignored; wasm green.
   - **(a) `⌥⌫` over-delete — FIXED (user-decided macOS semantics).** The worker's "did not reproduce" was HALF right: `abc def ghi⎸`→⌥⌫ already deleted only `ghi` (col 8), BUT the board's literal repro `abc ...⎸`→⌥⌫ landed at col 0 — it deleted the ` ...` run AND `abc`. **User tested real macOS: Option-Delete does NOT delete `abc`** — awl's old skip-nonword-then-word rule was genuinely WRONG. New rule in the ONE owner `buffer::word_delete_backward_boundary`: consume any trailing WHITESPACE, then delete exactly ONE token — a WORD run if a word char precedes the caret, else a PUNCTUATION run (never both classes). Buffer + all 5 minibuffers inherit it. LIVE-VERIFIED via `--keys "C-e M-Backspace"`: `abc ...`→col4 (`abc ` survives), `abc def...`→col7 (`def` survives), `abc def ghi`→col8 (only `ghi`), `hello`→col0. Only the `abc def...` sweep case flipped; `consecutive_word_kills_backward_accumulate` unchanged (the whitespace-fold keeps word+space kills intact). Regression case `abc ...⎸`→`abc ` added to `delete_word_backward_boundary_sweep`.
   - **(b) minibuffer word-ops — DELETE done, MOTION deferred.** Word-DELETE (⌥⌫) landed across all 5 overlay inputs, routed through the shared boundary owner (`nav::truncate_trailing_word`), tested end-to-end through `apply_core`→`overlay_intercept`. Word-MOTION (⌥←/⌥→) STOPPED as out-of-scope: the minibuffer is append/pop-from-end with NO in-query caret, and Left/Right already mean list-nav — needs an in-query caret model (a redesign). **Follow-up item, not a bug.**
   - **(c) bindable word-delete — done.** Added `delete_word_forward` + `delete_word_backward` catalog rows (empty slots; ⌥⌫/⌥Delete dispatch from keymap's static arms), keymap-defaults.toml entries, frozen-snapshot + GUIDE rows. `[keys] delete_word_forward = "M-d"` works. Naming: used `delete_word_forward/backward` (matches `forward_word` vocabulary) over the board's suggested `delete_next_word` — orchestrator ACCEPTED. Test `word_delete_commands_are_catalog_rows_and_rebindable`.
   - Combined suite (b/c + item 9): 2505 passed / 0 failed / 11 ignored; wasm green.

4. **Markdown inline render gaps.** ✅ LANDED @ a16a82a (claude, 2026-07-22, workflow item4-md-render build→verify, SHIP; combined suite 2515 passed/0 failed, wasm green, all 4 sub-parts vision-confirmed). (a) `~~strikethrough~~` never renders (the toggle command exists; the span styling doesn't) — SCOPE DECIDED 2026-07-22: `~~double~~` only (GFM, matches the toggle); single tilde stays inert. (b) Fenced code blocks should show the info-string language as a quiet label on the fence. (c) A nested list item whose content is an image (`- ![caption|w](path)`) mis-highlights, and the image CAPTION never renders at all. (d) Link affordance — DECIDED 2026-07-22: quiet underline in muted ink; link text stays full content ink (never amber; brackets/URL keep receding). Notes lines 17–31. **OUTCOME:** (a) strikethrough RENDER already shipped an earlier round (strike_ink/strike_line_band/strike pipeline) — this round added the missing real-pixel test (the user's "doesn't render" note was stale). (b) NEW muted per-language fence label via `markdown::fence_line_lang` (same `Lang::from_info` gate as CodeSyntax), rides the existing ornament pipeline. (c) FIXED a GENERAL bug — pulldown's `Tag::Item` range starts at the marker char, so `push_list_marker` dropped a nested item's leading indent from the ListMarker span (the "missing space" mis-highlight); rewrote it to walk to line-start via the shared `list_item()` scanner. Caption ALREADY rendered (that claim was wrong) — locked with a regression test. (d) NEW link underline: generalized the strike primitive into a shared `line_band`, reused the SpellUnderlinePipeline (5th instance), LinkText confirmed content-ink not amber in all 16 worlds. 9 new tests incl. real-GPU pixel-diffs.

5. **Images round.** ✅ LANDED @ 1c72e00 (claude, 2026-07-22; REWORKED after orchestrator vision-smoke rejected a stranded-marker compromise). (a) Mixed `- text ![img]`: marker+caption stay a NORMAL adjacent row, image draws directly BELOW — via forcing a REAL cosmic-text visual-row break (letter_spacing on the concealed markup's `[` byte — 2nd byte, avoids UAX14 LB13 'no break before !'), so the image gets a genuine dh-tall visual row with ZERO RowGeom/hit-test changes. Vision-confirmed marker-adjacent + image-below, no void. (b) Row geometry ALREADY owned image height (verified + locked with a test — the reported scroll-jump doesn't repro for plain images). (c) Theme-switch slowdown is the FONT reshape, NOT image reload (decode-count witness proves images cache by path+mtime, `sync_theme` never touches them; matches 'first slow, later fine') — LIVE feel human-confirm. Fixed a wasm --tests break + a font-measurement bug + the UAX14 re-strand en route. 3-way merge onto item 8 + dark-depth C auto-merged + compiled clean; combined suite 2557 passed/0 failed, wasm+--tests + web-smoke green. (a) A list item with text AND an image — STRATEGY DECIDED 2026-07-22: the image gets its OWN row below the text row (the text row keeps list metrics, the image row keeps image metrics; no hybrid row). (b) Scrolling past an image line makes the page jump as if the image had no height — row geometry must own image height so scroll feels browser-solid. (c) Probe the reported theme-switch slowdown (user suspects image reload; first switches slow, later ones fine) with `--bench-theme-burst` + a witness. Notes lines 32–35, 83–85.

6. **Spell-squiggle round.** ✅ LANDED @ c65205a (claude, 2026-07-22, workflow build-verify — RECOVERED: build agent finished without committing, work recovered from its worktree + verifier CONFIRMED that exact snapshot; orchestrator re-gated on the real commit: 2569 passed/0 failed, wasm+--tests+web-smoke green, Bilby squiggle vision-confirmed). (a) squiggle THICKER at default zoom (SPELL_THICKNESS/AMP/PERIOD doubled — zoom 1.0 == old 2.0, scale-aware). (b) per-world baseline DATA dial `RenderCaps::spell_underline_gap` (Bilby -1.0 tighter; all others default, byte-identical; no code path — grep-law clean). (c) eager+keyed: removed the ~150ms clock debounce, `recompute_spell_cache()` runs synchronously on every buffer-version change, verdicts KEYED to the exact text judged (`SpellVerdict::still_valid`) so a stale verdict never paints on changed text — completed-word flash structurally impossible (live no-flash frame is gpu-gated, human-confirm). Mutation-verified. [Workflow hardened: build agents now commit BEFORE the final suite so 'sha PENDING' can't recur.] (a) Squiggle too thin — the user says the 200%-zoom look is right for default zoom. (b) Per-world baseline gap: on Bilby it floats too far below the baseline — make the offset a data dial per world, not a code path. (c) Completed-word lag — MECHANISM DECIDED 2026-07-22 (eager + keyed): check every word as it CHANGES, including the caret word (only its DISPLAY is suppressed), AND key each verdict to the exact text it judged so a stale verdict can never paint on changed text. Leaving a word reveals an already-fresh verdict; the just-completed-word flash becomes structurally impossible. Notes lines 44–46, 67–71.

7. **Theme QA audit (systemic, audit-agent form).** ✅ LANDED @ 3109979 (claude, 2026-07-22, workflow build-verify, SHIP; suite 2538 passed/0 failed, wasm green; opus vision-smoke confirms). FIXED as DATA (grep-law re-verified NO per-world code path): universal `markdown::heading_row_lead(level)` decouples heading ROW height from font size so h3 no longer reads as body (TITLE 1.15/SECTION 1.26/SUBHEAD 1.34; extra lead sits space-BEFORE — vision-smoke: big help on no-bold Bombora/Mulga, mild-positive on bold Tawny, not too airy); per-world `bullet_scale` (Bombora 0.35 shrinks the touching manicule, Mopoke 0.8 closes the canyon); Mopoke bullet glyph U+2051→U+E670 (in-face damask rosette, was a footnote-ish two-asterisk). GUARDS (did NOT reproduce — locked with real-pixel tests): Saltpan `Theme::font`="Fraunces 9pt" IS the correct TEXT optical size (characterful, not a display cut); Potoroo table cell ink == body ink == heading ink (all (240,230,222)). **TASTE FINAL-LOOK BANKED for the next batched fable pass:** the universal heading-spacing AMOUNT + the Mopoke glyph choice. Also surfaced to user: Saltpan/Potoroo reports did not reproduce — may have been a different symptom in pasted-5. Reported cells: no-bold worlds (Mulga, Bombora) need vertical spacing around headings — h3 reads as body; bullet `-` glyph/horizontal padding wrong per world (Bombora too tight, Mopoke wrong glyph + too loose); Saltpan body text reads as a display face (regression? verify `Theme::font`); Potoroo table text ink wrong (pasted-5). Probe form per the standing policy: world × surface sampled cells, pixel arithmetic, and the audit ENDS by writing the missing law tests. Notes lines 40–48.

8. **Overlay/chrome polish.** ✅ LANDED @ eca669e (claude, 2026-07-22, workflow build-verify, SHIP; suite 2547 passed/0 failed, wasm+--tests green, gap vision-confirmed). ALL 5: (a) input gap OVERLAY_QUERY_BEAT 1.0→1.3 via the one-owner header_gap (+8px, law-tested, mutation-verified). (b) popover + spell popup now share ONE float-surface primitive (`prepare_float_panel` + `FloatElevation`; new `float_surface_law` grep-law bans stray `set_float_quads`). (c) debug `budget` LINE dropped (machine-readable sidecar field kept). (d) i-beam pinned for the whole text-drag (live-only — HUMAN CONFIRM). (e) USER-REPORTED palette selection-desync FIXED: `living_band_phase` re-anchored its glide from the STALE previous target (not the true in-flight position), so a fast/held Down outran the ~110ms slide and popped the highlight to stale rows — extracted ONE shared `retarget_band` owner used by both animators, mutation-verified regression test. This UNBLOCKS dark-depth C (item 8b unified the float-panel shadow into one owner). (a) Vertical gap after the command input box, before the results block (pasted-8). (b) The mouse-highlight popover and the spell popup should share ONE surface primitive (same behavior ⇒ same code). (c) Debug panel: drop the `budget` line — it hurts readability. (d) Show the i-beam cursor while drag-selecting with the mouse; restore the prior cursor on release (live-only). **(e) SELECTION DESYNC (user-reported 2026-07-22, screenshot):** navigating DOWN through the command palette highlights a row but 'doesn't actually select it' — user suspects the selection GLIDE ANIMATION. Folded into item 8 (overlay territory). Diagnose settled (sidecar selected_index vs highlighted band) then glide (`--screenshot-motion`); fix so the highlighted row always == what Enter runs. Notes lines 20, 36–39.

9. **Page-width drag snap oscillation.** ✅ LANDED @ 3c61526 (claude, 2026-07-22). ROOT CAUSE: the live drag matched the pointer against the ADAPTIVELY-SHIFTED rendered right edge via an `argmin` over the settable band; that rendered edge is non-monotonic in the measure (it cliffs LEFT when the column re-centers at the rail-hide boundary), so two measures (rail-granted ~105, re-centered ~119) shared one pointer-x and the argmin flipped as the pointer crept a pixel → the 105↔119 / 106↔120 oscillation. FIX (stable-reference, not hysteresis — kills the feedback at source): `begin_page_resize` snapshots the OPPOSITE column edge once into `App::page_resize_anchor` and holds it fixed for the gesture; the grabbed edge tracks the pointer 1:1 against that anchor, so the measure is a monotone affine fn of the pointer. Adaptive placement still DRAWS the rail (three regimes intact); only the MEASURE is decoupled. ONE owner `page_resize_measure_anchored` (geometry.rs); the two old inverses deleted. Law test `page_drag_measure_is_monotonic_across_the_rail_hide_boundary` WITNESSES the fixture is oscillation-prone (rendered edge decreases somewhere) THEN sweeps the pointer 1px/step through 1700..1799 asserting the measure sequence is monotone non-decreasing (+ climb check so it's non-vacuous, + symmetric left-edge). Suite 2498 passed / 0 failed; wasm green; adaptive geometry unregressed (col.left 312/width 576 @ measure 40). **Live drag FEEL is human-confirm** (headless can't drive a real pointer drag). Notes line 86.


11. **Goto unified.** ✅ LANDED @ b35a7ea (claude, 2026-07-22, workflow build-verify, SHIP; combined suite 2516 passed/0 failed, wasm green; vision-confirmed). All-lens ranks doc HEADINGS + files by ONE fuzzy score (no re-parse — reuses `markdown::headings()`), heading rows marked `❡` (mirrors `§`), heading-jump reuses the existing `Effect::JumpToLine`. By-type facet CUT: GOTO_FACET_STRIP 5→4, `goto_type_section`/`GOTO_TYPE_SECTIONS` deleted, no dead arm, no-wildcard law intact. Verifier proved non-vacuity by MUTATION (reverting the unify gate fails the tests) + byte-identical determinism captures. The DEFAULT Goto list = current doc's HEADINGS + files in one fuzzy list; the redundant "by type" facet is CUT. Jumping to a heading is a writer's main in-document movement — one list, fuzzy magic across both.

12. **Notes flip.** ✅ LANDED @ 25b10da (claude, 2026-07-22, workflow build-verify, SHIP after a verify-caught wasm-tests gate fix). A palette-only 'Notes' command (`Action`/`Effect::NotesFlip` → `App::notes_flip` over the pure `notes_flip_target(current, notes_root, previous)`) flips the active project to `notes_root` and BACK — a transient VISIT (`set_root` directly; NEVER persists the sticky `project_root` pref or pushes the recent-projects MRU), remembering the pre-flip root in `prev_project_root` (2-deep, like `prev_file`). Bare launch already opens the last project (verified, no code change; `resolve_root`: flag > sticky > cwd). Headless: `Effect::NotesFlip` Unsupported (mirrors LastBuffer); `--strict-replay` aborts naming it; capture-proven palette row (pixel-distinct from '§ Notes folder'). Fixed the new test's missing `#[cfg(not(wasm32))]` gate (verifier-caught, native-only `slug_for_action`). Suite 2591 passed/0 failed; wasm+--tests+web-smoke green. Live folder-switch feel human-confirm. Bare launch opens the LAST project (supersedes the questionnaire's stale "notes become home" checkbox). A "Notes" command flips to `notes_root`; the same chord (or Back) returns to the previous project. No open-current-dir flag.

13. **Finish Buffer gating.** ✅ LANDED @ 818bc83 (claude, 2026-07-22, workflow build-verify, SHIP). `commands::visible_hidden_mask(has_waiter)` (pure, mutation-checked) masks the FinishBuffer palette row; `OverlayState.hidden` parallel to `corpus` — `refilter` drops it WITHOUT shrinking corpus (preserves `visible_action_of` index math). Live `has_waiter` = `wait_conns` keyed to the CURRENT buffer, cfg-gated (not wasm/mas) with a `false` fallback; every non-live BuildCtx passes false → headless capture deterministically HIDES it (capture-proven: All lens no Finish, File lens 5 rows, 'Finish' query → no matches). Dispatch (C-x #) unchanged. Rebind menu still lists it (bindable ahead of use — scope call). App-level wiring live-only → **human-confirm the live `EDITOR=awl --wait` round-trip**. Suite 2585 passed/0 failed; wasm+--tests+mas+web-smoke green. Hidden from the palette unless a terminal is actually WAITING (a live `EDITOR=awl --wait` session); appears — and works — only then. (The user still owes themselves a live `EDITOR=awl` round-trip try; human confirm, not a build blocker.)

14. **Insert date.** ✅ LANDED @ 0c29b99 (claude, 2026-07-22, workflow build-verify, SHIP; suite 2534 passed/0 failed, wasm green, vision-confirmed). Pure `src/dateformat.rs` (5 formats, wasm-safe civil-date math), `Effect::InsertDate` deferral (no ActionCtx threading), `Buffer::insert_text` sealed both sides, cycling 'Date format' settings row showing the live example. DETERMINISM: capture folds today→fixed `CAPTURE_PLACEHOLDER_YMD=(2009,3,7)` (single-digit day+month, distinct → exposes DD/MM vs MM/DD + zero-padding); two identical --screenshots byte-identical. No date crate. Verifier note: worker said 15 unit tests, actually 12 (count overstatement, behavior all tested). Sidecar `date_format` + CAPTURE.md/schema row (/178). Palette command inserts today's date in the configured format. A Settings row cycles RENDERED examples of TODAY'S date — pick by sight, what you see is exactly what inserts: `22/07/26` (default) → `07/22/26` → `2026-07-22` → `2026/07/22` → `22 July 2026`. NO free-form pattern strings; NO locale sniffing (decided against — zero locale machinery exists and the settings row makes it one click; detection would be item-18-grade overengineering). NOTE: no absolute-date formatting exists anywhere (no chrono; only `index.rs::relative_time`) — hand-roll a five-arm formatter, do not add a crate. Date-picker variant stays banked v2.

15. **Bullet-level readability.** ✅ LANDED @ b2adc16 (claude, 2026-07-22, workflow build-verify, SHIP + opus vision-smoke). Per-level glyph ROTATION: `Theme::bullets` widened pair→triple, `bullet_for_depth` = depth%3 (cycles every 3 levels); `.0`/`.1` BYTE-IDENTICAL on all 19 worlds so item 7's Bombora manicule + Mopoke rosette are preserved (manicule law-tested level-1-only); BULLETS_PLAIN=(•,◦,▪); the 7 serif/ornament worlds each get their third hedera. Per-level INDENT: `Theme::list_indent_scale` (PLAIN=1.0 for 12 worlds byte-identical, WIDE=1.5 for 7 antique/serif) via `add_list_indent_span` (metrics override on the leading-space run, row height pinned). Both axes pure DATA (grep-law clean, mutation-verified). EXACT glyph vocabulary deferred to the pre-tag taste pass (item 20 / fable). Suite 2583 passed/0 failed; wasm+--tests green. **COMPLETES the triage render cluster (items 4,5,6,7,8,15,16).** Per-level bullet glyphs (• ◦ ▪-style rotation) + slightly wider per-level indent, tuned PER WORLD as data (caps-style; no world code paths). Glyph vocabulary gets judged in the pre-tag taste pass (item 20) — build with sensible defaults now.

16. **Selection reveals raw.** ✅ LANDED @ 44d435a (claude, 2026-07-22, workflow build-verify — SHIP after a verify-caught fix). A line reveals raw markdown when the caret OR an active SELECTION touches it: the ONE `wysiwyg_reveals` owner now takes the selection range (extracted `selection_touches` predicate), widening every line/block-scoped kind + the legacy rule/bullet conceal; a selected TABLE shows raw `|` pipes (x-ray generalized Option→Vec of touched rows) for correct select/copy. Perf-guarded (x-ray walks table BLOCKS, O(table rows) not O(doc)); `last_conceal_selection` invalidates row_geom on a selection-only change (tripwire, regression-tested). **Adversarial verify caught a CROSS-ITEM defect** (item 5's inline-image gate was caret-only → a selected image DOUBLE-RENDERED); FIXED by routing 3 sites through `selection_touches` (compute_image_layout, images_report, AND refresh_rule_conceal's mixed-line rescan — the 3rd found during the fix), so a selected image parks+dims like a caret-touched one. 4 mutation-checked tests. Combined suite 2580 passed/0 failed; wasm+--tests+web-smoke green; vision-confirmed. Extend the WYSIWYG reveal rule: a line shows raw markdown when the caret OR an ACTIVE SELECTION touches it. Tables get correct select/copy for free; the rule applies to selection everywhere, not tables-only.

17. **CLAUDE.md diet.** ✅ LANDED @ 1525f9d (claude, 2026-07-22, the user's live design session; docs-only, no code touched, suite not run). 258 → 94-line core; per-feature mechanism detail moved VERBATIM to seven docs/ files (config, markdown, fonts, syntax, render, platform incl. line-endings, licensing), each opening with a when-to-read trigger (the PI minimal-prompt spirit, pasted-6). Core keeps identity, build/verify, harness limits, audit policy, tripwires-as-facts, principles, conventions, git protocol, open decisions — register assumes a strong reader; each fact stated once, no repeated prohibitions (user rule this session). AGENTS.md symlink intact; the one test comment referencing CLAUDE.md (occlusion tripwire) still resolves.

19. **Neighborhood-audit round.** ✅ LANDED @ 6c6f194 (claude, 2026-07-23, workflow build-verify — SHIP after dropping one VACUOUS test the verifier caught). Trigger-3 sweep over the item-3/4/6/8 triage classes (item 7 = theme slice, done). TWO non-vacuous LAW TESTS landed (mutation-verified): `glide_anchor_law` (new grep-law banning any `overlay_band_drawn`/`living_band_phase` production call site outside the one shared Pane-family seam — search/spell-popup/whichkey/menubar carry NO selection glide, so they're structurally immune to the item-8(e) stale-anchor bug, not merely untested) + `spell_squiggle_paints_real_pixels_across_a_world_sample` (4-world sample proving the squiggle reaches a real framebuffer). The proposed task-list pixel test was DROPPED as vacuous — the verifier reverted the whole checked-task dim and got byte-identical frames (max_delta=0; it passed only on the `[ ]`/`[x]` glyph on a mono world). Suite green post-removal; wasm+--tests+web-smoke green. FOUND 2 defects → filed as items 28 + 29.

23. **Docs-vs-catalog law test (user-decided 2026-07-22).** ✅ LANDED @ 772aaff (claude, 2026-07-22, workflow build-verify, SHIP). Extends the shipped `{{key:slug}}` chord-token system with a sibling `{{cmd:slug}}` command-NAME token (keytoken.rs; unknown slug → loud `[[unknown-cmd:slug]]`, never vanishes); command citations in GUIDE.md + welcome.md now wrapped in tokens. New cfg(test) `src/docs_catalog_law.rs` harvests `site/guide.html`'s key table (structured parse, joined by `commands::slug`) + its prose glyph-chords, resolved against the canonical `generate_keys_reference_markdown` rows; `sibling_site_pages_carry_no_chord_glyphs` grep-law pins credits/index/check.html. A retired chord or renamed command in ANY user-facing doc now fails `cargo test` — mutation-proved both ways. No drift in current docs (pass clean). Suite 2603 passed/0 failed; wasm green. (Builds on the prior CONVENTION-TRUTHFUL SURFACES round that handled chords; this closes command-NAMES + the site-HTML mirror.) The welcome-doc incident (deployed welcome taught the retired `C-x` chords — caught by hand, board 2026-07-10 era) becomes structurally impossible: a law test harvests every chord notation and command name cited in the user-facing prose surfaces (GUIDE.md, the welcome/tour doc, site/guide.html and siblings) and asserts each resolves against the live catalog + keymap (both conventions) — a retired chord or renamed command in any doc fails `cargo test`. Same spirit as the existing frozen-snapshot/no-wildcard laws, applied to prose. Ships with whatever harvesting convention is least fragile (explicit markers in the docs are acceptable if bare regex proves flaky).

22. **Scrub compile-time paths from the shipped wasm.** ✅ LANDED @ 075adf2 (claude, 2026-07-22, subagent). `scripts/with-remap.sh` env-seam exports `RUSTFLAGS=--remap-path-prefix=$HOME=~ --remap-path-prefix=$CARGO_HOME=/cargo` at BUILD TIME (no path literal in any tracked file), prepended to every shipped/public build (deploy-web, ci web bundle, release web+mac+linux, package-macos MAS, site/README local rebuild). Native release 818→1 / wasm 456→2 home-path hits; residuals are `env!(CARGO_MANIFEST_DIR)` in native-only bench harnesses (un-remappable by construction, non-personal on CI runners). Clean artifact recommits at next deploy. (cargo `trim-paths` was the cleaner first choice but is unstabilized on cargo 1.94 — env-seam used instead.) The tracked `site/editor/awl-*_bg.wasm` embeds `/Users/<name>/…` source paths (Rust panic/debug locations baked in at compile time — found by the 2026-07-22 repo scrub, which fixed every TEXT hit). Fix at the build seam so it never recurs: add `--remap-path-prefix` (RUSTFLAGS or `.cargo/config.toml`) mapping `$HOME` out of the wasm + release builds, rebuild, and the next deploy recommits a clean artifact.

21. **CI mac live-probe → GATING (graduation decided).** ✅ LANDED @ 075adf2 (claude, 2026-07-22, subagent). Removed the JOB- and STEP-level `continue-on-error`; `scripts/ci-live-probe.sh` now captures the soak exit code and `exit "$fail"` (was terminal `exit 0`); floor raised `presents>0`→`presents>=100`; added a `faults==3` assertion (the soak binary already bail!s on presents=0/faults<3/unrecovered — the script now PROPAGATES it). Failure→red traced end-to-end + canned-input verified (presents=40→exit1, faults=2→exit1, PASS→exit0). **`live-probe.sh` left untouched** — the orchestrator brief pointed at the wrong file; its `:278 exit 0` is a REQUIRED PASS exit and `:59` is the macOS skip; the real swallow was `ci-live-probe.sh`'s terminal exit 0. Scope unchanged (push-to-main + dispatch, not PRs). **FIRST GATED RUN is the push that lands this — monitored by the orchestrator.** Remove all three failure-swallowing layers (job-level `continue-on-error`, step-level, the script's always-exit-0); a soak-contract failure turns CI red. Scope unchanged: pushes to main + workflow_dispatch, NOT PRs. Strengthen `presents > 0` to a floor of `presents ≥ 100` per 25s run so slideshow-grade degradation also fails. Accepted trade, eyes open (user's word): a GitHub runner-image change can red main through no fault of ours — guaranteed discovery was chosen over issue-pings. (`faults=3` in the report line is the HEALTHY target — three injected faults, all must recover; `faults<3` is the failure.)


26. **CI GREEN — main's CI has been red since 1c05c4b (2026-07-20), pre-dating this session.** ✅ LANDED @ ab60747 (claude, 2026-07-22, subagent) — **CI CONFIRMED GREEN** @ run 29895667403 (067fd48): linux + mac + web + the now-GATING mac live-probe ALL green — main's first green CI since 2026-07-19. The scratch-stash parallelism-race fix (unreproducible locally) verified on CI-linux AND CI-mac. Fixes: `53e5dae` gate item-4's `nested_list_image_reports_alt_caption` test native-only (`ImageReport::alt`/`images_report()` are `#[cfg(not(wasm32))]` by design — image headers read from the native fs); `318248e` make `sync_view_retitles_only_on_an_actual_dirty_flip` hermetic (it built a full `App(None)` startup with NO FsGuard/serial, so its scratch-restore raced into the corrupt-stash test's in-memory fs under CI parallelism → a 2nd corrupt sibling; the ONLY unisolated `App(None)` in the tree). Failure 3 (1c05c4b web red = `pdf_glyph_probe`) already fixed by b518587 this session, subsumed. Local: `cargo test --bin awl` 2538/0; `web-smoke.sh` 16 passed; `cargo build --target wasm32 --tests` compiles; native clean. Discovered 2026-07-22 while monitoring the newly-activated live-probe gate. Three jobs fail: (a) linux+mac `build+test` on `app::tests::files::scratch_stash_invalid_utf8_preserves_a_corrupt_sibling_then_starts_a_blank_scratch` — fails on CI (both OSes) but PASSES on a local mac, so environment-sensitive (HOME/tmp/locale/perms). (b) `web (wasm build+smoke)` compile error `no field 'alt' on ImageReport` — item 4's `render/tests/images.rs:524` (`nested_list_image_reports_alt_caption`) accesses `im.alt`, which is `#[cfg(not(wasm32))]`-gated; SESSION-INTRODUCED by item 4, masked by the already-red web job + a gate blind spot (`cargo build --target wasm` does NOT compile tests). Reproduced locally via `cargo build --target wasm32-unknown-unknown --tests`. (c) whatever web-smoke failed on at 1c05c4b PRE-item-4 (bracket it). Last green CI = b9ce07c (2026-07-19). GATE FIX (standing): add `cargo build --target wasm32-unknown-unknown --tests` to every train's wasm gate (build-only missed a test-only compile break).


- **Decision questionnaire ANSWERED — 2026-07-22 interview.** Every item has the user's word (record kept in the user's private notes); the winners are queued as items 11–21 plus the decision lines folded into items 4/5/6. The hold is lifted.

- **Tawny ↔ Mopoke differentiation — DECIDED: Option A (user's word + fable, 2026-07-22).** ✅ LANDED @ 22b5ca8 (claude, 2026-07-22, workflow build-verify, SHIP + opus vision-smoke). Mopoke-only: caret #FFC05E→#F56E3D (warm ember, fable-refined redder than the gallery's #F78645), selection #3A6FD8→#7B39C6 (violet-plum, same alpha). Caret BAR pixel-proven copper (244px, 0 old-gold — gallery 'pale bar' didn't reproduce). Amber guard holds (role hue anchors fixed). New law `tawny_and_mopoke_carets_and_selections_are_now_numerically_distinct` (RMS ~76, was 24.6). Suite 2548 passed/0 failed; wasm+--tests green. FIRST fable-decided taste change landed. Gallery quantified the root cause: 4 of 10 tokens byte-IDENTICAL between the pair (caret #FFC05E, selection #3A6FD8, primary_content, error) — RMS 24.6. Option A moves MOPOKE only: caret golden→copper/ember (gallery #F78645; FABLE REFINEMENT push a shade redder toward true ember), selection blue→violet-plum (#7B39C6 @ same alpha). RMS→~66. Fable: the plum selection is the felt workhorse (harmonizes with Mopoke's warm ground), keeps its character; C's extra ink/role-tint invisible at reading distance for a bigger diff. Refinement 2: verify the in-TEXT caret bar carries copper (gallery bar read pale). Re-verify all theme laws (amber guard esp. — copper stays warm; roles guarded), update any tightest-pair/RMS assertion. codex/tawny-mopoke-gallery branch is the throwaway exploration (never merged). Current tightest pair (RMS 24.6; identical caret/error/selection colors). Produce an evidence gallery and recommendation; no palette law or world change before the user picks.

- **Dark-world depth language — DECIDED: Option C (user's word + fable, 2026-07-22).** ✅ LANDED @ bbd3374 (claude, 2026-07-22, workflow build-verify, SHIP + opus vision-smoke — Currawong card floats via value-step + rim, NO pale slab). Deleted the drop-shadow quad in the one owner `set_float_quads` (ALL worlds, not dark-only); collapsed `FloatElevation::Shadowed`→`Rimmed` at every call site (shadow pipeline fields kept inert pending a future removal). Rim already on the muted ladder (`surface_selected`, well short of ink; Wagtail 1-bit stays the pre-existing named exception). NO-SLAB law `dark_world_card_casts_no_brightening_slab_below_it` (Currawong/Mopoke) + `light_world_card_still_reads_elevated…` (Saltpan/Bilby), MUTATION-verified (reinstating the shadow reproduces zoneA 0.116 vs zoneB 0.021). Suite 2550 passed/0 failed; wasm+--tests green. SECOND fable-decided taste change landed. Root cause (gallery): `float_shadow_srgba()` (render.rs) draws the card shadow in `base_content` INK — near-white on dark worlds → the shadow BRIGHTENS the ground into a pale slab (+0.12..0.25 luminance, measured on Currawong's card). Option C = DELETE the shadow, let a hairline value-step rim carry depth (exactly DESIGN §5). Fable: A (dark shadow on near-black) self-defeating; B (page scrim) heaviest/most-modal; C reads as 'a plate in the same world', calmest. BUILD: delete the shadow path OUTRIGHT (one rule, all worlds — no dark-only gate), keep the hairline on the MUTED value ladder (never full ink, else it recreates the brightness bug on darkest worlds); update the 2 tests asserting `float_shadow.instance_count()==1` (chrome_overlay::caret_preview_panel…, chrome_panels::spell_panel_floats…). Two call sites (popover.rs, chrome/mod.rs diff panel) already independently switched to Rimmed — this generalizes their fix to the shared primitive. codex/dark-depth-gallery is the throwaway exploration (never merged).

- **✅ 2026-07-22 live pass — user CONFIRMED (recompiled to current main first):** palette selection-glide fix (item 8e); page-width monotonic drag (item 9); i-beam-during-drag mouse POINTER (item 8d — confirmed it IS the i-beam; user has a nuance for their own suggestions list); Tawny↔Mopoke copper/plum (item TM-A — reads right; the plum SELECTION is the tell, caret shift is subtle); dark-world card depth (item C — "great", no slab); spell no-completed-word-flash (item 6); notes-flip round-trip (item 12 — works; settings-row clarity noted); Cassowary filled caret; copy pulse (motion is UNIVERSAL by design — user OK, flagged a possible per-world differentiation as a future tweak); living-band choreography; Kana→kanji IME (the hard-to-headless-verify one — works). Item 5 scroll + item 14 date-format were NOT re-flagged after the user's recompile → presumed resolved by the fresh build.
