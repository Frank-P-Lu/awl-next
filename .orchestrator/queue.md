# awl — live build queue

> This file contains only live execution state. For the multi-tool claiming protocol and board-write rules, see `README.md` beside it. Completed and superseded work lives in git history and `.orchestrator/reports/`; do not duplicate an archive here.

## Ready queue

10. **Minibuffer word-MOTION (⌥←/⌥→) — follow-up split off item 3(b), 2026-07-22.** Word-DELETE already landed; MOTION was correctly deferred: every overlay input (palette query + Rename/Link/Keep/Settings-value) is an append-to-end / pop-from-end field with NO in-query caret, and Left/Right are already bound to list-nav (lens-cycle / descend / ascend / selection move). Bringing ⌥←/⌥→ needs a real in-query caret MODEL across all 5 inputs + its render + resolving the Left/Right conflict — a design round, not a focused fix. Not started; needs a design pass before build.

Items 18 and 20 are the surviving 2026-07-22 interview outcomes (the rest of 11–21 landed — git history has them, e.g. `git log -p .orchestrator/queue.md`). Every decision is the user's word; each item embeds it.

18. **Overengineering audit round (bounded).** Produce a hit-list of over-defensive/speculative code (the malformed-timestamp-validation class), the USER APPROVES THE LIST, then cut each entry with tests. Editing correctness (graphemes, wrap, undo, CRLF, motion at boundaries) is sacred — cut machinery, never editing correctness.
    ✅ LANDED @ b386d14 (2026-07-23) — all 10 user-approved cuts in (safe forms; final gate 2695 pass, wasm green; sacred finalize_buffer_lines guard untouched). Cuts: (1) render.rs VisualRow.byte_start/end dead fields; (2) files.rs jump_to_line usize→String→usize round-trip; (3) overlay_nav.rs settings None-arms (law-backed → expect); (4) render.rs CornerAnchor::TopLeft (fold law into TopRight); (5) buffers.rs contains → cfg(test); (6) dateformat.rs unwrap_or(0) over exhaustive enum; (7) image_cache.rs double-get → Entry API; (8) export html.rs+docx.rs clamp(1,6); (9) stars.rs const self-clamp; (10) streaks.rs epoch/round-trip via TUPLE-THREADING (never .expect — would add a render-path panic). EXCLUDED (sacred): text.rs finalize_buffer_lines empty-buffer guard (edit-path).
20. **Pre-tag taste-pass bundle.** Fold the taste pass into the standing pre-tag journey sweep: ONE screenshot export across worlds + ONE fable judging session per tag (hard cap — fable usage is the scarce resource). Judges: per-world bullet glyphs (item 15), squiggle size/baseline incl. Bilby (item 6), dash padding + Saltpan font outcomes (item 7). Fires at the next pre-tag sweep, not before.
    ⏸ TIMING SETTLED — intentionally dormant until the user explicitly begins tag/release preparation. An ordinary push does NOT trigger it; no further product decision is needed before that pre-tag round.
24. **User-facing docs refresh (user-decided 2026-07-22, release-adjacent).** GUIDE.md, the welcome/tour doc, and the site guide get a full pass against the current product: the identity-round vocabulary, the four-door chords, and the interview features as they land (goto unified, notes flip, insert date, selection-reveal…). Docs voice rules apply (matter-of-fact, tables + short declarative sentences, facts verified). Fires after items 11–16 land so it documents a settled target, before the user's release; item 23's law then keeps it honest permanently.
    ⏸ TIMING SETTLED — no design blocker. Run once the current user-visible feature queue (including this wave-3 session's outcomes) has landed, before release preparation, so the refresh documents one settled target instead of chasing each merge. Site COPY may update; site DEPLOYMENT remains separately user-gated.
25. **Off-cursor image conceal vs spell/nit underline artifact (follow-up from item 4, 2026-07-22).** When an image line is OFF the caret's line/block, its WYSIWYG-concealed raw source (`![alt](path)`) still leaves a small, mispositioned spell/nit underline artifact inside the placeholder card (a faint muted dash + a warm-red tick). Reproduces for ANY off-cursor image (nested or not) — a spell/nit-vs-conceal interaction, NOT list-nesting or caption specific. Found + vision-confirmed during item 4 (visible near the left edge of the card in md-nested-list-image.png). Fix: exclude the zero-width-concealed image-source range from the spell/nit scan (or suppress the underline when the source advance is collapsed). Regression test at the spell/render seam.
    ✅ LANDED @ 7546e61 (parallel wave, 2026-07-23) — image-source range excluded from the spell/nit scan; off-cursor image card shows zero stray underline (pixel-verified).

27. **`image_hit_rects()` reads a STALE `revealed` flag (pre-existing, follow-up from item 16, 2026-07-22).** `image_hit_rects()` reads `image_report`'s STORED `revealed` field directly rather than through `images_report()`'s always-fresh override, so a resize handle can arm on a stale reveal state. Confirmed ORTHOGONAL to selection (reproduces with a pure CARET move onto an off-cursor mixed image line — zero selection). Pre-existing, flagged (not fixed) during item 16's image-double-render fix. Fix: route `image_hit_rects` through the fresh `images_report()` override; regression-test the caret-move-onto-image-line stale-handle case.
    ✅ LANDED @ ddb78ee (parallel wave, 2026-07-23) — image_hit_rects routed through the fresh images_report() override; stale-handle regression non-vacuous. LIVE-CONFIRM: right-click/drag near a revealed mixed image no longer grabs a phantom resize handle.

28. **`delete_word_forward` has the pre-item-3(a) over-delete bug (found by item 19's audit, 2026-07-23).** The ADVERTISED native mirror of ⌥⌫ — ⌥+forward-Delete (keymap.rs), `Buffer::delete_word_forward` (buffer/edit.rs) — STILL uses the OLD `skip-nonword-then-word` rule that item 3(a) proved wrong for the BACKWARD direction. Repro: `⎸... abc` (caret before a punctuation run + a word) → the forward chord deletes `... abc` (BOTH the punct run AND the word) in one stroke — the exact over-delete the user rejected for backward. Existing `delete_word_forward` tests only exercise WHITESPACE runs, never punctuation, so the asymmetry was missed. FIX = the forward mirror of item 3(a): a `word_delete_forward_boundary` deleting exactly ONE token class (consume trailing whitespace, then one word-run OR one punct-run, never both). **DECIDED 2026-07-23 (user's word — the symmetric rule; supersedes the live-macOS-check blocker):** one token per stroke, forward too — `⎸... abc` ⌥+forward-Delete removes the `...` run ALONE (leaving ` abc`), never both classes. (The user restated the rule via the backward twin — `... abc⎸` ⌥⌫ deletes only `abc`, the already-landed 3(a) behavior — and blessed the mirror; a live macOS sanity check is welcome but the user's word governs, as in 3(a).) Dispatch-ready. (Minibuffer forward-delete is a safe no-op — no `DeleteWordForward` arm in `overlay_intercept` — so no parallel bug there.)
    ✅ LANDED @ 3b94c34 (2026-07-23, bg cook cluster 1) — one-token-per-stroke forward delete; `⎸... abc` → ` abc`; non-vacuous punct tests; 2609 pass.

29. **Checked-task DIM may not render + needs a non-vacuous outcome test (found by item 19's verify, 2026-07-23).** The verifier reverting the ENTIRE checked-task dim (`MdKind::Task(true)`/`TaskDone` → full ink in `md_attrs`) produced BYTE-IDENTICAL frames (max_delta=0) on the tested world — so EITHER the checked-task text/box dim is a visual NO-OP (a real defect: checked todos don't actually recede) OR the fixture didn't exercise it (mono world where `[ ]`/`[x]` advances match + body byte-identical). INVESTIGATE whether the dim reaches the framebuffer (checked vs open task on a PROPORTIONAL-body world, caret off-line, pixel-diff the BODY region); if it's a no-op, fix the dim. Either way write a NON-vacuous outcome test that FAILS when the dim is reverted — the task-list neighborhood's missing appearance oracle (item 19 dropped its vacuous first attempt). USER APPROVED dispatch 2026-07-23.
    ✅ LANDED @ 4176426 (parallel wave, 2026-07-23) — non-vacuous appearance oracle for the checked-task dim (fails when the dim is reverted).

Items 30–44 are the 2026-07-23 designer-session outcomes (fable designer + user, over the user's 22/07 notes; 43/44 graduated from Held after interview round 2). Each embeds its decision; none needs a further user check before build.

30. **Mopoke body face + bullet triple (user + fable, 2026-07-23).** User verdict: Quattro's duospace reads as a mono body — swap Mopoke's `font` iA Writer Quattro S → **Bitter** (warm screen-bred slab, cosy in dim light; already bundled via Magpie — face-sharing is precedented (Plex Sans ×2, Iosevka ×3) and `at_least_six_distinct_faces` is unaffected; keep `heading_bold`, Bitter-Bold is bundled; `mono` stays IBM Plex Mono). The ROSTER-WIDE mono-body question is CLOSED — user's word: the rest of the roster keeps its mono faces. Bullets: `('\u{E670}', '\u{EF92}', '\u{E67D}')` — fable-picked 2026-07-23 from candidate triples rendered at bullet scale on Mopoke's own ground/ink: E670 keeps the rosette (the dash-ornament vocabulary link item 7 established), EF92 is the rosette's OPEN four-fold sibling (same damask register, visibly lighter), E67D a tiny foliate sprig — weight descends with depth in ONE register (the complaint was exactly the two-register clash of dense E670 → airy ❦). Build round re-verifies in-app: `bullet_scale` fit (the 0.8 literal may need retuning for Bitter's advance), `bullet_glyph_never_touches_the_following_text_in_any_world`, heading metrics under Bitter, vision-smoke.
    ✅ LANDED @ 4bc7e79 (2026-07-23, bg cook cluster 1) — Bitter body + E670/EF92/E67D bullets; bullet_scale retuned to shared ornament tier; vision-smoke pass; 2610 pass.

31. **Currawong star lifecycle (user-decided).** "I don't notice the blinking at all" — root cause is authored: `floor: 0.12` (a star never vanishes) + the quiet-band ceiling (peak ≤ the muted rung's deviation) tuned the twinkle below perception. Keep the hashed-cell layout + the one ambient clock (byte-determinism laws intact — NO true randomness; "random" is the integer hash). Change the ENVELOPE: per-star lifecycle — dark dwell at true zero → rise → shine → fade — so the visible population changes (stars appear and die, the user's ask); per-star tint from a tiny LOW-SAT palette (blue-white / white / champagne — real-star colors; amber guard holds by low sat). Stars get their OWN visibility band law (a real floor AND a calm ceiling ABOVE the muted whisper cap) — a deliberate relaxation of the ambient-stars law, user's word. Retina density check rides along (the open live-confirm). Gallery candidates via `AWL_STARS_PHASE`; twinkle feel over real seconds stays human-confirm.
    ✅ LANDED @ 4d2ce08 (parallel wave, 2026-07-23) — per-star dwell→rise→shine→fade lifecycle + low-sat tint palette + own visibility band; vision confirms varied population + tints across phases. LIVE-CONFIRM: twinkle feel over real seconds + Retina density.

32. **Organic text-hugging frost for BOTH lava worlds — Mangrove + Firetail, outline + gutter (user-decided 2026-07-23).** The structural half already made frost a `RenderCaps` capability. Finish the taste half by replacing each rectangular frost pill with ONE glyph-seeded organic FIELD: every visible glyph seeds a small close halo; neighboring seeds merge in ANY direction wherever their softened fields overlap, so nearby words/rows may naturally become a larger organic island — there is NO artificial per-row separation and no explicit component/zoom breakpoint. The shape is derived from the ACTUAL zoomed glyph geometry, so islands may join or separate naturally as zoom changes; the transition comes from the continuous field, never a `zoom >= X` mode switch. Mangrove and Firetail use the SAME shape/blur/dim/feather recipe (palette-derived color still belongs to each world); the outline and gutter share it; all non-lava worlds remain byte-identical and the 1-bit exclusion stays structural. The result is visible-but-gentle, close to the ink, never a rectangular bounding box, and always quieter than the text. PERFORMANCE IS PART OF DONE: record a same-machine BEFORE on the current rectangular frost and AFTER in `--release`, adapting `--bench-frame` to a witnessed page-mode lava fixture with a populated outline + gutter; report absolute median frame/stage ms and percentage delta for Mangrove + Firetail. The witness asserts a nonzero seed/island field, ZERO mask rebuilds across warm steady frames, and exactly one rebuild after margin text/layout OR zoom changes. If steady median frame cost regresses by >5%, take the named degradation arm — word-run seeds instead of per-glyph seeds, still merging organically in any direction — then remeasure. Contrast-floor/palette laws + a real-lava vision sweep at 50%/100%/200%/300% zoom (no box, clipping, discontinuous mode snap, or illegible ink) and live feel pass remain mandatory.
    🟡 STRUCTURAL HALF LANDED @ 044d9fd (wave 2, 2026-07-23); **TASTE/PERF HALF NOW DECIDED + DISPATCH-READY** — no user choice remains before build. This decision supersedes the old Mangrove-only/gentle-vs-visible taste blocker and subsumes the open gutter-frost confirmation.
    🟡 IN PROGRESS — claude (bg cook, wave 5), 2026-07-23, isolated worktree (organic frost).
    ✅ LANDED @ 116ae40 (wave 5, 2026-07-23) — organic glyph-seeded frost FIELD for both lava worlds (Mangrove + Firetail), outline + gutter: lava shader sums per-seed halos + thresholds (lava::frost_coverage), seeds merge in any direction into islands, NO box at any zoom (vision 0.5/1/2/3×, both worlds). PERF: shipped the WORD-RUN degradation arm (per-glyph = +31%, kept behind lava::FROST_SEED_PER_GLYPH=false); new --bench-frost witness (nonzero seeds, 0 steady rebuilds, +1 on zoom/margin-text). LIVE-CONFIRM: real-lava feel + zoom sweep by eye.

33. **Caret morphs to a plain bar during mouse drag (item 8d nuance, user-confirmed 2026-07-23).** While drag-selecting, the CARET renders as a thin bar; on release it returns to the configured style (Morph, for the user). The mouse POINTER (already i-beam, confirmed) is unchanged. Live feel human-confirm; assert the bar state through the caret-mode capture where reachable.
    ✅ LANDED @ 9e21bc9 (wave 2, 2026-07-23) — caret melts to the insertion bar during a live selection drag (ViewState.selecting_drag), restores the configured look on release; asserted at the caret-mode seam. LIVE-CONFIRM: drag feel + the momentary bar during a click-hold.

34. **Summoned overlays own the margins (user-decided).** While ANY overlay is open, the outline + gutter are suppressed; they return on dismissal. Fixes the theme/caret-picker collision (those stay CRISP — no blur added, live world preview is their point) and declutters the palette. Consistent with the lava rail-carve precedent (chrome yields the margin). Capture test: overlay open ⇒ zero outline/gutter ink.
    ✅ LANDED @ a6ccaae (wave 2, 2026-07-23) — any open overlay suppresses the outline + gutter (zero ink), returning on dismissal; theme/caret pickers stay crisp (no blur). Vision pixel-confirmed both halves.

35. **Overlay text must sit on an overlay surface (Mangrove Bars defect, pasted-11).** The Bars personality plates each row's LABEL but the right-aligned shortcut hints (⌘O, ⌘⇧P…) and section headers ("Recent") render BARE over the blurred page — the user's "floating commands." Rule: every glyph an overlay draws sits on an overlay surface — the bar spans the full row including the shortcut column (or hints move inside the bar), and headers get plated. Also repro the menu-bar/palette coexistence visible in the same screenshot. Pixel regression: no overlay text over unplated backdrop, all Bars worlds.
    ✅ PRIMARY FIX LANDED @ 5592022 (wave 2, 2026-07-23) — bare right-column shortcut chords now plate across all Bars worlds (pixel-tested). REMAINDER split to item 46: wave-2 vision found the facet/lens tab strip ("View"/"Recent" bare on Mangrove) + section headers still float.

36. **Date-format PICKER (user-decided).** Promote the cycling settings row to the caret-picker pattern: a picker overlay listing all five formats, each rendered with TODAY'S date — pick by sight, what you see is what inserts; the settings row becomes the entry point that opens it. Capture keeps the fixed placeholder date (item 14's determinism). Resolves the live-pass "settings-row clarity" nuance.
    ✅ LANDED @ 11ce9c5 (wave 2, 2026-07-23) — date-format promoted to a picker overlay listing all five formats each with today's date; the settings row opens it. Determinism preserved.

37. **Theme-switch latency round (user-decided).** (a) SETTLE-LATENCY readout in debug mode: input event → settled present, "theme switch settled: N ms", plus a once-per-switch phase breakdown (font load / reshape / row-geom / atlas upload / first present) so the dominant cost names itself instead of being guessed. (b) DEBOUNCE experiment: the user localized the freeze — three rapid picker Downs, freeze lands on the THIRD — consistent with the preview debounce coalescing the first two and the felt freeze being the ONE reshape that fires on settle. Run with the debounce at 0 to expose true per-step cost (expected: a smaller freeze on EVERY step — a diagnostic, not the fix); with the readout's numbers, decide tune/keep/kill. Watch the crossing-bracket suspects too (present-transaction arming on ambient/1-bit picker crossings is compositor-side latency the app's timers never see). External ground truth: AGENT-DRIVEN end to end, no human recording (user-decided 2026-07-23). A live-probe-family script (the `ci-live-probe.sh` lineage — "extend the harness toward reality"): launch the LIVE app foregrounded (occlusion tripwire), start `screencapture -v -V N` (records the compositor's own output for N seconds, auto-stops; 60fps ≈ 17ms resolution suffices for a 100ms+ freeze), drive Down presses into the picker via osascript/System Events, then the agent explodes frames (ffmpeg) and brackets the interval by pixel arithmetic — start marker = the Down-press's own row-highlight move (the first visible response), end = the last frame where the document region still changes; frames/fps = felt latency, cross-checked against the readout. ONE-TIME setup the user grants once: Screen Recording + Accessibility permission for the driving shell — **GRANTED (user confirmed 2026-07-23): permissions are already in place on this machine; the whole item is dispatch-ready end to end.** **DECIDED 2026-07-23 (user's word): the tune/keep/kill call on the debounce is the USER'S — the round lands the readout, runs the experiment, reports the phase breakdown + felt-latency numbers WITH a recommendation, and parks tune/keep/kill as a queue note the user resolves by editing the board (no agent or fable delegation).** Designer note: the screencapture probe is a LOCAL script (the `ci-live-probe.sh` lineage names its ancestry, not its home) — it is never wired into the gating CI (the free mac VM has no granted Screen Recording and item 53 shows that tier is already timing-marginal). Two deliberate NOs: this canNOT be headless (the headless path has no clock and no compositor BY DESIGN — felt latency is a live-window phenomenon; hands-free ≠ headless), and NO in-app recording feature (the app testifying about itself is what the readout already is — the external witness must be the OS's pixels, and a video recorder inside a calm text editor is exactly the infrastructure machinery the scope forbids).

38. **Squiggle starts at the crest (user's note).** Phase-shift the spell squiggle so the wave begins at its top (cosine start) under the word's first glyph. Joins item 6's dials (thickness/amp/period); update any phase-pinning pixel assertion.
    ✅ LANDED @ 914fb4f (parallel wave, 2026-07-23) — squiggle phase-shifted to begin at the crest under the first glyph; phase-pin assertion updated.

39. **Add to dictionary (user-decided).** Right-clicking a squiggled word summons the EXISTING spell-suggestions overlay (⌘;) targeted at that word, gaining an "Add '<word>' to dictionary" row (present on keyboard summon too — one surface, NO new context-menu chrome class). The dictionary: a plain-text word list, one word per line, beside config.toml in the config dir; GLOBAL across projects; user-editable by hand; zero-network. Removal v1 = edit the file. Regression at the spell seam: an added word never squiggles again, including across restart.
    ✅ LANDED @ d10733b (parallel wave, 2026-07-23) — Add-to-dictionary row on the spell overlay + plain-text global word list beside config.toml; never-squiggle-again incl. restart tested; zero-network. LIVE-CONFIRM: the right-click summon path (keyboard summon + persistence are tested).

40. **BUG: `-` + Enter yields an UNWRITABLE empty list item (user, 22/07 notes).** Typing `-` then Enter leaves an empty item the caret can't write into. Fix the writability/caret-placement (likely a conceal-vs-empty-item interaction) regardless of continuation semantics; regression via `--keys`. SEMANTICS DECIDED 2026-07-23 (user's word): after `-` + Enter, EITHER the dash stays a literal `-` (no list) with a plain new line below, OR it becomes an empty bullet with the new line below — implementer picks whichever is simpler in the render/conceal model; the one hard requirement is that nothing ends up unwritable.
    ✅ LANDED @ cd7c18c (parallel wave, 2026-07-23) — '-'+Enter keeps the dash literal with a writable new line below; --keys writability guard.

41. **REOPEN: a nested child renders its PARENT list item bold (pasted-9; survived item 4c).** The item WITH a nested child renders bold while its childless sibling doesn't — smells like a loose-list/span-range SIBLING of the 4(c) marker-range bug, not the same defect (4c fixed the indent mis-highlight, and the user reports this twice now, pasted-4 then pasted-9). Write the failing test FIRST (nested-list fixture; assert the parent row's weight == body weight), then fix; sweep the neighborhood (tasks in lists, blockquotes in lists) per the bugs-cluster policy.
    ✅ LANDED @ d4e7980 (parallel wave, 2026-07-23) — appearance-oracle law: a nested parent item stays body weight (asserted at the span-structure seam, not raw pixels).

42. **REOPEN: image scroll jump when scrolling UP (item 5b verified the wrong direction).** The user's bottom-up repro is "very obvious" (reported twice): rows ABOVE the viewport likely carry estimated text-height geometry until visited, so scrolling up past an image jumps the page. Target = the browser model: EVERY image row reserves its real height doc-wide (eager header-sniff for dimensions; placeholder box before decode). Regression: open scrolled to the BOTTOM, scroll up through an image, assert zero jump across a motion capture — direction matters, which is exactly what the landed test missed.
    ✅ LANDED @ c092cc4 (parallel wave, 2026-07-23) — image rows reserve their real height doc-wide in the shape budget; direction-sensitive scroll-UP no-jump regression. LIVE-CONFIRM: scroll-up feel past images in --release.

43. **Wagtail: chunk the highlight stipple (user-decided 2026-07-23; graduated from Held).** The two Wagtail emphasis patterns stay EXACTLY as they are — Bayer stipple for the highlight class, inverse-video for selection; the user's word closes the earlier unification idea for good. What changes: the ONE shared stipple texture goes CHUNKY — the dither round's razor holds, so highlight spans + search matches + version-history insertions (prosediff renders insertions in the `==highlight==` wash; Wagtail's rendition of that wash IS the stipple, and it is Wagtail-ONLY — other worlds keep their colored wash) all coarsen together through the single density owner: quantize the Bayer lookup to ~2-logical-px cells (Retina-aware) so it reads as deliberate "dithered pixels," not fine noise. Cell-size candidates get a small gallery; the composite pixel law + real-GPU dither tests re-run (density assertions inherit the new cell math — still opaque-or-nothing per pixel, 1-bit-legal by construction).
    ✅ LANDED @ 62ddd97 (parallel wave, 2026-07-23) — shared stipple texture quantized to ~2 logical-px cells; vision confirms chunky + opaque-or-nothing (1-bit-legal). Candidate cell-size gallery in tmp.

44. **Collapse sections v1 — headings-only (user-decided, all sub-decisions in; graduated from Held).** A fold is VIEW state, never file content (no markers, no syntax — the file stays plain text). PERSISTENCE DECIDED 2026-07-23: in-memory for the app run ONLY (user's word: "while the app is open") — folds survive buffer switches within the run, NOT relaunch; nothing written to session.toml, which also erases the native/wasm split (works identically in the browser). OUTLINE DECIDED: untouched in v1 — no fold state shown there, no toggle from it. The rest (user's word, designer session): chevron affordance visible ONLY on hover or when the caret is on the heading line; palette command + chord, both conventions; a "Collapse other sections" command (fold everything but the caret's section — the daily-notes gesture); a collapsed heading shows a quiet `faint` tail (⋯ N lines), clickable to expand; any edit, search hit, or goto-jump landing inside a fold auto-expands it; selection never spans a fold invisibly; folds are never on the undo timeline. Tests: fold/unfold through `--keys` with sidecar state + row-geometry assertions (hidden rows contribute zero height); auto-expand laws at the action seam.
    ✅ CORE v1 LANDED @ e43a6a5 (wave 2, 2026-07-23) — fold model + fold/collapse-others actions + both-convention chords + auto-expand laws (edit/motion/search/goto) + fold-filtered render with zero-height hidden rows + sidecar (2662 pass). DEFERRED to item 47: the faint "⋯ N lines" tail glyph + chevron affordance (hover/caret) + click-to-expand. LIVE-CONFIRM: the chords ⌘⇧E (Fold section) / ⌘⇧M (Collapse others) on a real keyboard.

45. **Overlay/picker ALIGNMENT as a personality axis — audition → one fable look → land (user-decided 2026-07-23).** Today only two layouts exist (left-anchored Bars, centered pane); alignment becomes overlay-personality DATA (`left | center | right`) through the ONE `rowlayout` owner — no world code paths (grep-law). HARD RULE: an open overlay NEVER relocates — alignment applies at SUMMON time only, so theme-picker preview crossings between differently-aligned worlds hold the open picker in place (regression test). THE AUDITION GENERATES: a small deterministic capture gallery — for 2–3 candidate worlds (Cassowary and Mangrove first; right-rail suits a terminal readout and a tidal margin), the command palette WITH results + the theme picker WITH facets, current alignment vs right-aligned, page mode on. THE PICK: ONE fable pass over that gallery (fable just looks — the scarce resource, item-20 discipline; no back-and-forth), decides keep-or-flip per world, and the build LANDS the picks directly — the user pre-delegated the final call, no user gate. Tests: alignment-is-data grep-law; summon-time-only application; pixel assert that right-aligned rows genuinely right-anchor (row x-extents against the column edge).
    ✅ LANDED @ 38bfe10 (wave 3, 2026-07-23) — alignment is a left|center|right DATA axis (one resolver, originally frozen at summon; item 52 supersedes that freeze); fable flipped Cassowary + Mangrove to RIGHT; right-anchor + summon-time-only + grep-law tests (2675 pass, wasm green). **TASTE NOTE RESOLVED 2026-07-23 (user approved the sketch):** item 51 makes every right-anchored Bars picker a compact content-width group that genuinely hugs the right edge, including plain pickers with no secondary column. No conditional anchor and no decorative marker.

46. **Overlay facet/lens tab strip + section headers must sit on a plate (item 35 follow-up; wave-2 vision, 2026-07-23).** Item 35 landed the primary fix — bare shortcut chords now plate across all Bars worlds — but wave-2 vision found the SAME floating-commands class still present in two Bars spots: (a) the facet/lens TAB STRIP ("File Edit View Recent") plates only the leading tabs — on Mangrove "View" and "Recent" render bare (crisp text) over the blurred backdrop (same ListStyle::Bars path → presumably Galah/Magpie/Firetail too); (b) faceted grouped-lens SECTION HEADERS (e.g. "Recent") still render bare. FIX: every tab label and every section header draws on an overlay surface, ALL Bars worlds. Pixel regression: every tab's and header's background matches the card wash (no bare glyph over the unplated page). Menu-bar/palette coexistence is explicitly OUT (the menu bar is persistent chrome, not overlay-drawn text).
    ✅ LANDED @ 5c485bf (wave 4, 2026-07-23) — faceted section headers + lens-strip tabs now plate on ALL Bars worlds (Mangrove/Firetail/Galah/Magpie); vision confirms no bare glyph, list_surfaces laws green (2673 pass).

47. **Collapse affordances follow-up: faint ⋯ N-lines tail + hover/caret chevron + click-to-expand (item 44 v1 follow-up, 2026-07-23).** Item 44 landed a functional v1 (fold model, fold/collapse-others actions + both-convention chords, fold-filtered render with zero-height hidden rows, auto-expand laws, sidecar). Finish the deferred decorative/affordance passes: (a) the quiet `faint` "⋯ N lines" tail glyph on a collapsed heading — synthetic non-buffer text shaped + placed at the collapsed heading's row via the folded-heading→filtered-row + hidden-count mapping; (b) the chevron affordance visible ONLY on hover or when the caret is on the heading line; (c) click-to-expand (on the tail or the chevron). Tests: the tail renders with the correct N and adds to the heading row alone; a synthesized click on the tail/chevron expands the fold at the action seam (hover is live-only — flag).
    ✅ LANDED @ 81a5f22 (wave 4, 2026-07-23) — collapse affordances complete: faint "… N lines" tail (47a) + caret/hover expand chevron (47b) + click-to-expand (47c); nothing deferred (final gate 2688 pass, wasm green). LIVE-CONFIRM: the hover chevron reveal (caret-on-heading reveal IS tested) + fold feel across worlds.

Items 48–52 are the user's 2026-07-23 wave-3 OVERLAY decisions. They are dispatch-ready and share enough overlay/render footprint to cook as an ordered cluster rather than parallel file-colliding branches: 48 first (the selection appearance oracle), 49–51 in either merge-safe order, 52 last against the settled surfaces. Every render-touching round gets the standing surface×world audit + vision-smoke.

48. **REOPEN: the actionable picker row moves invisibly; the visible highlight catches up only every SECOND Down (user live report, 2026-07-23).** This is the same user-visible class as item 8(e)'s selection-band glide desync despite that round's `retarget_band` fix: one Down advances the REAL/actionable selection but leaves the band on the prior row; the next Down makes the pixels catch up. Write the failing outcome test FIRST and make it observe EVERY intermediate move, never only the final settled index: after each individual Down, the sidecar/action target and the PNG's visible selected-row band must name the SAME row. Include slow single presses + rapid repeat/in-flight retargeting, Pane + Bars, and left/center/right anchors; if the settled headless path hides the recurrence, extend the deterministic motion capture toward the live cadence rather than weakening the assertion. The neighborhood audit checks pointer highlight + Enter dispatch too. This item lands before item 52, whose jumping Theme picker makes honest per-step selection indispensable.
    ✅ LANDED @ d855ac4 (wave 5b, 2026-07-23) — HYBRID glide+snap: the chase_or_snap arbiter — a single deliberate move GLIDES (living-band kept), a move that outruns the in-flight glide SNAPS so band==selection every step (kills the every-2nd-Down trail). New hybrid_band_snap oracle (Pane/Bars, single+rapid, 3 anchors). LIVE-CONFIRM: the feel under real key auto-repeat (the rapid-snap half is structurally unreachable in headless capture — unit-tested + live only).

49. **Spell overlay's `Add '<word>' to dictionary` row is compressed/elided at an ordinary wide width (pasted-12; user-confirmed bug).** The synthetic add row is a first-class picker row: at ordinary desktop width its COMPLETE label renders, and the spell card measures ALL rows (suggestions + add action), not merely the correction corpus. Route it through the same `rowlayout`/card-width owner as every picker; narrow-window elision remains the existing last resort, not the wide default. Regression: a wide capture of short `zzz` shows the full `Add 'zzz' to dictionary` glyph span inside its surface; a long adversarial word still stays inside the card and elides only when the real cap is exhausted. Pixel/geometry assertion, not sidecar label alone.
    ✅ LANDED @ 91d3da0 (wave 5, 2026-07-23) — Add-to-dictionary is a first-class picker row: card floors to the widest row's char-grid + reserved cells, cap 360→520·scale; whole "Add 'word'" renders at wide width, long word still elides at the real cap. Outcome test on the real shaped panel.

50. **Split Pane overlay composition — the room shows between the query and results (user-decided, pasted-14).** Replace the current unified `Pane` presentation with TWO surfaces: the title/query input lives on the upper surface; a visible strip of the world's actual background separates it from one lower surface owning facets/section headers + candidate rows + footer/hints. This becomes the default for EVERY current Pane world EXCEPT Cassowary, whose existing unified Pane stays exactly as-is; Bars worlds keep their per-row surface language. The exception is personality DATA through the shared render owner, never a Cassowary identity branch. Tests: exhaustive surface roster/no-world-branch law; pixel arithmetic proves real ground is visible across the full inter-surface gap and that no lower glyph escapes its lower surface; narrow-width and zero-result pickers still form two valid, unclipped surfaces. Vision-smoke across representative flat/lava/1-bit worlds.
    ✅ LANDED @ c9b3c45 (wave 5b, 2026-07-23) — split-Pane composition as a PaneSplit capability (DATA): every Pane world's takeover card = upper query surface + a real-background gap + lower results surface; Cassowary stays unified (data exception, no world branch); no-world-branch law + pixel gap proof. LIVE-CONFIRM: feel across worlds.

51. **Every right-anchored Bars picker is ONE compact content-width group against the right edge (user-decided from the approved sketch; subsumes pasted-15 + item 45 taste note).** Plain pickers (Theme/Caret/Dictionary/Date) shrink the group to the widest visible primary label and genuinely hug the right edge while keeping text LEFT-aligned inside it — no 300–400px dead middle, conditional anchor, right-aligned prose, or decorative edge marker. When optional shortcut hints exist, they retain a tidy shared scanning column beginning after the widest visible primary plus the standard cell gap, never at the remote card/window edge. Bars plates span the complete group/cells, preserving item 35's every-glyph-on-a-surface law. Left/center layouts remain governed by the same `rowlayout` owner and must not regress. Pixel laws: plain + secondary-bearing right-anchored fixtures share one right edge; secondary cells align across rows; the label→shortcut gap is content-bounded; every glyph stays inside its plate at wide + narrow widths. Vision-smoke matches the user-approved three-panel sketch (current dead gulf → compact plain → compact shortcuts).
    ✅ LANDED @ cabca50 (wave 5b, 2026-07-23) — right-anchored takeover cards shrink to measured content + hug the right edge (no 300–400px dead middle); flat + faceted, Pane + Bars, plain + secondary; one shared right edge + content-bounded gaps proven by pixel law.

52. **Theme selection drops the user into the COMPLETE destination world — the real picker may jump (product law, user-decided 2026-07-23; supersedes item 45's summon-time freeze).** The Theme picker is not a neutral control panel or a frozen stage: changing the highlighted theme immediately applies every theme-owned visible property to the REAL open picker — world palette/type, background/ambient treatment, Pane/Bars/facets, title/placard/chrome face, motion, AND left/center/right card anchor. Interaction state alone survives the crossing: query, filtered corpus, selected theme, row window/scroll, and input focus. Re-anchor on deliberate keyboard selection movement or a click, not passive pointer hover; SNAP to the destination (no new glide animator). Explicit NOs: no dedicated neutral Theme picker, no prerendered thumbnail/specimen, no frozen-geometry hybrid. Amend PHILOSOPHY/DESIGN/THEMES in the landing round with the standing law: **theme-owned appearance changes completely; interaction state survives the crossing** — choosing a world drops you inside it. Regressions: crossings spanning left↔center↔right and Pane↔Bars preserve the selected row/query while pixel x-extents + surface/title treatment match the destination on EVERY step; rapid Down stays synchronized via item 48's oracle; passive hover does not start a spatial chase. Live-confirm the deliberate theatrical jump by eye.
    🟡 IN PROGRESS — claude (bg cook, wave 5c), 2026-07-23, dedicated agent. Unblocked: 48 (hybrid oracle) + 50/51 surfaces all landed; cooking the theme world-jump on the settled base.

53. **CI is RED on every main push since 2026-07-22 ~07:40 — the mac live-probe's soak contract is mis-calibrated for the 25s CI window (user-reported, designer session 2026-07-23).** NOT a product regression: between the last green run (29896271218 @ cd4f3af) and the first red (29901053613 @ 3038be2) the ONLY diff is a queue.md board edit — zero code. The probe itself proves the VM healthy every time (presents=825 ≥ the 100 floor; all 3 injected faults recovered, recovery_ms populated), but `cargo run -- --soak-gpu --soak-gpu-seconds 25` exits 1 because `required_cycles_met` (src/soak_gpu/mod.rs:336) demands the ABSOLUTE targets (RESIZE_TARGET=300, THEME_TARGET=300, OVERLAY_TARGET=150 — constants tuned alongside the 15-min DEFAULT_DURATION) while the time-fraction-paced stimulus on a slightly-slow shared "Apple Paravirtual" VM only EXECUTES ~95–96% before the hard time cut (290/287/143 on 29932492590; reproduced on 29993509794 today). Marginal by construction: identical code was green at 06:16 and red at 07:40. SECONDARY defect, same log: the workflow step prints its own grep verdict "→ PASS … but the soak process exited 1" — TWO disagreeing pass oracles for one probe (same-behavior⇒same-code law: one owner). FIX CONTRACT (mechanism = implementer's pick, item-40 precedent): the contract stays HONEST — the failure-swallowing layers 39a6520 removed must NOT return; the presents floor and all-3-faults-recovered stay hard gates; a genuinely broken soak (presents=0, faults<3, no recovery) still exits 1. Candidates: (a) RECOMMENDED — run-to-schedule-completion: the soak keeps stepping until the full stimulus schedule has EXECUTED (bounded grace, say ≤2× the requested seconds), so the cycle contract stays absolute and elapsed time becomes a reported outcome, not a guillotine; (b) a declared short-run tolerance (cycles ≥ a named fraction of target when requested duration < default) — simpler but a magic number; (c) bumping `--soak-gpu-seconds` alone only moves the margin — acceptable only as a stopgap alongside (a)/(b). Either way, collapse the workflow's outer grep-verdict so the binary's exit status is the ONE verdict (the step keeps tailing the report for humans). Acceptance: a unit test pinning the short-duration contract math (slow-VM simulation: full schedule executed late ⇒ PASS; presents=0 or faults<3 ⇒ FAIL regardless); then 3 consecutive green `mac live-probe` runs on the free tier. Footprint: `src/soak_gpu/` + `.github/workflows/ci.yml` — disjoint from the 48–52 overlay cluster, parallel-safe. PRIORITY: while this is red every push gate is noise — cook ahead of new feature work.
    🟡 IN PROGRESS — claude (bg cook, wave 5c), 2026-07-23, dedicated agent. Fix (a) run-to-schedule-completion (bounded grace ≤2× requested) so the cycle contract stays absolute + elapsed is reported not a guillotine; collapse the workflow's outer grep verdict so the binary exit is the ONE oracle; keep the presents floor + all-3-faults hard gates. Unit-test the short-duration contract math (slow-VM sim). Disjoint from 48-52; prioritized. NOTE: the 3-green-`mac live-probe` acceptance is verified on CI post-push (can't run GitHub Actions locally).

## Held — user decision or taste

- **Export save-dialog platform scope — DECIDED 2026-07-20 (user's word):** both macOS and Linux. One cross-platform seam, live-App-only and capture-gated (headless export takes an explicit path, no dialog). Codex may proceed on resume.
- **Per-world living-band choreography.** The mechanism is data-driven; audition distinct motion voices such as TwoShape, Slam, and Soft against the uniform Morph baseline. Live feel is the oracle.
- **Site deployment.** Deploy only on the user's explicit word.
- **Per-world copy-pulse differentiation.** Flagged by the user as a possible future tweak (2026-07-22 live pass; motion is universal by design today). Unqueued; needs a design conversation first.

## Release blockers and reminders

- App icon.
- Dictionary/font/license notices plus code copyright/NOTICE review.
- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user's explicit word. A dry run may precede them.

## Live confirmation still useful

- **Still OPEN (not yet confirmed):** Currawong star breathing + Retina density; Dawn/Bilby world feel. Writer-diff panel/Tab flow + zoom readout feel. **Gutter frost (item 2) — user "doesn't notice the frost" on a recompile; verify it's at least FAINTLY visible on a lava world in page mode (possible visibility concern, not just Retina taste).**
- **Parallel-wave landed 2026-07-23 (bg cook) — live/feel confirm pending:** item 27 phantom resize-handle near a revealed mixed image (right-click/drag); item 42 scroll-UP feel past images in `--release`; item 39 right-click "Add to dictionary" summon (keyboard path + persistence tested); item 31 twinkle over real seconds + Retina density; item 43 stipple cell-size taste (2px chosen; candidate gallery in tmp).
- GPU memory: no action unless the 6 GB symptom recurs; then probe the live surface path with the window foregrounded.

## Standing execution notes

- Follow `AGENTS.md` for toolchain PATH, verification, audit triggers, and capture truth. Follow this directory's `README.md` for claim-first coordination and orchestrator-only board writes.
- **TRIPWIRE — never create a worktree under `/private/tmp`** (macOS wipes it on reboot/periodic cleanup; the 2026-07-20 wipe destroyed every `/tmp` worktree, and the poster-bars round survived only by replaying Codex's session log). Use `.claude/worktrees/` or another durable path, and COMMIT work-in-progress before any pause — a usage-cap stop, an overnight gap.
- Never pipe a build or test gate through `head`, `tail`, or another command that can hide its exit status; record a positive pass count.
- **TRIPWIRE — run the wasm gate on EVERY train** (`cargo build --target wasm32-unknown-unknown`, cheap incremental). The has-glyph round (ff2b359, landed fb0c9c3) shipped a native-only `crate::export::pdf_glyph_probe()` call into the unconditionally-compiled `perfbench.rs`, silently breaking the browser build until 2026-07-22's push gate caught it (fixed @ b518587, block gated native-only + 0 on wasm). Native-only helpers (`#[cfg(not(target_arch = "wasm32"))]`, e.g. the whole PDF path) must never be called from code that also compiles to wasm — the hidden `--bench-perf`/bench harnesses DO compile to wasm even though the flags are unreachable there.
- Retry impossible-looking Rust failures with `CARGO_INCREMENTAL=0` before diagnosing product code.
- Never kill `awl` by bare process name; terminate only a PID owned by the current run.
- Background model/effort routing follows the Brew skill and any narrower repository rule. Record substitutions when the launcher cannot enforce a requested override.
- **TRIPWIRE — reclaim worktree disk after each cook wave.** The Workflow tool's isolated worktrees (`.claude/worktrees/wf_*`) each carry a full `target/` (tens of GB) and are NOT auto-pruned; they accumulate across waves. A landed wave's worktrees are merged to main and disposable — `git worktree remove --force` them (then `git worktree prune`) once the wave integrates. The 2026-07-23 cook filled a 926G disk to 172Mi (202G across worktrees) and a vision `cargo test` failed mid-link with "No space left on device"; removing the completed wf_* worktrees freed 167G. Leave worktrees you did not create (codex/*, probes) alone.
