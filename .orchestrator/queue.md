# awl — live build queue

> This file contains only live execution state. For the multi-tool claiming protocol and board-write rules, see `README.md` beside it. Completed and superseded work lives in git history and `.orchestrator/reports/`; do not duplicate an archive here.

## Ready queue

10. **Minibuffer word-MOTION (⌥←/⌥→) — follow-up split off item 3(b), 2026-07-22.** Word-DELETE already landed; MOTION was correctly deferred: every overlay input (palette query + Rename/Link/Keep/Settings-value) is an append-to-end / pop-from-end field with NO in-query caret, and Left/Right are already bound to list-nav (lens-cycle / descend / ascend / selection move). Bringing ⌥←/⌥→ needs a real in-query caret MODEL across all 5 inputs + its render + resolving the Left/Right conflict — a design round, not a focused fix. Not started; needs a design pass before build.

Items 18 and 20 are the surviving 2026-07-22 interview outcomes (the rest of 11–21 landed — see reports/). Every decision is the user's word; each item embeds it.

18. **Overengineering audit round (bounded).** Produce a hit-list of over-defensive/speculative code (the malformed-timestamp-validation class), the USER APPROVES THE LIST, then cut each entry with tests. Editing correctness (graphemes, wrap, undo, CRLF, motion at boundaries) is sacred — cut machinery, never editing correctness.
20. **Pre-tag taste-pass bundle.** Fold the taste pass into the standing pre-tag journey sweep: ONE screenshot export across worlds + ONE fable judging session per tag (hard cap — fable usage is the scarce resource). Judges: per-world bullet glyphs (item 15), squiggle size/baseline incl. Bilby (item 6), dash padding + Saltpan font outcomes (item 7). Fires at the next pre-tag sweep, not before.
24. **User-facing docs refresh (user-decided 2026-07-22, release-adjacent).** GUIDE.md, the welcome/tour doc, and the site guide get a full pass against the current product: the identity-round vocabulary, the four-door chords, and the interview features as they land (goto unified, notes flip, insert date, selection-reveal…). Docs voice rules apply (matter-of-fact, tables + short declarative sentences, facts verified). Fires after items 11–16 land so it documents a settled target, before the user's release; item 23's law then keeps it honest permanently.
25. **Off-cursor image conceal vs spell/nit underline artifact (follow-up from item 4, 2026-07-22).** When an image line is OFF the caret's line/block, its WYSIWYG-concealed raw source (`![alt](path)`) still leaves a small, mispositioned spell/nit underline artifact inside the placeholder card (a faint muted dash + a warm-red tick). Reproduces for ANY off-cursor image (nested or not) — a spell/nit-vs-conceal interaction, NOT list-nesting or caption specific. Found + vision-confirmed during item 4 (visible near the left edge of the card in md-nested-list-image.png). Fix: exclude the zero-width-concealed image-source range from the spell/nit scan (or suppress the underline when the source advance is collapsed). Regression test at the spell/render seam.

27. **`image_hit_rects()` reads a STALE `revealed` flag (pre-existing, follow-up from item 16, 2026-07-22).** `image_hit_rects()` reads `image_report`'s STORED `revealed` field directly rather than through `images_report()`'s always-fresh override, so a resize handle can arm on a stale reveal state. Confirmed ORTHOGONAL to selection (reproduces with a pure CARET move onto an off-cursor mixed image line — zero selection). Pre-existing, flagged (not fixed) during item 16's image-double-render fix. Fix: route `image_hit_rects` through the fresh `images_report()` override; regression-test the caret-move-onto-image-line stale-handle case.

28. **`delete_word_forward` has the pre-item-3(a) over-delete bug (found by item 19's audit, 2026-07-23).** The ADVERTISED native mirror of ⌥⌫ — ⌥+forward-Delete (keymap.rs), `Buffer::delete_word_forward` (buffer/edit.rs) — STILL uses the OLD `skip-nonword-then-word` rule that item 3(a) proved wrong for the BACKWARD direction. Repro: `⎸... abc` (caret before a punctuation run + a word) → the forward chord deletes `... abc` (BOTH the punct run AND the word) in one stroke — the exact over-delete the user rejected for backward. Existing `delete_word_forward` tests only exercise WHITESPACE runs, never punctuation, so the asymmetry was missed. FIX = the forward mirror of item 3(a): a `word_delete_forward_boundary` deleting exactly ONE token class (consume trailing whitespace, then one word-run OR one punct-run, never both). **DECIDED 2026-07-23 (user's word — the symmetric rule; supersedes the live-macOS-check blocker):** one token per stroke, forward too — `⎸... abc` ⌥+forward-Delete removes the `...` run ALONE (leaving ` abc`), never both classes. (The user restated the rule via the backward twin — `... abc⎸` ⌥⌫ deletes only `abc`, the already-landed 3(a) behavior — and blessed the mirror; a live macOS sanity check is welcome but the user's word governs, as in 3(a).) Dispatch-ready. (Minibuffer forward-delete is a safe no-op — no `DeleteWordForward` arm in `overlay_intercept` — so no parallel bug there.)

29. **Checked-task DIM may not render + needs a non-vacuous outcome test (found by item 19's verify, 2026-07-23).** The verifier reverting the ENTIRE checked-task dim (`MdKind::Task(true)`/`TaskDone` → full ink in `md_attrs`) produced BYTE-IDENTICAL frames (max_delta=0) on the tested world — so EITHER the checked-task text/box dim is a visual NO-OP (a real defect: checked todos don't actually recede) OR the fixture didn't exercise it (mono world where `[ ]`/`[x]` advances match + body byte-identical). INVESTIGATE whether the dim reaches the framebuffer (checked vs open task on a PROPORTIONAL-body world, caret off-line, pixel-diff the BODY region); if it's a no-op, fix the dim. Either way write a NON-vacuous outcome test that FAILS when the dim is reverted — the task-list neighborhood's missing appearance oracle (item 19 dropped its vacuous first attempt). USER APPROVED dispatch 2026-07-23.

Items 30–44 are the 2026-07-23 designer-session outcomes (fable designer + user, over the user's 22/07 notes; 43/44 graduated from Held after interview round 2). Each embeds its decision; none needs a further user check before build.

30. **Mopoke body face + bullet triple (user + fable, 2026-07-23).** User verdict: Quattro's duospace reads as a mono body — swap Mopoke's `font` iA Writer Quattro S → **Bitter** (warm screen-bred slab, cosy in dim light; already bundled via Magpie — face-sharing is precedented (Plex Sans ×2, Iosevka ×3) and `at_least_six_distinct_faces` is unaffected; keep `heading_bold`, Bitter-Bold is bundled; `mono` stays IBM Plex Mono). The ROSTER-WIDE mono-body question is CLOSED — user's word: the rest of the roster keeps its mono faces. Bullets: `('\u{E670}', '\u{EF92}', '\u{E67D}')` — fable-picked 2026-07-23 from candidate triples rendered at bullet scale on Mopoke's own ground/ink: E670 keeps the rosette (the dash-ornament vocabulary link item 7 established), EF92 is the rosette's OPEN four-fold sibling (same damask register, visibly lighter), E67D a tiny foliate sprig — weight descends with depth in ONE register (the complaint was exactly the two-register clash of dense E670 → airy ❦). Build round re-verifies in-app: `bullet_scale` fit (the 0.8 literal may need retuning for Bitter's advance), `bullet_glyph_never_touches_the_following_text_in_any_world`, heading metrics under Bitter, vision-smoke.

31. **Currawong star lifecycle (user-decided).** "I don't notice the blinking at all" — root cause is authored: `floor: 0.12` (a star never vanishes) + the quiet-band ceiling (peak ≤ the muted rung's deviation) tuned the twinkle below perception. Keep the hashed-cell layout + the one ambient clock (byte-determinism laws intact — NO true randomness; "random" is the integer hash). Change the ENVELOPE: per-star lifecycle — dark dwell at true zero → rise → shine → fade — so the visible population changes (stars appear and die, the user's ask); per-star tint from a tiny LOW-SAT palette (blue-white / white / champagne — real-star colors; amber guard holds by low sat). Stars get their OWN visibility band law (a real floor AND a calm ceiling ABOVE the muted whisper cap) — a deliberate relaxation of the ambient-stars law, user's word. Retina density check rides along (the open live-confirm). Gallery candidates via `AWL_STARS_PHASE`; twinkle feel over real seconds stays human-confirm.

32. **Frost becomes a RenderCaps capability; Mangrove outline plate → gentle frost (user-decided).** ONE frost recipe (blur radius + dim + tint), one owner; consumers: the outline pills + the gutter corner (overlay backdrops may join later); per-world dials as DATA (grep-law: no world code path); the 1-bit exclusion stays structural; the frost cache keeps hashing the held lava phase. Mangrove: the outline PLATE (pasted-10 — "kinda ugly") is replaced by the gentle frost pill, and the look ALIGNS with the page-mode gutter frost (the user's own ask). Tune ONCE to "visible but gentle": the user currently can't see the gutter frost AND dislikes the plate — the same recipe mistuned in opposite directions. Subsumes the open "gutter frost faintly visible?" live-confirm.

33. **Caret morphs to a plain bar during mouse drag (item 8d nuance, user-confirmed 2026-07-23).** While drag-selecting, the CARET renders as a thin bar; on release it returns to the configured style (Morph, for the user). The mouse POINTER (already i-beam, confirmed) is unchanged. Live feel human-confirm; assert the bar state through the caret-mode capture where reachable.

34. **Summoned overlays own the margins (user-decided).** While ANY overlay is open, the outline + gutter are suppressed; they return on dismissal. Fixes the theme/caret-picker collision (those stay CRISP — no blur added, live world preview is their point) and declutters the palette. Consistent with the lava rail-carve precedent (chrome yields the margin). Capture test: overlay open ⇒ zero outline/gutter ink.

35. **Overlay text must sit on an overlay surface (Mangrove Bars defect, pasted-11).** The Bars personality plates each row's LABEL but the right-aligned shortcut hints (⌘O, ⌘⇧P…) and section headers ("Recent") render BARE over the blurred page — the user's "floating commands." Rule: every glyph an overlay draws sits on an overlay surface — the bar spans the full row including the shortcut column (or hints move inside the bar), and headers get plated. Also repro the menu-bar/palette coexistence visible in the same screenshot. Pixel regression: no overlay text over unplated backdrop, all Bars worlds.

36. **Date-format PICKER (user-decided).** Promote the cycling settings row to the caret-picker pattern: a picker overlay listing all five formats, each rendered with TODAY'S date — pick by sight, what you see is what inserts; the settings row becomes the entry point that opens it. Capture keeps the fixed placeholder date (item 14's determinism). Resolves the live-pass "settings-row clarity" nuance.

37. **Theme-switch latency round (user-decided).** (a) SETTLE-LATENCY readout in debug mode: input event → settled present, "theme switch settled: N ms", plus a once-per-switch phase breakdown (font load / reshape / row-geom / atlas upload / first present) so the dominant cost names itself instead of being guessed. (b) DEBOUNCE experiment: the user localized the freeze — three rapid picker Downs, freeze lands on the THIRD — consistent with the preview debounce coalescing the first two and the felt freeze being the ONE reshape that fires on settle. Run with the debounce at 0 to expose true per-step cost (expected: a smaller freeze on EVERY step — a diagnostic, not the fix); with the readout's numbers, decide tune/keep/kill. Watch the crossing-bracket suspects too (present-transaction arming on ambient/1-bit picker crossings is compositor-side latency the app's timers never see). External ground truth: AGENT-DRIVEN end to end, no human recording (user-decided 2026-07-23). A live-probe-family script (the `ci-live-probe.sh` lineage — "extend the harness toward reality"): launch the LIVE app foregrounded (occlusion tripwire), start `screencapture -v -V N` (records the compositor's own output for N seconds, auto-stops; 60fps ≈ 17ms resolution suffices for a 100ms+ freeze), drive Down presses into the picker via osascript/System Events, then the agent explodes frames (ffmpeg) and brackets the interval by pixel arithmetic — start marker = the Down-press's own row-highlight move (the first visible response), end = the last frame where the document region still changes; frames/fps = felt latency, cross-checked against the readout. ONE-TIME setup the user grants once: Screen Recording + Accessibility permission for the driving shell. Two deliberate NOs: this canNOT be headless (the headless path has no clock and no compositor BY DESIGN — felt latency is a live-window phenomenon; hands-free ≠ headless), and NO in-app recording feature (the app testifying about itself is what the readout already is — the external witness must be the OS's pixels, and a video recorder inside a calm text editor is exactly the infrastructure machinery the scope forbids).

38. **Squiggle starts at the crest (user's note).** Phase-shift the spell squiggle so the wave begins at its top (cosine start) under the word's first glyph. Joins item 6's dials (thickness/amp/period); update any phase-pinning pixel assertion.

39. **Add to dictionary (user-decided).** Right-clicking a squiggled word summons the EXISTING spell-suggestions overlay (⌘;) targeted at that word, gaining an "Add '<word>' to dictionary" row (present on keyboard summon too — one surface, NO new context-menu chrome class). The dictionary: a plain-text word list, one word per line, beside config.toml in the config dir; GLOBAL across projects; user-editable by hand; zero-network. Removal v1 = edit the file. Regression at the spell seam: an added word never squiggles again, including across restart.

40. **BUG: `-` + Enter yields an UNWRITABLE empty list item (user, 22/07 notes).** Typing `-` then Enter leaves an empty item the caret can't write into. Fix the writability/caret-placement (likely a conceal-vs-empty-item interaction) regardless of continuation semantics; regression via `--keys`. SEMANTICS DECIDED 2026-07-23 (user's word): after `-` + Enter, EITHER the dash stays a literal `-` (no list) with a plain new line below, OR it becomes an empty bullet with the new line below — implementer picks whichever is simpler in the render/conceal model; the one hard requirement is that nothing ends up unwritable.

41. **REOPEN: a nested child renders its PARENT list item bold (pasted-9; survived item 4c).** The item WITH a nested child renders bold while its childless sibling doesn't — smells like a loose-list/span-range SIBLING of the 4(c) marker-range bug, not the same defect (4c fixed the indent mis-highlight, and the user reports this twice now, pasted-4 then pasted-9). Write the failing test FIRST (nested-list fixture; assert the parent row's weight == body weight), then fix; sweep the neighborhood (tasks in lists, blockquotes in lists) per the bugs-cluster policy.

42. **REOPEN: image scroll jump when scrolling UP (item 5b verified the wrong direction).** The user's bottom-up repro is "very obvious" (reported twice): rows ABOVE the viewport likely carry estimated text-height geometry until visited, so scrolling up past an image jumps the page. Target = the browser model: EVERY image row reserves its real height doc-wide (eager header-sniff for dimensions; placeholder box before decode). Regression: open scrolled to the BOTTOM, scroll up through an image, assert zero jump across a motion capture — direction matters, which is exactly what the landed test missed.

43. **Wagtail: chunk the highlight stipple (user-decided 2026-07-23; graduated from Held).** The two Wagtail emphasis patterns stay EXACTLY as they are — Bayer stipple for the highlight class, inverse-video for selection; the user's word closes the earlier unification idea for good. What changes: the ONE shared stipple texture goes CHUNKY — the dither round's razor holds, so highlight spans + search matches + version-history insertions (prosediff renders insertions in the `==highlight==` wash; Wagtail's rendition of that wash IS the stipple, and it is Wagtail-ONLY — other worlds keep their colored wash) all coarsen together through the single density owner: quantize the Bayer lookup to ~2-logical-px cells (Retina-aware) so it reads as deliberate "dithered pixels," not fine noise. Cell-size candidates get a small gallery; the composite pixel law + real-GPU dither tests re-run (density assertions inherit the new cell math — still opaque-or-nothing per pixel, 1-bit-legal by construction).

44. **Collapse sections v1 — headings-only (user-decided, all sub-decisions in; graduated from Held).** A fold is VIEW state, never file content (no markers, no syntax — the file stays plain text). PERSISTENCE DECIDED 2026-07-23: in-memory for the app run ONLY (user's word: "while the app is open") — folds survive buffer switches within the run, NOT relaunch; nothing written to session.toml, which also erases the native/wasm split (works identically in the browser). OUTLINE DECIDED: untouched in v1 — no fold state shown there, no toggle from it. The rest (user's word, designer session): chevron affordance visible ONLY on hover or when the caret is on the heading line; palette command + chord, both conventions; a "Collapse other sections" command (fold everything but the caret's section — the daily-notes gesture); a collapsed heading shows a quiet `faint` tail (⋯ N lines), clickable to expand; any edit, search hit, or goto-jump landing inside a fold auto-expands it; selection never spans a fold invisibly; folds are never on the undo timeline. Tests: fold/unfold through `--keys` with sidecar state + row-geometry assertions (hidden rows contribute zero height); auto-expand laws at the action seam.

## Held — user decision or taste

- **Export save-dialog platform scope — DECIDED 2026-07-20 (user's word):** both macOS and Linux. One cross-platform seam, live-App-only and capture-gated (headless export takes an explicit path, no dialog). Codex may proceed on resume.
- **Per-world living-band choreography.** The mechanism is data-driven; audition distinct motion voices such as TwoShape, Slam, and Soft against the uniform Morph baseline. Live feel is the oracle.
- **Site deployment.** Deploy only on the user's explicit word.
- **Collapse sections + Wagtail chunky stipple — RESOLVED 2026-07-23 (interview round 2), graduated to ready items 44 and 43.** Kept here only as the pointer; every decision is embedded in the items.

## Release blockers and reminders

- App icon.
- Dictionary/font/license notices plus code copyright/NOTICE review.
- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user's explicit word. A dry run may precede them.

## Live confirmation still useful

- **The anticipated suggestions list WAS discussed — 2026-07-23 designer session; the pre-build hold is LIFTED.** Outcomes queued as items 30–44 (collapse sections → 44, Wagtail chunky stipple → 43): i-beam nuance → item 33; settings-row clarity → item 36; theme-switch lag → item 37. Per-world copy-pulse differentiation was NOT covered — it stays a future tweak, still unqueued.
- **Still OPEN (not yet confirmed):** Currawong star breathing + Retina density; Dawn/Bilby world feel. Writer-diff panel/Tab flow + zoom readout feel. **Gutter frost (item 2) — user "doesn't notice the frost" on a recompile; verify it's at least FAINTLY visible on a lava world in page mode (possible visibility concern, not just Retina taste).**
- GPU memory: no action unless the 6 GB symptom recurs; then probe the live surface path with the window foregrounded.

## Standing execution notes

- Follow `AGENTS.md` for toolchain PATH, verification, audit triggers, and capture truth. Follow this directory's `README.md` for claim-first coordination and orchestrator-only board writes.
- **TRIPWIRE — never create a worktree under `/private/tmp`** (macOS wipes it on reboot/periodic cleanup; the 2026-07-20 wipe destroyed every `/tmp` worktree, and the poster-bars round survived only by replaying Codex's session log). Use `.claude/worktrees/` or another durable path, and COMMIT work-in-progress before any pause — a usage-cap stop, an overnight gap.
- Never pipe a build or test gate through `head`, `tail`, or another command that can hide its exit status; record a positive pass count.
- **TRIPWIRE — run the wasm gate on EVERY train** (`cargo build --target wasm32-unknown-unknown`, cheap incremental). The has-glyph round (ff2b359, landed fb0c9c3) shipped a native-only `crate::export::pdf_glyph_probe()` call into the unconditionally-compiled `perfbench.rs`, silently breaking the browser build until 2026-07-22's push gate caught it (fixed @ b518587, block gated native-only + 0 on wasm). Native-only helpers (`#[cfg(not(target_arch = "wasm32"))]`, e.g. the whole PDF path) must never be called from code that also compiles to wasm — the hidden `--bench-perf`/bench harnesses DO compile to wasm even though the flags are unreachable there.
- Retry impossible-looking Rust failures with `CARGO_INCREMENTAL=0` before diagnosing product code.
- Never kill `awl` by bare process name; terminate only a PID owned by the current run.
- Background model/effort routing follows the Brew skill and any narrower repository rule. Record substitutions when the launcher cannot enforce a requested override.
