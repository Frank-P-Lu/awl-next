# awl — build queue

> **Multi-tool claiming protocol** (Codex + Claude Code + human): see `README.md` beside this file — claim-then-work (commit the claim BEFORE writing code), work in a worktree branch named on the claim line, re-read this board at HEAD before firing anything, flip to ✅ LANDED @ sha when merged.

## 📣 PUSH-POLICY UPDATE (2026-07-15) — REPO IS PUBLIC, CI-MINUTE HOLD LIFTED
Verified via `gh repo view`: visibility PUBLIC, Actions enabled. Standard-runner minutes (including mac) are free on public repos → the July "pushes held" batching constraint is obsolete. Resume the normal policy: push main after each green merge train (tags/releases still need the user's explicit word). 4 commits unpushed right now (56616d8 checkpoint + 3 board commits) — the push rides the first cooking round's green landing so the checkpoint gets hard-gated on a full suite first. CLAUDE.md's "mac runners bill 10× on the private repo" line is now stale — fix at the next doc-sync (the working tree currently carries a user edit to CLAUDE.md; don't mix).

## 🐛 BOLD ON MONO-DISPLAY WORLDS SHAPES IN A FOREIGN SANS (user screenshot 2026-07-15, Firetail) — 🟢 QUEUED (fix direction = user taste pick)

**Evidence (verified by capture, all five mono worlds):** `**bold**` markdown on
Firetail / Potoroo / Tawny / Currawong / Mangrove shapes in the SAME non-mono
bold sans (closest visual match among registered 700s: Figtree Bold; exact face
doesn't change the fix), complete with its merged-ﬁ ligature — the user's "weird
FI ligature" report. Regular Xenon prose does NOT ligate fi (verified — the
weirdness is entirely the foreign face). This contradicts the documented intent
at `render/spans.rs:294-299` ("the mono renders unbolded"): requesting Weight
700 in a Regular-only family trips the `weight_diff == 0` fallback trap the
codebase already documents (the IBM Plex Mono Light tripwire), DROPPING the
family instead of degrading within it. Repro:
`cargo run --release -- --screenshot out.png --theme Firetail --measure 45 <md with **bold**>`.

**Fix options (pick one):**
- (a) Pin bold spans to the display face's REGISTERED weight on Regular-only
  worlds — matches the documented trade (emphasis carried by the revealed `**`
  markers); the CJK weight-pin at `spans.rs:87-124` is the existing template.
- (b) Bundle real mono Bolds (JetBrains Mono / Iosevka / Monaspace Xenon ship
  Bold upstream; verify licenses before claiming OFL) — mono bolds keep the
  grid (same advance), so this gives REAL emphasis in the code-flavored worlds
  at ~4 embedded faces of binary cost. IBM Plex Mono needs care (we ship
  Light-300 deliberately; `mono_safe_weight`).

**Done / verify:** extend the existing bold law tests
(`render/tests/theme.rs:416` registration + `:444` regression — both currently
cover only the 10 proportional worlds) with a NO-WILDCARD sweep over the mono
worlds asserting the chosen OUTCOME (bold content resolves to the SAME FAMILY
as the display face — never a foreign one); pixel probe across the five mono
worlds; regular-prose shaping byte-identical.

## 🧯 GPU OOM MUST NOT KILL THE EDITOR (user crash report 2026-07-15 morning, suspected overnight repeat) — 🟢 QUEUED

**Evidence:** launch panic `wgpu error: Out of Memory` at `Queue::write_buffer`
(wgpu-29.0.3 `wgpu_core.rs:1952`) — wgpu's DEFAULT uncaptured-error handler
panics, so a transient Metal allocation failure kills the whole editor. The
machine recovered within a minute (retry launched clean; memory-pressure level
normal; awl RSS flat at 173MB over a 75s idle sample — no resident leak at
rest). Overnight crash unconfirmed (unified log empty for awl; a Rust panic
only reaches the launching terminal's stderr). Autosave + scratch stash mean
small blast radius — this is robustness polish, not data loss.

**Goal:** an allocation failure degrades calmly (skip/retry the frame + calm
notice), never a dead editor. Install `device.on_uncaptured_error` (or error
scopes around allocation-heavy paths); consider one short-backoff retry of
device/surface creation at launch before giving up.

**Also:** a bounded SOAK probe for the lava worlds — they redraw continuously
by design, so any per-frame growth gets all night to compound; run a release
soak sampling RSS/allocation counts and assert flat (the 75s sample was flat;
a soak is the honest test).

**Done / verify:** simulated OOM (error scope / forced-tiny device limits)
produces the calm-notice path, not a panic; soak numbers recorded; autosave,
daemon socket, and session teardown still clean after a degraded frame.

## 🏦 PARKED — VIEWPORT-DRIVEN TEXT SHAPING (2026-07-14 zoom-performance report)

**Goal:** retire the whole-document shaping assumption behind
`render/text.rs::full_shape_height` so zoom, DPI, font, and wrap changes shape
the visible viewport plus a bounded overscan window instead of every logical
line in a large document. This is item 5 from the zoom investigation, explicitly
banked by the user; it is NOT part of the current profiler + event-coalescing
round.

**Why parked:** row geometry, cursor motion, hit-testing, scrolling, selection,
WYSIWYG conceal, tables, and outline navigation currently rely on document-wide
layout knowledge. Treat this as an architecture round, not a local height cap.

**Done / verify:** one owner for shaped-window bounds; stable total-height and
line↔visual-row estimates outside the window; cursor/search jumps materialize the
target window before use; selections and tables crossing the window remain
correct; release zoom-transition cost is approximately viewport-bounded at
100/1,000/5,000+ lines; long-document scroll/jump/click capture laws plus live
zoom and high-velocity scroll confirmation. Preserve the current plain-text and
drop-to-source-on-cursor behavior exactly.

## 🏦 BANKED IF NECESSARY — CACHE DOCUMENT SEMANTICS ACROSS METRIC-ONLY REFLOWS (2026-07-15)

**Goal:** stop reparsing unchanged markdown, syntax roles, inline images,
tables, conceal spans, and related document semantics when zoom, DPI, font
metrics, or wrap width changes but the document bytes and semantic settings do
not. Reuse the prior semantic model and recompute only metric-dependent layout
and glyph attributes.

**Why conditional:** present-boundary zoom coalescing already cut the reported
burst by 3.7–4.2×. Fire this only if live confirmation or the
`--bench-zoom-burst` numbers still justify deeper work; do not add cache
invalidation complexity speculatively.

**Done / verify:** one explicit semantic-cache key covering text identity/version
and every parsing-affecting setting; zoom/DPI/wrap/font changes produce zero
semantic parses; edits and WYSIWYG/syntax-relevant setting changes invalidate
exactly once; parsed spans/images/tables are byte-identical to a cold rebuild;
release profiler reports parse and layout costs separately at 100/1,000/5,000+
lines.

## 🏦 BANKED IF NECESSARY — ONE SHAPE PASS PER METRIC TRANSITION (2026-07-15)

**Goal:** collapse the intermediate shape operations currently reachable during
a zoom/DPI/font/wrap transition into one final shape after metrics, size, wrap,
semantic spans, heading styles, and conceal attributes are all ready. No shaping
against state that another step in the same transition immediately supersedes.

**Why conditional:** this is the next bounded optimization after semantic reuse,
but it changes ordering around heading geometry, cursor targeting, conceal, and
row-cache invalidation. Fire only if the coalesced single transition remains too
slow in release measurements.

**Done / verify:** transition instrumentation proves exactly one whole-document
shape operation for a metric-only change; edit/incremental paths remain
incremental; heading caret placement, visual-row motion, scroll bounds,
selection, tables, images, and reveal-on-cursor laws stay green; final pixels and
sidecars match the current settled result; `--bench-zoom-burst` records the
before/after transition and first-frame costs.

## 📐 STANDING ORCHESTRATION POLICY — triggered spot-check audits (user-approved 2026-07-13)

Spawn an adversarial audit agent (matrix probe: state × surface × world/platform, ARITHMETIC assertions over pixels/sidecar, never model vision for the verdict, sampled along the changed axis) when a TRIGGER fires — never as a constant background hum (the standing load belongs to the law suite):
1. NEW AXIS VALUE lands (world / platform / convention / capability field) → probe it × the full surface roster (rosters = the code's own no-wildcard enums, e.g. OverlayKind).
2. IDENTITY-GATED REFACTOR lands → byte-identity preserves pre-existing bugs; follow with an OUTCOME audit of the area.
3. USER REPORTS ONE BUG in an area → audit the NEIGHBORHOOD (bugs cluster — the vertical-selection fix should have prompted "what else is invisible on Wagtail?").
4. A DEGRADATION/FALLBACK arm ships → aim a probe at exactly the degraded state.
5. PRE-TAG → one full journey sweep (use the product headlessly end-to-end across worlds).
RULE THAT COMPOUNDS: every audit that finds something ENDS by writing the missing law test — scouts find the hole, laws close it permanently.
PLUS THE VISION-SMOKE TIER (user's "5 screenshots" idea, 2026-07-13): every render-touching round's VERIFIER must LOOK at ~5 gallery shots drawn from the INTERACTION CHECKLIST (palette open + moved selection, search active, selection + caret, the changed feature in each state) and answer AFFORDANCE-LOCATING questions — "which row is selected? where is the caret? is any text clipped?" — never "does this look fine?" (agreeable-model trap). Failure to locate an affordance = defect signal. Vision = hypothesis-FREE noticing (catches what nobody asserted); arithmetic = hypothesis-driven proof (laws); the user = taste. Fold the checklist + question template into the distinguishability round's THEMES.md/CAPTURE.md amendments. Bake the vision-smoke requirement into future render-round verify prompts.

## 🎬 PLAYWRIGHT / SHOT-SCRAPER HARNESS ROUND (2026-07-14) — 🟡 IN PROGRESS — claude, 2026-07-15, branch playwright-harness (sequential workflow off main @ 56616d8; user-approved 1–5)

**Goal:** turn the capture harness from a final-state key macro into a truthful,
scriptable scenario runner that can emit fast deterministic demo films. Land in
the dependency order below; the film slice must never silently cross a seam the
headless driver does not implement.

1. **Strict replay truthfulness.** Classify every `actions::Effect`
   exhaustively as `Applied`, `Intercepted` (external handoff observed but safely
   not performed), or `Unsupported`. A strict scenario aborts on `Unsupported`,
   an unbound chord, or a missing GPU layout oracle, naming the exact action/effect.
   Keep legacy one-off `--keys` permissive if compatibility requires it, but warn.
2. **Fresh layout oracle per action.** Reshape from the CURRENT buffer/view before
   every scripted action (initially favor correctness over invalidation cleverness),
   including edit→wrapped-motion, zoom, page-measure, and buffer-switch cases.
3. **Shared search/replace input routing.** Extract the live search key interception
   into a renderer-independent seam used by both live input and headless scenarios;
   cover query typing/backspace, next/previous, case toggle, replacement editing,
   replace-one, and replace-all.
4. **Hermetic scenario filesystem.** Promote the existing `InMemoryFs` (or an
   equally hard sandbox) for normal scenario runs; seed files/config/history from
   the storyboard and never touch user files by default. Assert external actions
   such as URL-open/Trash as intercepted effects rather than performing them.
5. **Storyboard + trace/video v0.** Support `press`, `type`, `pause`, `run_for`,
   and basic state expectations. One run emits per-step PNG+JSON, `trace.json`
   (actions/effects/assertions), deterministic WebM, and optional MP4 through
   `ffmpeg` while retaining frames if encoding is unavailable. Drive motion with
   the virtual clock at fixed frame steps; this is deterministic visual review,
   not a claim about real compositor cadence.

**Explicitly deferred:** semantic surface locators, pointer/drag scripting, full
event-loop/compositor coverage, a complete `HeadlessApp`, and a polished trace-viewer UI.

**Done / verify:** prefixed `cargo build` + `cargo test`; Linux-convention tests;
wasm build; byte-identical repeated trace; regressions for search typing and
edit→wrapped-motion; one scenario that intentionally aborts on an unsupported
effect; and one checked-in storyboard demonstrating typing → search → selection →
palette with a human-reviewable film. Render-touching work also runs the standing
five-shot affordance-locating vision smoke above.

## 🔴 LIVE-TESTING POLISH ROUND (2026-07-14, user's live session on the lava worlds) — ✅ LANDED via 049ae41 + 318e1fe (verified in-tree 2026-07-15: field_viewport resize-freeze, lava phase in blur_signature, Moved handling, crash-notice → About/Settings, page-drag per-edge fix; live taste checks still open)

> **Cook-ready spec → `handoffs/2026-07-14-live-world.md`** (5 lava fixes · crash-notice+toast · 2 adaptive-column/geometry fixes · 3 design rounds; A/B/C disjoint, fire once the 14-file WIP is committed off a clean base). Roster + design direction (the per-world loudness dial, light+dark poles, the STUNNING bar, the ~20-world target, feature gaps) → **`WORLD-ROLES.md`**. Full running notes below.

User doing a live pass on the newly-landed lava worlds; a cluster of live-only rendering bugs + taste tweaks. Firetail confirmed FINE (smooth, blurred, reads as background). Harness can't repro any of the bugs (capture is deterministic single-frame, lava frozen t=0) — these are live-only classes.

DECISIONS LOCKED (AskUserQuestion, this session):
- **Mangrove lava "smaller" = SMALLER, SEPARATED BLOBS** — shrink radii + spread so it reads as several distinct globs, not one merged amoeba mass (that merge is the "gross" read; bf Duhai's reaction, user agrees). Tune `BACKDROP_BLOBS` (src/lava.rs) + possibly the field falloff; verify with a before/after AWL_LAVA gallery + the lava law tests.
- **Crash notice = RELOCATE ONLY** — remove the "awl crashed last time — ⌘P → Report a Problem" BOTTOM-CENTER notice (set at app.rs:~1013 from `crashlog::pending_notice`); surface a pending crash passively instead (a quiet About-card line, About-card idiom like the "checked … ago"/"⌘P → Credits" lines; user asked for "something in settings" too → a Settings "Report a Problem" row). KEEP all other center notices (save/rename/duplicate/move confirmations + the autosave clobber-guard warning). Center-notice writers enumerated (src/app/files.rs + app.rs): save "saved"/"save failed", rename ×4, duplicate ×2, move "move failed", clobber-guard "changed on disk…", crash — crash is the ONLY intruder. Keep crash-LOG writing + privacy law untouched; clear the pending marker on acknowledge so it never nags.

BUGS + DIAGNOSES (both agents landed 2026-07-14; all live-only classes):
1. **MANGROVE DRAG-FLASH** ✅ DIAGNOSED — NOT the animation: the metaball field geometry is tied to the LIVE viewport size, so every RESIZE tick re-lays the whole field; Mangrove's 5-level Bayer posterize snaps regions between levels as the drifting field crosses quantization boundaries → shimmer (Firetail's smooth field ramps imperceptibly → Mangrove-only). NUANCE: no `WindowEvent::Moved` handler exists, so a pure same-size move can't redraw/flash — the flashing gesture is edge/corner RESIZE or a cross-display scale-factor change (flagged to user to confirm). FIX: add `field_viewport` to lava `Globals` (drives centers/radii/amplitude ONLY; `viewport` still drives `in.px`, `margin` still drives the mask so lava keeps hugging the moving column); hold it at the last-settled size during an active resize, snap once on settle (reuse `resize_settle_at` + a generic `resizing` bool for cross-platform). Unit-test: field_viewport held across simulated resize ticks, updated once on settle. Firetail untouched.
2. **PALETTE-BLUR × LIVE WORLDS** ✅ DIAGNOSED — orthogonal spatial+temporal bugs. (2a "weird crosses": blurring the axis-aligned ~16px Bayer dither grid — the ×4 box downsample under-filters + the SEPARABLE Gaussian preserves the axis-aligned residual → cross/plus moiré; Mangrove-only. FIX: force `dithered=false` in `prepare_lava_layer` when `backdrop_blur()` is on — the lava is drawn ONLY into the offscreen blur capture while an overlay is open (visible pass skips `draw_document_layers`), nothing on-screen to sync; blur source becomes the smooth field → no grid. Log the relaxed "captured backdrop matches live doc" invariant (blurred to mush anyway). CAPTURE-VERIFIABLE: palette-open-on-Mangrove before/after.) (2b "different bg on live worlds": `blur_signature` (render.rs) omits the lava phase, so the frosted snapshot freezes at open-time phase while the lava keeps ticking → pop on close + stale re-open. FIX: hash `lava_render_phase()` into `blur_signature` (REQUIRED) + freeze the lava while a blur-eligible overlay is open — add an overlay term to `lava_should_tick` so phase HOLDS (do NOT route through `freeze_lava()`, which snaps to 0); mirrors pause-on-blur. = design choice **2A** (recommended: no pop, 0% idle, cheaper) over 2B (re-blur every tick, margins breathe, full re-render+blur per 100ms — DECLINED). Live-only + unit-test signature-includes-phase.)
3. **PAGE-WIDTH DRAG DOUBLES RATE (narrow/adaptive regime)** — NOT yet diagnosed; workflow discover stage. Narrow window: dragging the page-width handle grows only on the right (expected, left pinned for the outline rail) but at 2× the drag rate. User hypothesis: the pinned-left width delta is double-applied to the right. Owner: `adaptive_column_left` (render/geometry.rs) + `persist_page_width` / the page-drag-resize seam.

MANGROVE LAVA SMALLER (decided: smaller + SEPARATED blobs). NOTE: `BACKDROP_BLOBS` is a SHARED const (both lava worlds). Default plan = give Mangrove its OWN smaller/separated layout, leave Firetail's as-is (user said Firetail is fine) — per-world blob layout, theme-as-data idiom. Gallery verifies both.

OPEN (pending user): TOAST scope — should the KEPT center notices become auto-dismiss toasts (save/rename/duplicate/move = toasts; clobber-guard stays STICKY as a state, not an event)? Fold into the crash round, or keep crash round minimal (relocate only). Crash relocate itself is LOCKED (About-card line + Settings "Report a Problem" row; clear marker on acknowledge; keep the other center notices; privacy law intact).

USER HUMAN-TASKS (not code): buy a domain + dedicated support email, THEN swap `MAINTAINER_EMAIL` (src/crashlog.rs, one const) off the personal address (spam). Blocked on the domain purchase. NOTE: relocating the crash notice doesn't remove the exposure — "Report a Problem" in ⌘P still uses the address.

FIRING PLAN — 3 concurrent worktree workflows; a merge subagent reconciles any render.rs/app.rs overlap rather than serializing:
- **A — live-world lava rendering** (SEQUENTIAL; shares lava.rs/render/layers.rs/blur/shaders): flash-freeze (field_viewport) → blur undither → blur phase-in-signature + 2A freeze-on-overlay → Mangrove smaller/separated blobs (per-world) → gallery (AWL_LAVA: Firetail smooth vs Mangrove dither, before/after) + lava law tests + vision-smoke (palette-open-Mangrove: no crosses).
- **B — crash-notice relocate** (+ toast IF user picks): remove center crash notice; About-card line + Settings "Report a Problem" row; clear-on-acknowledge; keep other center notices; capture of About-with-pending-crash (marker = FS state, deterministic); default capture byte-identical; privacy law passes.
- **C — page-width drag fix**: diagnose-then-fix at the geometry owner; narrow-regime 1px-drag = 1px-measure unit tests.

NEW (2026-07-14 flurry, mid-collection):
4. **WINDOW DARKENS ON PURE MOVE (one screen)** ✅ DIAGNOSED — LAVA-SPECIFIC (structural: non-lava worlds issue ZERO redraws during a move → can't dip). NOT focus-dim (nothing in the renderer reads `focused`; no dim-on-blur exists) and NOT the lava freeze (only holds phase). Root cause: on a lava world, `about_to_wait` keeps arming ~10fps ambient ticks that PRESENT new metaball frames; a title-bar MOVE never arms `presentsWithTransaction` (unlike RESIZE — `WindowEvent::Moved` falls through to `_ => {}` at app.rs:1385), so those async CAMetalLayer presents are UNSYNCED with the window-server move transaction → compositor briefly shows a mid-move/reconfigured drawable → reads as "darker then normal" on Mangrove's dark margins. Same compositor-race CLASS as the resize-stretch fix, but for MOVE. FIX (recommended, try first): add a `Moved` arm that PAUSES the lava tick while moving (hold phase; gate `lava_should_tick` on a `move_active` flag cleared by a settle debounce, mirroring pause-on-blur) → no mid-move presents; one redraw on settle. (Alt: arm `presentsWithTransaction` during the move.) FOLDS INTO WORKFLOW A — and note it's the SAME "hold the lava still during a transient interaction" family as bug 2b (freeze-while-overlay-open) and bug 1 (freeze-field-during-resize): A's coherent thesis = animate ONLY when truly idle+focused+visible+no-overlay+not-moving.

⛔ PREREQUISITE BEFORE FIRING WORKFLOWS A/B/C: the user's WORKING TREE IS DIRTY — 14 files uncommitted, incl. the EXACT fix targets: `src/lava.rs` (+132 lines!), `shaders/lava.wgsl`, `src/render.rs`, `src/app.rs`, `src/theme/worlds.rs`/`model.rs`/`tests.rs`, + DESIGN/THEMES/WORLDS docs. A worktree workflow branches from a REF, not the dirty tree, so it would build on a base MISSING this WIP → merge conflicts / clobber risk in lava.rs. MUST commit (or stash) the WIP first so the fixes stack on top cleanly. The personality-gallery workflow is UNAFFECTED (isolated worktree, throwaway code, only PNGs copied out — no merge).

CLOBBER-GUARD RESET (user Q, answered from code files.rs:1590-1645): the sticky "changed on disk — autosave held" clears when the CONFLICT resolves — manual Cmd-S force-writes YOUR version + updates disk_mtime → notice flips to "saved" (yours wins); or reopening adopts theirs + clears (`load_path`:900). A mere edit re-checks + re-holds (correct — still diverged). DECISION: NO X BUTTON (would hide a live safety state while still unprotected); better = make the message self-explaining ("⌘S keeps yours · reopen for theirs"). Fold into the notice/toast round.

## 📏 PERF BENCH SUITE (user-approved 2026-07-15: "your bench suite sounds good") — 🟡 IN PROGRESS — claude, 2026-07-15, branch bench-suite
`--bench-suite`, one unified release-only runner: corpus tiers S≈500w / M≈2k / L≈50k (novel — the missing tier) / X pathological (one huge unbroken paragraph, heavy markdown) / CODE (large .rs), generated DETERMINISTICALLY from a fixed seed at bench time (no fixture blobs in git) × scenario matrix (cold-open→first-frame, typing burst, scroll page-through + jump-to-end, zoom burst, theme burst, search type+next×N, palette open, wrap/resize step). Every scenario WITNESSED (house law — a bench must prove the work happened). Emits the human table + bench.json (sidecar idiom); scripts/bench.sh diffs vs a checked-in benches/baseline.json (~20% warn threshold, machine-keyed, updated deliberately like the audit ledger). The existing five flags stay working (become cells/aliases — builder's call, flagged). PLUS work-count TRIPWIRES into cargo test (deterministic counts, NO timing in tests; only invariants true today). Cadence: merge-train days + pre-tag. NO CLAUDE.md edits this round (the tree carries a user edit; doc-sync later) — round docs live in benches/README.md. Banked synergy: bench scenarios ride playwright storyboards once that harness lands.

## 🌋 LAVA FOLLOW-UPS: OUTLINE LEGIBILITY + MOVE-FLASH REGRESSION (user reports 2026-07-15) — 🟡 IN PROGRESS — claude, 2026-07-15, branch lava-followups (one sequential workflow; same lava-family files)

**B — MOVE-FLASH "kinda back" (user, same session):** the Mangrove flash-while-moving reappeared despite 318e1fe's Moved handling + field_viewport freeze. Diagnose-FIRST round: enumerate every path that can present a new lava frame or re-lay the field around a window move (candidates: the settle-SNAP itself reads as the flash — same quantization-jump class as the resize bug; a move_active gating gap; ScaleFactorChanged mid-move on cross-display; tick armed before settle). Live-only compositor class — unit tests witness the gating state machine (held phase+field across a simulated Moved burst, exactly one settle redraw); LIVE confirmation flagged (user's eyes are the oracle). Must not regress the landed resize/overlay freezes.

**A — OUTLINE OVER LAVA (screenshot: Mangrove margin outline unreadable over the dithered blob):**

Flagged twice before but never queued (lava-probe v2 finding 3 "outline rail collides with left lamp — coexistence call needed"; WORLD-ROLES.md dither watch item) — now a real round. The current/selected entry (full ink) reads; MUTED entries drown in the Bayer dither. FIX SHAPE: one owner — preferred mechanism carves the outline RAIL band out of the lava field MASK whenever the outline is visible (the same margin mask that keeps lava hugging the column; outline hidden → lava reclaims the margin), soft-edge/scrim fallback if the hard carve reads badly, builder A/Bs + flags. Keyed off the Background capability, never a world name (theme_caps_law). Both lava worlds (Firetail proven fine or fixed same mechanism). LAWS: structural (rail band lava-free when outline visible — phase-independent) + legibility floor (muted entries clear the perceptible floor over local ground). Trigger-3 NEIGHBORHOOD audit: all margin-resident chrome (gutter, word-count, drag handle, debug panel) × both lava worlds, arithmetic. Non-lava worlds byte-identical. LIVE flags: carve-edge feel during blob drift.

## 🎨 PERSONALITY THREAD (user 2026-07-14) — gallery-driven, NOT a blind build; connects to the queued "OVERLAY PERSONALITY AS DATA" item

- **Persona placard "where did it go?"** — ANSWER: it LANDED (machinery `0e9e096` + facets `3891d8b`; `TitleStyle{InlinePrefix|Placard{corner,scale,ink}}` on RenderCaps; `AWL_OVERLAY_STYLE_FORCE` probe) but is DORMANT BY DESIGN — all 15 worlds default to InlinePrefix (byte-identical), so nothing visibly changed. Needs PER-WORLD assignment (one data line each). Probe gallery exists on the earlier artifact (Galah×3 BL/3.0/ghost = reference look; Kingfisher×2; Mopoke; Saltpan control).
- **Card borders to more worlds** — `elevation: Bordered` capability EXISTS, Wagtail-only today; user wants others able to use it = one data line per world + taste look (this IS part (a) of the queued overlay-personality item).
- **Wagtail PAGE border (thin white line around the WRITING COLUMN)** — NEW small capability (a page-column border, DISTINCT from the card border); fits Wagtail's stark 1-bit character (its "one thing" is minimalism/ground). Per-world data.
- ✅ GALLERY DONE (workflow wn8sjx9ak → 25 shots in gallery/personality/; Artifact published: https://claude.ai/code/artifact/6367081b-85ce-4c31-93df-eb61af0be964). FINDINGS/PICKS:
  - (1) **PAGE FRAME = the standout** → ship Wagtail 2px white (reads like a framed page on pure black; 1px = calmer; LIGHT worlds need a DARK line, white vanishes). Graduate the AWL_PAGE_BORDER probe to a real per-world capability.
  - (2) **PLACARDS** = the P3R ghost wordmark; reads on LIGHT worlds (Galah/Saltpan/Magpie, BL corner), FAINT on dark (Undertow near-invisible → dark worlds want a stronger ink); BL only (TR/BR clip long words); tracks the surface (COMMANDS/SETTINGS). OPEN DECISION: the wordmark BLEEDS to the canvas corners, NOT clipped-to-card (contradicts a STALE theme/model.rs::TitleStyle doc comment) — decide clip-vs-bleed (agent + I lean bleed).
  - (3) **CARD BORDERS** = subtle; rim reads on DARK, near-invisible on LIGHT (drop-shadow carries it); the probe forces only shadow+rim and LEAVES the selected-row band alone (forcing it white = the Wagtail invisible-row bug on colour worlds) → bordering a colour world is edge-only by design.
  - ✅ DECIDED (user delegated to orchestrator, 2026-07-15) + ✅ LANDED @ 95fc793 (claude, 2026-07-15) as the PERSONALITY-ASSIGNMENT round: BLEED wins (wordmark bleeds to canvas corners; fix the stale clip-to-card doc comment on TitleStyle). Placards BL-only — Galah 3.0/Ghost (the reference), Magpie Ghost, Mangrove STIPPLE (user call mid-round: the Bayer dither IS Mangrove's language — new stipple ink/texture variant on the Placard data, ladder-ink dots only, available to any world by one data line), Firetail Faint flat (smooth is Firetail's deliberate contrast with Mangrove — never dither it). Bordered elevation adds Currawong (OLED true-black kills shadows — the rim is functional elevation), Mangrove + Firetail (the card must hold an edge over moving lava). Page-frame graduates from the AWL_PAGE_BORDER probe to a real RenderCaps capability: Wagtail 2px white ONLY (the dark-line-on-light variant is reserved for the future light-silent pole world). Wagtail placard: NONE (user confirmed 2026-07-15; the stipple variant stays available as data if ever revisited). Quokka deliberately skipped (differentiates the Quokka↔Galah near-pair). All quiet/silent worlds stay InlinePrefix. NEW QUEUE ITEM logged from WORLD-ROLES.md: retire the runtime lens picker (axes → build-time ruler) — own round, not this one.

DESIGN DIRECTION EVOLUTION (user 2026-07-14, after a Persona-3-Reload deep-dive) — 🔑 THE FRAMING: **loudness is a PER-WORLD DIAL, not a global setting.** awl is "calm by default with room to shout"; themes exist for FRESHNESS (a new room when tired) → worlds must be DISTINCT. Calm end (reading worlds, Wagtail) stays minimal; LOUD end (statement/lava worlds — Firetail/Mangrove) turns up: off-center card, bold placard, the wild menu. The dial IS the product. Fits the existing "statement world" concept + capabilities-as-data (overlay_style ranges calm→bold). RULE THAT SURVIVES THE LOUD END: still LEGIBLE (Persona's slanted list still reads). The "clean core, loud frame" insight (Persona keeps the functional list clean + puts spectacle in the margins) = awl's OWN architecture already (clean writing column, character in the margins = lava/placard).
- THE TWO AWL-NATIVE LOUD DIALS (P5 lesson, user showed the P5 difficulty screen): a TYPE tool shouts in **TYPE + MOTION**, never decoration/chrome-for-its-own-sake. And even P5 keeps the RULE — loud type on the LABELS (SAFE/EASY/NORMAL/…), clean legible type on the READING text. So awl's loud dials are: (1) TYPE CHARACTER — a statement world's CHROME (placard/overlay title/lens tabs) in a more distinctive higher-contrast face (attitude where awl already spends type; NOT literal cut-out letters); (2) MOTION — a loud world's overlay ARRIVES (spring-in, livelier selection) = a per-world juice dial (deliberate extension of "only the caret gets juice", like the copy-pulse exception). Writing column ALWAYS stays pristine.
- 🟡 PROPOSAL (awaiting user y/n) — **FIRETAIL = the deliberate MAXIMALIST SHOWCASE world** (the loud-end proof-of-concept): turn EVERY dial up on Firetail alone — bold placard, off-center card, the slanted "wild menu", a distinctive title face, an overlay entrance animation. Poles of the spectrum: **Firetail = the P5 room, Wagtail = the silent room**, everything else on the dial between. Gives boldness a HOME so it never leaks into the reading worlds. Its own round; fires after WIP-commit + the lava fixes land under it.
- BANKED STRETCH — "THE WILD MENU" (part of the Firetail showcase): a Persona-style ITALIC + half-centered/slanted overlay list. REAL build (changes overlay LAYOUT, not just color) — big lift, the maximalist end of the overlay_style dial.
- 🧱 PALETTE-COMPOSITION ROUND (steal Persona's COMPOSITION, not its VOLUME): (1) kill the FAT BOTTOM LIP — dead empty space below the palette content (card taller than content; pin cause: reserved fixed-height vs oversized bottom padding); (2) OFF-CENTER placement — top-left reads more anchored than top-center AND opens the right side for the ghost placard (menu TL + wordmark BR = balanced asymmetry); global, or another per-world dial; (3) HEADER SPACING — a gap after the `commands ›` title + `File Edit View Recent` lens row to separate chrome from the list (negative space = a divider = the calm "edge"); (4) a defined CARD EDGE (the bordered elevation); (5) a clearer-but-calm SELECTED ROW. One "compose the overlays" round.
- BUG CONFIRM (from user's clearer shots): the Mangrove "blur STRIPES" = the SAME bug 2a (blur eating the axis-aligned Bayer dither → axis-aligned banding); fix (undither the blur capture) is in workflow A. MOVE-DARKEN bug reported "gone" by user → consistent with its intermittent compositor-race nature; STILL land the pause-on-move guard (don't drop it), lower urgency.

## ✅ LANDED (2026-07-10 night — release-infra round) — CI + release + fly-site workflows, docs synced

- **Release-infra round LANDED (docs-only push authorized + done this pass).** `.github/workflows/{ci,deploy-web,release}.yml` + `RELEASING.md` + `scripts/package-macos.sh` all landed on `main` (workflow-file SHAs: see `git log -- .github/workflows RELEASING.md scripts/package-macos.sh` for the exact commits — this pass did not re-verify each SHA individually, just read + doc'd the current tree state). CI gates linux (build+test, FIRST-EVER Linux run, `cargo test` allow-failure pending a real-vs-environmental triage TODO left inline) / web (wasm build+smoke+trunk) / mac (build+test, one named test skipped for a GH-runner font-fallback fixture gap). Release pipeline: tag `v*` push (or manual dry run) → mac universal `.app`/`.dmg` (signed+notarized iff all 5 Apple secrets set) + linux tar.gz + web zip → one GitHub Release. Fly site (`site/`) deploys via `workflow_dispatch` only, gated on `FLY_API_TOKEN`, builds a FRESH wasm bundle every deploy (never commits blobs — `site/editor/`'s checked-in bundle is legacy). CLAUDE.md gained a new "The fly site, CI, and release pipeline" section (this pass) + one banked sentence in the Supply-chain section on a future user-invoked "Check for Updates" command.

## 🔜 STILL ARMED (added this pass)

- **#14 Keymap defaults as DATA (user-proposed 2026-07-11: "everything unbound by default and specified by the data file")** — the keys sibling of the capabilities-as-data refactor, and the merge-don't-align endgame for bindings: today defaults live in TWO places (commands.rs native/emacs slots + keymap.rs static dispatch arms), aligned by law tests; this round makes them ONE embedded data file. Shape: an `include_str!`'d defaults TOML in the EXISTING `[keys]` grammar (command slug → up to 2 chords), plus built-in keep-list defaults (the C-k line) — `KeymapState` builds its maps from (embedded defaults ∪ user `[keys]`), catalog slots/labels/GUIDE derive from the same parse. STAYS CODE (policy, not assignment): plain-arrow motions (uncataloged by design), modal/overlay intercepts, the P0 swallow guard, Cmd→Ctrl convention translation, prefix machinery, webreserved tiers. COMPAT GUARD: the file stays EMBEDDED (internal data model — no new on-disk user format, no freeze; `[keys]` remains the only user-facing contract; a future "keymap packs" surface inherits an already-battle-tested grammar). Law tests: file parses, every slug names a real command, ≤2 chords, per-convention pairwise conflict sweep runs over the DATA, swallow-guard sweep still holds. SEQUENCING: collides with every keymap-adjacent branch — fires ONLY after train-5 lands. The narrow C-k fix (agent cooking on the links branch now) still lands first; it collapses to one data line when this round arrives.

- **#12 Check for Updates ✅ BUILT @ `7d0b865`** on `worktree-wf_1fa28698-49d-1` (workflow `wylnz2rgp`, verify clean round 1: 1912 green BOTH conventions, wasm 0 warnings, websmoke L1+L2 10/10). The browser-handoff design as approved: `src/updates.rs` one owner (check_url + atomic last-checked marker + checked_line), "Check for Updates" palette command (native_only, no chord), rides the EXISTING follow_link seam — the app itself stays network-code-free; `site/check.html`+`check.js` do the comparing against `version.json` (written by deploy-web.yml from `git describe --tags`, gitignored, never committed). About card gains a faint "checked … ago" line (placeholder in captures; sidecar `about.checked`, schema 165→166). ROADMAP moved it out of "Deliberately waiting". Headless replay proven a total no-op (no marker write, no browser). JOINS TRAIN-5. Taste flags: About-card line placement/spacing; check.html copy (docs-voice, matter-of-fact). Pre-existing note from verify: 3 test-profile-only wasm lints (buffer/tests import, menu_bar_on, parse_log) — audit-day fodder. LIVE-ONLY: the real browser opening on a physical machine + the deployed check page once a real tag exists.
  - (original spec, for reference) user-approved "maybe" → approved-in-principle.

## 🛡️ DISTINGUISHABILITY LAW ROUND (user 2026-07-13: "how do we stop this from happening in the future?") — 🟢 QUEUED, fires AFTER the row-highlight fix merges

The 6/6-invisible-picker anatomy → structural cures, all in the codebase's own idiom: (1) OUTCOME LAW SWEEP `interactive_states_are_visible_in_every_world` — no-wildcard roster of stateful surfaces (picker selected row, menubar open-title, search match, selection band, caret-vs-ground) × all 15 worlds, perceptible-difference floors (redmean/pixel), new surface fails to compile till it joins, new world swept free; (2) TYPE-LEVEL: row-highlight decision returns enum with NO absent variant (ValueBand(color) | Invert | …) — "no indicator" unrepresentable (the transparent-[0,0,0,0]-as-policy ban); (3) CLAUDE.md review rule: a degradation needs its compensating mechanism NAMED + LAW-TESTED (the false "caret still marks the position" comment class); (4) THEMES.md add-a-world gallery checklist grows SUMMONED-SURFACE shots (palette open + moved selection, search active, selection+caret) — Wagtail's gallery never contained an open picker. Also fold: the audit's fixed-caveat that identity-gated refactors preserve pre-existing bugs (note in the refactor discipline). PLUS the user's harness-doctrine diagnosis ("it's a harness problem right?" — half right, the sharp half): (5) CAPTURE.md doctrine amendment — the sidecar is a STATE oracle, not an APPEARANCE oracle; "prefer the sidecar over the PNG" steered verifiers away from the one check that catches invisibility bugs; appearance-class properties MUST assert pixels; (6) a reusable A/B PIXEL-DIFF TEST HELPER (capture two states, assert perceptible difference in a region, one line) — make the right assertion the cheap assertion, so instance_count-style mechanism checks stop being the path of least resistance. Depends on: one-bit-row-highlight fix landing first (law would correctly fail today).

## 🎭 OVERLAY PERSONALITY AS DATA (user 2026-07-13, P3R CONFIG screenshot as reference) — 🟢 APPROVED-IN-PRINCIPLE, fires after row-highlight + distinguishability land (same overlay-chrome files)

User wants: (a) card BORDERS available to more worlds than Wagtail (already just elevation:Bordered — one-line world-data edits + taste check; the roadmap's "ruled-border cards on light worlds" experiment); (b) per-world palette LOOK divergence — the Persona-3-Reload placard move: overlay title as a HUGE ghosted corner-anchored wordmark (their words: "the theme system should be able to put the labels in some positions (eg top left/bottom right)... maximalist themes as well as minimalist ones"). SHAPE: overlay_style capability block on Theme — title: InlinePrefix | Placard{corner TL/TR/BL/BR, scale, ink Ghost/Faint} + card_border — ONE renderer, per-world data, all worlds default to today's look (byte-identity gate). LAWS: placard rides the world's own ink ladder (never amber, never fights content), rowlayout untouched, deterministic, the distinguishability sweep polices selected-row findability under any placard. PROCESS: probe gallery first (lava-round pattern) — placard corners/scales/inks on 4-6 candidate worlds (Galah maximal BL wordmark; Wagtail stark 1-bit type; Saltpan stays InlinePrefix minimal) for the user's eyeball; ship only what wins. Maximal/minimal becomes WORLD IDENTITY as data.

## ✅ THE 2026-07-13 WAVE — ALL LANDED (main @ `8a3dac5`, 2028+2 both conventions, 16 ahead of origin, held)

Jitter ramp `6d7163a` → dedupe `04d256f` → row inversion `695a224` (vision-smoke's first run: all 5 shots answered) → live-resize `f4c554e` (whole-pixel snap PROVEN via AA-phase bytes 4.4%→0% + presentsWithTransaction armed-during-drag, settle 150ms) → **distinguishability law `8a3dac5`** (pixel-diff helper, floors tuned by real measurement 40→12; two-tier sweep +1.4s runtime; HighlightTreatment no-absent-variant enum; CAPTURE.md 4th live-only class + state-vs-appearance oracle; THEMES.md step-8 summoned-surface gallery; verifier SABOTAGED it twice — original bug + 3%-alpha — both caught loudly). USER LIVE CHECKS OPEN: slow-drag no-boil + fast-drag no-stretch (RESIZE_SYNC_SETTLE the knob), inverted-row taste, § marker + titles taste.

## 🎭 OVERLAY PERSONALITY — ✅ LANDED (machinery `0e9e096` + facets fix `3891d8b`; main @ 3891d8b, 2043 both conventions, ~20 ahead of origin, held)

Machinery byte-identical by default (all 15 worlds InlinePrefix; 12/12 identity cmp) — TitleStyle{InlinePrefix|Placard{corner,scale,ink}} on RenderCaps, ink structurally ladder-only (Ghost=faint.lerp(base_300,.5)), placard clipped-to-card drawn behind rows, AWL_OVERLAY_STYLE_FORCE probe override, 1-bit-Ghost guard law. VISION-SMOKE CAUGHT ROUND-BUG #2: placards never rendered on FACETED pickers (palette/Settings — geom.theme means "has lens strip", 7 kinds; 8 unit tests all fixture-blind to lens strips); lldb-root-caused, guard fixed (theme picker included per merge-don't-align), regression tests added, gallery regenerated. WAGTAIL FINDING: flat-ink placard is 1-bit-legal but useless (one ink = no recede, reads as clutter) — future stipple-textured wordmark or none. GALLERY ON THE ARTIFACT: Galah×3 (BL/3.0/ghost = the reference look) + Kingfisher×2 + Mopoke + Saltpan control. DECIDED 2026-07-15 (user delegated): placards Galah/Magpie/Mangrove/Firetail BL, bleed-not-clip, Wagtail none — see the personality-thread entry; assignment round cooking.

## (superseded) THE 2026-07-13 LIVE-BUG WAVE — status

- **RESIZE JITTER ✅ BUILT @ `52659e4`** (verify clean r1: 2007/2009 both conventions; independent 36-combo sweep, worst step 5.16px vs pre-fix 46px snap at w=1130→1131). Root cause NOT the gutter width (provably constant) — the no-payoff guard's binary threshold snapped the column 46px on 1px of resize; fixed with a 16px entry ramp (RIGHT_MARGIN_BREATH reused). 🟡 MERGE AGENT RUNNING. LIVE: drag feel + ramp-width taste (16px un-tuned).
- **PALETTE DEDUPE ✅ BUILT @ `983c5f0`** (verify clean r1: 2012/2014 both conventions, websmoke 16/16). TEN pairs covered (Theme/Caret/Dictionary/Keybindings pickers + 6 toggle twins); Zoom deliberately excluded (different affordance — typed-value vs stepwise, documented); web-hidden edge future-proofed generically (covered row reappears if its command hides; law-tested via stand-in). Settings menu untouched. AWAITING MERGE behind jitter.
- **1-BIT ROW HIGHLIGHT 🟡 COOKING (`wml8xigbf`)** — cross-check against the audit's list on landing (menubar_hi second site at render.rs:3080, theme-picker preview-caps nuance, color-assertion tests, the false doc comment at theme/model.rs:134).
- **DISTINGUISHABILITY LAW ROUND 🟢 QUEUED** behind the row-highlight merge (details above).

## (original) RESIZE JITTER (user live report 2026-07-13) — details in the round entry above

"jitter when resizing horizontally, even when the center panel is not supposed to be moving (right gutter collapsing) — are we not locking the left gutter width?" Diagnose-first round on adaptive_column_left: 1px-at-a-time width sweeps (shrink+grow, all 3 regimes, multiple measures/scales) asserting the stability contract — pinned-regime constancy, no oscillation, constant measure px, path-independence. Suspects in order: symmetric-term odd/even rounding parity leaking through the .max() floor · regime flip-flop at unrounded thresholds · measure_px float wobble · outline rail-want flicker. Fix at the one owner only; WIDE-regime byte-identity guarded; hysteresis allowed only if documented + sweep-covered. LIVE-ONLY residue: the real drag feel on a real window.

## 🧹 CLEANUP PASS (2026-07-12 evening) — ✅ COMPLETE (janitor + chores + equivalence audit + deletions)

EQUIVALENCE AUDIT VERDICTS (evidence-grade: SHA-identical fonts, verbatim commits/tests on main): jp-faces, jp-typo (+its orphaned unreferenced font debris), JP-world-faces draft (superseded 5-font design — Zen KAKU/BIZUD/MPLUS never shipped; Zen MARU won), CJK-companions, Asset-Cleaner, web-srgb (+legacy-bundle churn), web-deploy scratch — ALL confirmed duplicates of landed work → DELETED (6 worktrees+branches -D'd + 1 detached worktree). RESCUE ✅ LANDED @ `a1049e0` (2004+2 both conventions; live --bench-frame smoke witnessed): spell/syntax verifier_probe modules reimplemented vs current main (comment misclassification measured ~1.0% vs 5% ceiling; snake_case-half spell residue = known documented class) + framebench wash stage — AND a REAL PRE-EXISTING BUG fixed: --bench-frame panicked on unmodified main (table-grid stage mark had no STAGE_NAMES entry, 22 names vs 23 marks; never caught — manual CLI flag outside cargo test); STAGE_NAMES now 24 + run_one_frame extraction + witness unit test pins the class. Local `verify` branch deleted (origin/verify retains the historical copy). END STATE: worktrees = main + lava-probe; branches = main + lava-probe; suite 2004+2; FIVE commits held unpushed (e642ac8, 987137b, 91df6c1, 2b8285e, a1049e0) riding the next push window. USER ITEM UNCHANGED: `git push origin --delete master` (541 behind, 0 ahead, full ancestor — one word).

CHORES DONE (3 commits on main, held unpushed with the batch: `e642ac8` warnings — menu_bar_on got its MISSING leniency test written instead of deletion, unused import + orphaned entry_aged removed, test-profile now 0 warnings, suite 1998; `987137b` CLAUDE.md branch-note truth — origin default IS main, local origin/HEAD symref was the stale bit (orchestrator ran `git remote set-head origin -a`, now → main); `91df6c1` audit day — NO new findings, 3 carried-forward advisories re-verified via dry-runs (quick-xml ×2 winit-capped, ttf-parser unmaintained), crates 326→340 organic, no bumps, no license regen, ledger appended).

JANITOR DONE: 21 stale worktrees + branches removed (all passed merged-safety), worktree prune clean, /private/tmp/awl_base deregistered; + 47 worktree-less merged local branches swept by orchestrator (git -d guard). KEPT (unmerged, need a CONTENT-EQUIVALENCE AUDIT — likely stale duplicates of rounds re-landed via other commits, but not provably): jp-typography-faces, jp-typography-fix (DIRTY — untracked Klee One/M PLUS 1 Code/Shippori Mincho font assets, probably now-duplicate of landed assets/fonts/), wf_02196eb4-bb8-2 (JP world-faces), wf_bccd045a-b93-1 (CJK companions — CLAUDE.md documents this round as LANDED), wf_fa4f8135-be7-1 (Asset Cleaner), wf_082b96bb-b8b-1 + jobs/tmp/web-deploy (both dirty ONLY with legacy site/editor bundle churn — disposable-looking, user's judgment), loose `verify` branch, lava-probe (KEEP, active). ORIGIN/MASTER: 541 behind, 0 ahead, full ancestor — SAFE TO DELETE on the user's word (`git push origin --delete master`). FOLLOW-UP QUEUED: equivalence-audit agent for the 6 unmerged strays → then delete-or-rescue each.

## 🚂 BATCH-2 LANDED + PUSHED (2026-07-12 evening) — main @ `4ec774a`, origin current, suite 1995+2 both conventions

All four of the day's second-wave rounds merged suite-gated and PUSHED (`4ff0b6e..4ec774a`): theme-caps `7450fe9` → keymap-defaults `e2e7b53` → table-overflow fix `0641c55` → palette-§/titles `4ec774a` (only real conflict: CLAUDE.md keep-both union; commands.rs untouched by the branch — union rode overlay/build). CI GREEN on 4ec774a (run 29176634886: linux 15m39s blocking gate ✓, mac 9m10s, web 2m38s — exactly 119 billed min → July ~1,955/2,000; PUSHES HELD this month unless repo flips public). Site REDEPLOYED locally (zero GH minutes): bundle `awl-e5df0e15c5750b62` live, /check.html + version.json 200, machine v9 good. Pre-existing test-profile warnings now 3 (buffer/tests import, menu_bar_on, history entry_aged) — audit-day chore. IN FLIGHT: margins-only lava probe v2 (warm+dithered, hard-edge vs edge-glow, page-off wrinkle shot). (original cooking list below)

## (original) FOUR ROUNDS COOKING (2026-07-12, all worktree-isolated off `4ff0b6e`)

1. `wu0f2ffob` table-overflow fix (below); 2. `w4r5ergkg` **theme capabilities as data** (is_one_bit branches → declarative Theme fields; byte-identity gate over all 15 worlds; grep-law bans render-side is_one_bit); 3. ✅ **keymap defaults as data BUILT @ `0f5b203`** (verify clean r1: 1987 both conventions, wasm 0 warnings, websmoke 16/16; catalog snapshot byte-identical vs base). `assets/keymap-defaults.toml` (78 commands + linux_builtin_keep=["C-k"]) → LazyLock parse → COMMANDS spliced from it; labels/GUIDE/rebind/conflict-sweep/keep-floor all derive. HONEST SCOPE TRIM (agent-disclosed): keymap.rs's keypress DISPATCH arms stay hand-written — the pre-existing catalog↔keymap agreement law is deliberately KEPT as a real cross-check (a TOML edit that dispatch doesn't match fails tests loudly). FOLLOW-UP QUEUED (#15): data-driven dispatch — seed KeymapState's override maps from the same parse, retire the static default arms; a real input-core rewrite, own round. AWAITING MERGE behind theme-caps. 4. `wqcq2b0l9` (run wf_ce81544e-083, twice re-specced mid-flight on user input) **palette-settings union + overlay titles** — (a) settings rows searchable in Cmd-P, one dispatch owner with the Settings menu; (b) marker from AWL'S OWN symbol set first (user: "we have the awl symbols... our own set") — ornament.rs vocabulary + § measured for coverage across every embedded face (web has no fallback), world-INVARIANT glyph, gear only if it wins, dim "Settings" tag floor, secondary column carries current value; (c) EVERY picker names itself (user: "showing the current menu... makes it less confusing") — overlay_title(kind) no-wildcard law, quiet "<title> › " prefix on the input line, sidecar overlay.title, minibuffer prompts excluded (already self-naming). One combined schema bump. MERGE-TRAIN-6 NOTE: palette-union + key-defaults both touch commands.rs/overlay (union expected); theme-caps + table-fix share render.rs adjacency. USER LIVE ITEM: keymap-setting dead row was fixed in 70dbc13/2f06bd7 — user likely on the pre-fix tarball; told to grab 29163018019 or hard-refresh web; if still dead on fresh build, escalate to a live round.

## 🌋 LAVA LAMP WORLD (user 2026-07-12: "omg we need to add a lava lamp theme!!!") — 🟢 APPROVED, sequenced AFTER theme-caps lands

Ambient slow-moving background (metaballs, lava-lamp register) as a new STATEMENT world — the mirror of Wagtail: the one world whose warm thing is the GROUND itself. Ships as a capability field (background: Static | Lava{...}) on the theme-caps machinery, never a code path. LAWS TO NEGOTIATE (logged DESIGN.md amendment, WYSIWYG-pivot style): 0%-idle law → slow internal cadence (~8-12 fps, ~100ms WaitUntil, pause on blur, setting-gated); Reduce Motion → frozen frame; determinism → captures render a fixed phase (t=0); figure/ground → blobs stay in the ground value band, law-tested at WORST phase. STEP 1 ✅ PROBE DONE (branch `lava-probe` @ `1184cb9`, disposable, never merges): 27 renders + 3 contact sheets in the worktree's gallery/lava-probe/, pushed into the phone artifact. Warm (Potoroo-hosted umber/wine, 37° clear of caret amber) = agent pick; dithered (Bayer posterize) = strong second, arguably its own world; legibility clears the contrast floor 12–16× everywhere (one logged caveat: blob_hi overshoots base_300 by ~1.4–3 L-points — strict-ceiling vs tasteful-stretch is a real-round decision). USER PICKED (2026-07-12): MARGINS-ONLY by default ("can we try it in the gutter by default?") + loves dithered too. PROBE v2 ✅ DONE @ `263202d` (same branch): two thin flanking lamps, hard vs EDGE-GLOW (agent pick — capped light-spill under the column edge; riser blobs make it honest), gallery/lava-probe-margins/ (41 files) + artifact updated. FINDINGS: (1) INVERSION — modest measure-70 margin reads BETTER than wide measure-40 (fixed-UV blobs strand in wide margins → real round must size lamp margin-RELATIVE); (2) page-off = no lava (agent rec: auto-page-on for this world); (3) outline rail collides with left lamp on headed docs — coexistence call needed. STILL AWAITING USER: both palettes as two worlds (Firetail + Cuttlefish?) or one · glow vs hard · auto-page-on. REAL-ROUND SKETCH BANKED in the probe report: Background::Metaballs variant in the existing tagged union (not a second pipeline), WaitUntil cadence, Reduce-Motion freeze at mid phase, capture t=0 stillness + sidecar background block, blur-interplay decision. Name candidates: Firetail / Cuttlefish. General "moving backgrounds" capability for other worlds stays NOT-v1 — one statement world first.

## 🐛 TABLE OVERFLOW ON SHRINK — ✅ BUILT @ `1a4a695` (verify clean r1: 1978 both conventions, wasm 0 warnings)

ROOT CAUSE (the codebase's own live-only class (b), confirmed failing-test-first): TextPipeline::set_size re-wraps directly, so sync_wrap_width's drift branch — the ONLY caller of resync_table_layout_for_width — never fires on a live resize; TableGridCache served old-width column geometry into the new canvas (pre-fix: col x=[86, 823] into a 500px window). FIX: set_size calls resync_table_layout_for_width under the same width-changed gate row_geom already uses — one wiring line, one owner, no parallel math. STALE-PAN SIBLING: investigated, proven a NON-BUG (min-content floor is avail-independent; pan clamp recomputed fresh per frame) — agent deleted its own non-discriminating test rather than keep a misleading guard, reasoning recorded in the doc comment. Gallery/table-overflow/ has a README explaining why captures can't show a resize sequence (harness structurally immune) — the unit test is the proof. AWAITING MERGE behind theme-caps + key-defaults. (original spec below)

## (original) TABLE OVERFLOW ON SHRINK (user live report 2026-07-12) — 🟡 COOKING (`wu0f2ffob`)

"tables with long text can overflow if you resize the page smaller" — grid draws past the writing column after a shrink. Suspected class: CLAUDE.md's live-only class (b), missing resize invalidation (TableGridCache key/invalidations not covering the wrap-width change; capture harness structurally immune, so tests must drive sequential set_size directly). Fix pinned to the one geometry owner (compute_table_layout/TableGridCache), pan stays the reach model, stale-pan-offset sibling case covered, fit-tables byte-identical. Worktree build+adversarial verify, repair loop 2. MERGE on green (single-item train), rides the next push batch.

## 🚂 TRAIN-4 LANDED (2026-07-12) — main @ `b7a218c`, 1904+2 green, UNPUSHED (batching with train-5)

All five merged + suite-gated in order: page-reset `a3b74a0` (pre-landed, gate confirmed 1901) → caret rounded silhouette `d0b50aa` (1903) → 1-bit card borders `206e0e2` (1904, ALSO linux-forced suite + wasm 0 warnings — the train's render-family gate) → web loading screen `04fe28e` (1904) → credits ledger `b7a218c` (amended message; 1904+2 fault_kill9, 0 flakes). All five worktrees janitored, branches deleted, tree clean. My Windows-roadmap docs commit `336bf16` sits between merges (cherry-landed on main after a wrong-branch mishap — stray commit stripped from the web-escape branch, which is back at `ad056a6`).

## 🚂 TRAIN-5 LANDED (2026-07-12) — main @ `1e0a8e8`, all EIGHT merged, UNPUSHED (31 ahead; push after the guide round)

Workflow `w9imploo3`, zero product collisions: menubar `f6bdc14` (1915) → vert-selection `2b7d8a9` (1915) → caret-state `4844a71` (1921) → web-config `65c8e6b` (1923) → docs-tokens `e45ff65` (1934, keytoken.rs landed) → web-escape `7093a7e` (1938 — TWO notable saves: Keybindings native_only conflict correctly resolved to FALSE (worktree predated web-config un-hiding it; agent initially took true, law tests caught it, corrected with tracing) + a git-stash-dropped-MERGE_HEAD hiccup caught at janitor and rebuilt as a proper 2-parent merge via commit-tree) → links+C-k `619b350`+`bcafc40` (1966 BOTH conventions; one exhaustive-match fix: OverlayKind::InsertLink into the one-bit CenteredPanel family) → check-updates `1e0a8e8` (final full battery). All worktrees janitored, branches deleted. Pre-existing test-cfg unused-import warning (buffer/tests.rs:1244) noted again — audit day.

## (original manifest, for reference)

1. menubar sliver + debug-above-bar `5a522b3`; 2. vertical-selection coverage `2ab3b54`; 3. caret statelessness `5e4a184`; 4. web-config persistence `727342e`; 5. docs-tokens/web-alternates `b1dc531`; 6. web-escape `ad056a6` (ROADMAP union possible); 7. links + C-k rework (base `6c33989`, rework commit pending — agent nudged off an idle-wait 2026-07-12); 8. check-for-updates `7d0b865` (ROADMAP/CLAUDE.md union likely with web-escape + links branches; GUIDE.md regenerated on THREE branches — links, check-updates, C-k rework — expect the generated table to conflict; resolution = re-run the generator on the merged catalog, never hand-merge the table).

THEN (on the settled train-5 base): guide/welcome/tour de-fluff + "awl in the browser" web-differences section (5MB ≈ 8–10 novels, copy-out/paste-in asymmetry, Download file, per-profile prefs, desktop pitch) → ONE batched push (policy) → fresh release dry-run → fly redeploy → artifact refresh. Then #14 keymap-defaults-as-data + capabilities-as-data. Needs the `version.json` static endpoint from the deploy round (host it on the fly site, e.g. `site/version.json`) + a logged PHILOSOPHY.md amendment (the zero-AMBIENT-network law stays intact — this is explicit, user-invoked, never on launch/idle/timer). Cook AFTER the first real tagged release exists (so there's a version to compare against and a release cadence to point users at).
- **#13 Mac release execution** — waiting on three user inputs before a real `v*` tag can be cut for public consumption: (1) the user's app icon (`assets/macos/Awl.icns` — `scripts/package-macos.sh` wires it in automatically once present, see its "ICON:" TODO comments), (2) the LICENSE gap resolution (`RELEASING.md` §4 — Hunspell dict license notices + a code copyright/NOTICE file, both still missing), (3) Apple + Fly secrets (`RELEASING.md` §1–2 — `MACOS_CERT_P12`/`_PASSWORD`, `APPLE_API_KEY_ID`/`_ISSUER`/`_B64`, `FLY_API_TOKEN`). A dry run (`gh workflow run release.yml -f dry_run=true`) is fully exercisable today with zero of these — only a REAL public tag needs them.

## 🔥 CURRENT STATE (2026-07-10 late night — supersedes the stale in-flight notes below)

**Pushed through `f9f5c2d`** (user authorized: "push it" for the night's 22 commits @ `098c5ad`, then infra push authorization — verified scope-clean: workflows/RELEASING/package script/CLAUDE.md only, zero src/). **CI IS LIVE AND GREEN** on origin (run 29073208563): linux (FULL SUITE PASSED FIRST TRY — the "never run on Linux" fear didn't materialize; allow-failure scaffolding retained unused), web (smoke L1+L2 + trunk), mac (one test skipped by name — see follow-ups). Release pipeline validated locally for real (working Awl.app + 28MB DMG produced; actionlint clean); deploy-web.yml validated by assembling the real site structure.

**🚂 MERGE TRAIN #2 LANDED** (workflow `wut1g1sz6` + finisher after a mid-suite harness interrupt on the MAS merge): a11y `498c83c` → crash `97f1456` → credits `5584f4b` → MAS `5ffa7f3` (package-macos.sh unified: one script, default + --mas modes; real assembly run verified) → test fix `5ed9bad`. FINAL: default suite **1769 green**, mas suite **1767** (delta = daemon tests out, mas tests in — exactly accounted), wasm **0 warnings**, websmoke **L2 10/10**, palette **68 commands** (About/Credits/Report a Problem all present), 4 worktrees janitored, tree clean. **9 commits ahead of origin, UNPUSHED** — pushing also lets CI prove the un-skipped outline test on the runners. TEST-FIX NUANCE (honest): `Wrap::None` proved redundant belt-and-braces — `outline_pixel_fit`'s measured-pixel truncation is the real invariant holder; disabling the whole correction reproduces the original 5-runs bug; the test now asserts the law measure-relatively (font-stack-independent).

**✅ LICENSE + CREDITS BUILT** @ `011938f` on `worktree-wf_fc9aed4d-984-1`. SURPRISE: LICENSE/NOTICE/fonts-LICENSES.md already existed (GPL-3.0-ONLY was the settled prior choice — agent preserved it, documented flippability; flag: user earlier heard "-or-later" from orchestrator — repo reality is -only, one word to change). NEW: assets/dict/LICENSES.md (en_GB LGPL 2.1 verified in-file; en_US/en_AU FLAGGED no in-file license — open gap, low practical risk, not inferred away); THIRD-PARTY-LICENSES.md generated via cargo-about (~300 crates, all GPL-compatible, MPL/dual-licensed resolved); CREDITS.md (warm voice) + src/credits.rs include_str + "Credits" palette command + About card "⌘P → Credits" faint line (wording taste-flagged); site/credits.html (hand-duplicates CREDITS.md — drift risk flagged); artifacts carry all four docs (.app Resources, DMG, tarball, zip). RELEASING.md license-gap → RESOLVED.

**✅ MAS SANDBOX BUILT** @ `4e154fb` on `worktree-wf_7ba013b3-975-1` (default build byte-identical — 1737 green default / 1735 with `--features mas`, the delta exactly the compiled-out daemon tests; 0 warnings all targets). Container finding: sandbox redirects $HOME → data/config land in-container with ZERO code (law-tested). Landed: `mas` feature (daemon compiled out), src/mas.rs GrantStore (security-scoped bookmarks, hand-rolled TOML+hex, session.rs idiom), NSOpenPanel powerbox via in-tree objc2 (rfd investigated, not added), fence at load_path/set_root (cancelled panel aborts a switch), restore_all_grants on live startup only, packaging/mas/entitlements.plist (sandbox + user-selected + bookmarks, NO network). HONEST GAPS flagged: CLI launch-arg path bypasses the fence (no MAS CLI — fine, stated); bare-GUI-launch cwd fallback unaudited; panel feel/2-launch persistence/kernel-accepts-entitlements = live-only. ⚠️ MERGE-TRAIN NOTE: branch was cut mid-infra and built its OWN scripts/package-macos.sh — CONFLICTS with the infra round's pushed one; reconcile = keep infra's, fold the --mas mode in.

**✅ A11Y TIER 1 BUILT** @ `406435b` on `worktree-wf_432e66ad-076-1` (suite 1753 green, 0 warnings both targets, captures byte-identical; verify clean round 1). `src/motion.rs` one-owner `reduced()` — config `reduce_motion` (absent = auto) beats OS; mac = NSWorkspace accessibilityDisplayShouldReduceMotion, web = prefers-reduced-motion, linux auto = off (config is the door). THREE gated steppers (the complete advance() fold): caret spring/flinch/streak, copy pulse, caret-preview loop — each settles in ONE step, same end states. STRICTER-than-sticky determinism: apply_at_startup only on App::new, never the capture seam (two tripwires). Settings row under Editor. BONUS FIX: initial window title unified with update_title into one pure fn (pre-GPU title lacked the theme suffix). ACCESSIBILITY.md states the VoiceOver gap plainly; AccessKit named/banked. LIVE-TASTE: motion-off feel, real OS toggle, settings row look.

**✅ CRASH VISIBILITY BUILT** @ `867f446` on `worktree-wf_44ae24af-e78-1` (suite 1752 green, 0 warnings both targets; verify clean round 1). `src/crashlog.rs`: live-app-only panic hook → plain-text log under data_root/crashes (version/platform/location/backtrace, prune to newest 20, raw std::fs mid-panic by design); "Report a Problem" palette command (mailto via the follow-link seam — names the newest log's PATH, never content; MAINTAINER_EMAIL one const = git author address, user-changeable); quiet once-per-crash next-launch notice (wording taste-flagged). PRIVACY LAW proven two ways (type-level + 22 REAL induced subprocess crashes, sentinel absent from every log); headless capture structurally can't install the hook (tripwire + subprocess-proven). Verifier's temp induce-flag fully reverted + re-verified.

**PUSHED (user: "ok push please"):** `f9f5c2d..5ed9bad` — train 2's 9 commits on origin. CI run 29081511002 on it: overall SUCCESS; **the un-skipped outline test RAN AND PASSED on the real macos runner** (and linux) — fix proven where it failed. ⚠️ BUT the linux job's allow-failure gate was HIDING a real exit-101: 4 tests fail on the linux runner (pickers_nav lens cycle C-f/C-b, catalog↔keymap agreement law, peek starter card, stats discoverability) — all chord-shaped; read = Convention::current()=Linux on the runner serves the Ctrl table to tests written mac-blind; the picker one may be a REAL Linux product bug (in-overlay C-f/C-b lens chords displaced by native-wins). → **✅ LINUX-CONVENTION GAPS MERGED** @ `2bf058e` (independent verification complete: both suites 1787 green, the 4 CI-failing tests green by name under forced-linux, builds 0 warnings, ci.yml gate confirmed blocking; both round worktrees janitored with containment-proof on the cherry-picked sibling). Main **5 ahead of origin, UNPUSHED**. (Original build detail @ `d986e07`, workflow `wc5ahfl3m`:) VERDICT: all 4 = CONVENTION-BLIND TESTS, no product bug — the picker C-f/C-b displacement on Linux is BY DESIGN (collision table law; arrows cover lens-cycling on every convention; labels truthful per #10). Law strengthened: catalog↔keymap agreement asserted PER CONVENTION over both tables; ~80 further mac-literal test sites swept via convention-pinning test shadows; fixer ran BOTH full suites green (1787 ambient / 1787 forced-linux). ci.yml linux allow-failure gate REMOVED — linux failures block from now on. Janitor note: sibling worktree wf_868f9d53-ee3-1 (round 1, cherry-picked) needs cleanup too.

**✅ DATA-SAFETY MERGED** @ `7b7a547` (zero conflicts; 1787 default / 1785 mas green + kill-9 harness 2/2; wasm 0 warnings; worktree janitored). Main ahead of origin by 2, unpushed.

**✅ DATA-SAFETY HARDENING BUILT** @ `e9d8b7d` (workflow `wqvw15rk8`; merge agent running + checking CI on the 5ed9bad push). **HEADLINE — REAL DATA-LOSS BUG FIXED:** history/store.rs `write_log` was a BARE fs.write — kill-9 mid-write tears the snapshot log, the lenient parser accepts the truncated remainder, the NEXT flush overwrites with the shortened state = every snapshot after the tear permanently destroyed. Now atomic + trust-tracking parse (`parse_log_checked`) preserving raw bytes to a `.corrupt` sibling first. Also routed: config write_default/write_binding/write_pref/remove_pref, crashlog acknowledge, paste-image PNG, credits refresh (defense-in-depth). New src/durable.rs one-owner recovery seam (capped .corrupt siblings, monotonic seq — its own ms-collision bug caught by its own tests); session/stats/recents/mas load through it; scratch stash preserves-on-invalid-UTF8; config EXEMPT (typo ≠ corruption, documented). tests/fault_kill9.rs witnesses never-torn via real SIGKILL subprocesses; audit law test blocks future bare writes. Original round spec fully covered (workflow `wqvw15rk8` build+verify clean). (1) atomic-write AUDIT — every durable write site routed through fs::write_atomic (crashlog's mid-panic writer exempt by design) + a structural law test against bare fs::write; (2) PRESERVE-ON-CORRUPT recovery — a parse-failed store (session/history/scratch/grants) copies the original to a capped .corrupt-<ts> sibling BEFORE the lenient default (config exempt: typos ≠ corruption, keep-prior+notice stands); scratch stash = a manuscript, special care; (3) kill-9 FAULT HARNESS — subprocess SIGKILL mid-write-burst, re-read asserts old-or-new-never-torn (witnessed, not assumed). Format freeze stays a release-gate doc item.

**NEW FOLLOW-UPS from CI:** (a) `outline_hit_test_stays_aligned_past_a_wide_glyph_heading` skipped by name on the mac runner — fixture assumes the dev Mac's ⌘-glyph fallback width; needs a src/ fix (stable-width glyph or measure-relative assertion); (b) periodic glance at the linux job in case a real failure ever hides behind its allow-failure gate.

**GO-PUBLIC STATUS (2026-07-10 late):** SECRETS SCAN CLEAN — 0 secrets across all 504 commits + tree (gitleaks + sweeps; stray branches individually checked); personal info = deliberate authorship only; clone ≈ 50MB; .gitignore hygiene confirmed. SAFE TO FLIP PUBLIC — remaining user items: hand-written README rewrite (facts checklist provided; a README.md already exists), then the Settings→visibility click. CI green with BLOCKING linux gate on 2bf058e (run 29091247366). Web demo DEPLOYED fresh @ awl-editor.fly.dev/editor (bundle awl-e2742106, tonight's tip — Ctrl-P live on Linux browsers). DEFAULT BRANCH FLIPPED master→main (was blocking workflow_dispatch — GitHub only dispatches workflows on the default branch; also right for going public; CLAUDE.md's origin/HEAD-is-master note now stale — fix at next doc sync). Release dry-run 29092027019 in progress (watch agent on it). NEW BANKED: wasm path-string strip (`--remap-path-prefix` + strip-debug in the web build — /Users/frank panic-paths ship in the live bundle; fold into the web-diet round).

**🚂 MERGE TRAIN #3 LANDED (2026-07-11):** `0365fce` clipboard → `bfea42d` adaptive-column → `eef83c5` save-feedback → `b21fd0d` emacs-hands (+ `2e26dc6` honest println-audit recount — the round's 2 new keymap.rs config diagnostics). Tip `2e26dc6`: **1833 green BOTH conventions** (identical counts), mas 1831, wasm 0 warnings (fresh-rebuild-verified), websmoke 10/10, palette 72, save-conversion smoke end-to-end ("hello" + Cmd-S → notes_root/hello.md — content-derived naming confirmed). All 4 worktrees janitored; ~24 pre-existing stragglers untouched. UNPUSHED pending flavor round. Minor flag: 2 pre-existing test-profile wasm lints (buffer/tests import, history parse_log dead_code) — audit-day fodder. **KEYMAP FLAVOR 🟡 BUILDING** (`w8qwd8x1e`, worktree on the fresh base): preset derived FROM the displaced table (no-drift law), Settings toggle row (live re-apply + sticky; web = session-only, documented), [keys] carve-outs compose, mac-inert law, Omarchy recipe in the template. THEN: push (user word) → fresh dry-run → the Linux-wave tarball.

**🚢 LINUX-WAVE DELIVERED (2026-07-11):** flavor merged `b8127bd` (1851 green both conventions) → pushed `2bf058e..b8127bd` (11 commits) → CI ALL GREEN on tip (blocking linux gate 16m29s ✓, run 29111393685) → dry-run 29112397859: **linux tarball + web zip GREEN, downloadable** (`gh run download 29112397859 -n awl-linux`). ⚠️ mac job failed ENVIRONMENTALLY: `hdiutil create: No space left on device` — 4/4 attempts (warm+cold cache), same image passed this morning, zero packaging files in the pushed range; NOT a regression. BANKED follow-up: re-dispatch the dry-run later + harden package-macos.sh (stage dmg on the workspace volume, print df -h before hdiutil). NEXT UP when the user's ready: verbs round (Rename/Duplicate/HUD-saved) → user-guide round, both on the settled base; user's live Linux verdict on the new tarball (keymap="emacs" + Omarchy carve-outs) is the real test.

**🔴 LINUX LIVE-TESTING WAVE (2026-07-10/11 night — the user's first real Linux sessions, Omarchy/Hyprland):**
- **Save-feedback round** ✅ BUILT @ `ab14691` (`wyxe6gp4h`, clean round 1). Scratch Ctrl-S → note promotion (Effect::ConvertScratchAndSave, stash retired, 2nd save plain, unwritable → notice; headless replay converts for REAL — sidecar-proven); SaveDone → "saved"/"save failed" notices; 8 toggle eprintlns silenced + move-note failure → notice; **src/println_audit.rs structural law test pins every remaining println to an audited table** (12 background-bookkeeping eprintlns kept + flagged as future notice candidates; FinishBuffer's = logged gap); dirty marker "• " in window_title (retitle only on flip; clears on autosave) + mac set_document_edited. TASTE: wording, glyph, conversion-vs-notice-only all flagged/flippable.
- **Wayland clipboard** ✅ BUILT @ `2fee4f6` (worktree-agent-a58f78c9c94906ef3, Cargo-only): arboard `wayland-data-control` feature enabled — root cause: X11-only default backend, Clipboard::new() failed on pure Wayland → silent no-op mirror (menu Copy same path; the apply.rs comment worried about Hyprland around a compiled-out write path). 6 Linux-only crates; wasm graph clean; suite 1787 green. LIVE-ONLY: real Omarchy copy/paste. Follow-up flagged: the 3 silent clipboard error sites → notice promotion.
- **Emacs-hands round** 🟡 BUILDING (`wunbcunvi`): complete the bindable motion catalog (C-f/C-b/C-n/C-p etc. were keymap-only — [keys] couldn't restore them) + per-chord `linux_keep_emacs` suppression at the collision owner + truthful labels compose. User's live verdict: native-wins ate his emacs nav — "borderline unusable".
- **Adaptive column** ✅ BUILT @ `9187d55` (`wavakt4or`; worktree-wf_44bfdd0e-cff-1). HEADLINE: the outline was HIDDEN at the DEFAULT 1200px/70ch canvas (5 chars < 6 floor) — not just a small-laptop issue; now shows with a 12-char rail via `geometry::adaptive_column_left` (one pure owner; every reader composes through column_left() for free; sidecar page.column reports it free). Wide = exact passthrough (capture-diff proven byte-identical at the measure-40 reference); narrow = shift capped at RIGHT_MARGIN_BREATH; narrowest = recenter+hide off the SAME value. TUNABLES flagged: OUTLINE_PREFERRED_CHARS=18 (=MIN*3), breathing floor 16px; BANKED: content-aware grant sizing (entries' needs, ¼ cap — the user-conversation refinement), glide-vs-snap transition + its reduce-motion interplay. ⚠️ merge train must re-run the FULL suite (build agent's whole-repo run was interrupted; module suites green, call sites hand-audited). 3-CYCLE DECISION (user conversation): rejected — grant policy over a coupled layout mode; revisit only if the user wants a deliberate "study layout" stance.

- **KEYMAP FLAVOR round** 🟢 APPROVED (user: "native by default, toggle emacs flavoured"), fires AFTER emacs-hands lands (same files): `keymap = "native"|"emacs"` config + sticky Settings toggle row; emacs = wholesale collision flip implemented as the built-in preset expanding through the keep-list machinery; [keys] = the carve-out layer (Omarchy recipe documented: keymap="emacs" + copy/cut/paste carved back to C-c/C-x/C-v); mac ≈ no-op (no collisions there); labels truthful.
- **Verbs round** 🟢 APPROVED, queued after save-feedback merges: Rename note… (minibuffer-style prompt via overlay input), Duplicate note (the Save-As fork job in notes clothing), HUD "saved · Ns ago" stat. Save-As itself stays refused (anti-rec A6 upheld after user challenge — the jobs decompose; familiarity via verbs, not the foreign lifecycle).
- **User-guide round** 🟢 APPROVED, fires after verbs: in-app "Guide" palette command (embedded markdown — dogfood), GENERATED per-convention keybinding reference from the catalog (can't drift), site /guide mirror, welcome points at it. README stays hand-written by the user (facts checklist provided).
- THEN: merge train → fresh dry-run → the user's next tarball (save + clipboard + emacs-hands + flavor).

**BANKED (elephants review, user ack'd):** AccessKit screen-reader support (the named Tier-2 project; Tier 1 reduce-motion cooking now); web bundle diet (43MB wasm → same-origin lazy CJK fonts + wasm-opt — the funnel's first impression); pre-release QA ritual (consolidate the live-only flags into a checklist); MAS submission checklist (icon, App Store Connect record + reserve "Awl" name, distribution cert, store copy; sandbox round cooking); README no-contributions statement.

---

**MERGE TRAIN 🚂 LANDED** (workflow `w8fxzzhp6`) — five rounds merged onto local `main` in order: warnings base → tables → outline+motions → keybindings → platform-commands, each suite-gated, conflicts reconciled on `commands.rs`. Doc-sync (this pass) followed.

**🧹 CLEANUP LANDED**: `79723b4` (warnings re-zeroed both targets — gates restored on slug_for_action/has_native_chord/peek_row_for_slug; slug_of_index/name_of_index/effective_chord_lists DELETED as superseded by the visible() family; commands::names + settings::names gated #[cfg(test)] as the unfiltered test baselines) + `fcc6d46` (seeds branch merged; welcome.md chord copy refreshed to the merged catalog — ⌘, settings, ⌘I italic — every other named chord audited current; seeds worktree janitored). Suite **1710 green**, both targets **0 warnings**. Local `main` = 14 commits ahead of origin, UNPUSHED (user's call).

**#7 LINUX-NATIVE KEYMAP ✅ BUILT @ `a897eea`, 🟡 MERGE AGENT RUNNING** (workflow `wg7i0q0j4`; verify clean round 1 — 1720 native tests, wasm 0 warnings). Landed: `convention.rs` Convention{Mac,Linux} (native cfg!, web set-once UA classifier, dev-only AWL_CONVENTION_FORCE); data design = ONE canonical mac string + pure Cmd→Ctrl translation + `LINUX_NATIVE_OVERRIDE` exceptions (Line start/end→Home/End, Doc start/end→C-Home/C-End); `native_down` gate (Mac Super-sans-Ctrl / Linux Ctrl-sans-Super) with arm order unchanged → native-wins for free; mac-byte-identical law test; [keys] literal/convention-agnostic. DISPLACED ON LINUX (each one [keys] line away, documented in template): C-s/C-f/C-p/C-n/C-a/C-e/C-w/C-r/C-b/C-g/C-c/C-x/C-v lose to Save/Find/Palette/New note/Select all/Inline code/Finish file/Replace/Bold/find-next/Copy/Cut(+prefix-arm loss)/Paste; C-k + C-d SURVIVE. Logged consequences: Follow link default fully empty on Linux; bare-Ctrl-X [keys] prefix arming unreachable there. Cosmetic gap → FOLD INTO #10: displaced emacs labels not blanked in the palette (dispatch correct, label lies). MERGE AGENT also fixing the PRE-EXISTING websmoke L2 breakage its verifier unearthed (stats.rs test module lacks the native cfg gate since e249097 → wasm test binary 96 compile errors, reproduced on parent commit; L1 always fine) + janitor. #10 fires when this lands.

## ✅ LANDED (2026-07-10 merge train — this session's doc-sync pass)

1. **wasm warnings → 0 + WebGL2 fallback verify** — `e249097` "chore: cfg-gate native-only paths — wasm build warnings 48→0" (real baseline was 48, not the earlier-assumed 43; native stayed 0). WebGL2 fallback **CONFIRMED** via Playwright: `navigator.gpu` stripped → renders, types, canvas webgl2-owned; control run (WebGPU present) picked the `webgpu` context, non-degenerate. Evidence: `gallery/webgl2/{fallback-loaded,fallback-typed,control}.png`. Docs updated: WEB.md's WebGL2-not-yet-confirmed line, CLAUDE.md's stale-warnings-count line.
2. **Platform-scoped commands (web hides desktop-only)** — `2a14197`, merged `ef2df0f` "merge: platform-scoped commands". `native_only` field on `Command` (8 flagged: Recent projects…, Version history…, Clean unused assets…, Keep version, Finish file, Lifetime stats, Quit, Keybindings…) + `Platform{Native,Web}` with one `cfg!` read + pure `available_on`/`visible()`; every consumer rerouted (palette, rebind menu, whichkey, settings rows, `menu::roster_for`); dispatch gate at the top of `apply_core` (Cmd-Q on web = total no-op). Native byte-identical. Documented in CLAUDE.md's new "THE PLATFORM-SCOPED COMMANDS ROUND" section.
3. **Tables: overlap bugs** — `1424001`, merged `f224867` "merge: table render fixes (reveal swaps to source, one-owner grid geometry)". Reveal is now a true swap (caret's table row uploads zero grid cells); new `layers::TableGridCache` is the one owner of table layout (`compute_table_layout` writes, `prepare_table_grid` only reads), fixing a `sync_wrap_width` divergence window. Evidence: `gallery/tables-fix/after-caret-{in,outside}-table.png`.
4. **Outline click mis-targeting when cramped** — `bb716a4`, merged `c2194a0` "merge: outline click fix + rebindable motions". `Wrap::None` on `outline_buffer` + `outline_pixel_fit` (new `rowlayout::fit_primary_end_to_px`) corrects the char-estimate to measured shaped pixels.
5. **Keybinding report tiers 0–3** — `f298ae8`, merged `26b3d86` "merge: keybindings tiers 0-3 (Settings Cmd-, / Italic Cmd-I / Task list / Finish file / Stats HUD)". P0 unhandled-Cmd swallow guard, P1 `Cmd-,` Settings…, P2 `Cmd-G`/`Cmd-Shift-G` find next/prev with remembered-query fallback + `Cmd-F` prefill-from-selection, P3 App-menu Hide block, P4 `Cmd-Shift-L` Task list, P5 `Cmd-W` Finish file; HUD relocated to hold-`Option-Cmd-I`, freeing plain `Cmd-I` for Italic. `Cmd-K` deliberately left RESERVED (inert) for Links v2. Documented in CLAUDE.md's new "THE KEYBINDING-IDIOM AUDIT — tiers 0-3" note.
6. **Rebindable motion keys** — `f3dfb44`, merged `c2194a0` (same merge commit as #4). Six curated navigation motions promoted to the `commands.rs` catalog — Forward/Backward word (M-Right/M-Left), Line start/end (Cmd-Left·C-a / Cmd-Right·C-e), Document start/end (Cmd-Up/Cmd-Down) — palette-visible, rebind-menu-listed, `[keys]`-rebindable; config template documents `forward_word = ["M-Right", "M-f"]` / `backward_word = ["M-Left", "M-b"]`. This is what makes CLAUDE.md's "any retired chord is one `[keys]` line away" claim literally true for motions, not just aspirational.

## 🔜 STILL ARMED

7. **Linux-native keymap + web platform detection** — 🟢 APPROVED IN PRINCIPLE (user 2026-07-10), fires now that the merge train (incl. #2's Platform work) is on `main`. Spec sketch: slot 1 becomes platform-RESOLVED ("Primary-S": Primary = ⌘ on macOS, Ctrl on Linux) — one catalog, per-platform chord tables, palette/menu labels render the resolved form (⌘S vs Ctrl+S). TWO AXES kept distinct: capability (native/web, #2's hide list) vs convention (mac/linux chord layer); web detects convention via web_sys navigator.userAgent/platform at startup (CodeMirror/Monaco precedent; default Ctrl when unsure; browser-reserved-chord caveat mirrors to Ctrl-T/N on non-mac — emacs slot stays the web escape hatch). THE DESIGN DECISION: on Linux, Ctrl-native collides with the emacs survivors (Ctrl-S save vs C-s search, Ctrl-F find vs C-f char-forward, C-w/C-n/C-a/C-e…) — POLICY = NATIVE WINS on Linux (orchestrator rec, pending user nod; displaced emacs chords are one [keys] line away, #6 made that literally true for motions); pairwise-conflict sweep runs PER PLATFORM. Note: Linux native build today silently carries the mac ⌘/Super table — the advertised keymap is simply wrong there; this round is what makes the muda-on-gtk menu-bar follow-up (banked in the menu round) coherent too.

8. **Curate web first-load seeds** — 🟡 BUILT @ `7f33048` on `worktree-agent-a04e7292e553b39b3` (1678 tests green at build time) but **NOT yet an ancestor of `main`** — still needs its own merge pass. ⚠️ MERGE-TIME NOTES carried forward: (a) WELCOME-CHORD RACE — the branch verified chords against MID-TRAIN main (pre-keybindings-merge), so its welcome.md says "no ⌘, chord / Italic palette-only" — now stale against the landed catalog (⌘, Settings…, ⌘I Italic exist; HUD is ⌥⌘I) and MUST be refreshed against the final merged catalog before landing. (b) It may carry a few wasm warnings from its mid-train base (predates the cfg-gating commit `e249097`) — re-run the wasm 0-warnings check and cfg-gate anything new. WEB.md is touched on both this branch and the just-landed doc-sync pass — expect a small mergeable overlap. New seed set: /welcome.md (rewritten warm tour), /tour.md (new one-page WYSIWYG showcase), /prose.md, /japanese.md; longwrap.md + spellcheck.md dropped from seeding (stay in samples/ for the harness). Mechanics: sentinel `awlfs:seeded:v2` + per-file write-if-absent.

9. **Outline yields to the in-app menu bar** — ✅ MERGED @ `4dce3ba` (clean, zero conflicts; suite 1723 = 1720 + its 3 new tests; L2 still 8/8; worktree janitored). Mechanism: `outline_layout.top` = `TEXT_TOP + menubar_reserve()` — the SAME height owner `doc_top` already used (the reference implementation); row budget shrinks with it; hit-test follows free (shared layout struct); toggle reflows per-frame for free. BONUS SIBLING FIX in the same diff: the search/replace panel card (`rects.rs::panel_layout` `card_y`) had the identical blind spot — same one-owner fix. 3 tests added. (History: original agent accidentally killed by user post-suite; finisher verified + committed — no changes needed, work was complete.)

**#7 MERGED**: `bd4ed31` (clean) + `6a85f9d` (L2 unbreak — the stats.rs gate was layer one of SIX files with the same missing-cfg disease, L2 had never compiled past the first; also caught a REAL keymap-round regression: websmoke's C-f test hit the native-wins collision because wasm's ambient convention defaults to Linux with no wasm_start — pinned Mac per keymap.rs's own test precedent). L2 now 8/8 green incl. both UA-classifier tests. Main @ 6a85f9d, 17 ahead of origin, suite 1720, 0 warnings both targets. #9 merge agent running (behind it, disjoint files).

10. **Web chord sanity (browser-reserved shortcuts)** — ✅ MERGED @ `098c5ad` (built `e52c4e1`, verify clean round 1). Tier 1: NOTHING NEEDED — winit prevent_default + canvas focus already correct, live-confirmed via Playwright (Ctrl-S/Ctrl-F arrive defaultPrevented, focus survives click-away). Tier 2: new `src/webreserved.rs` per-convention reserved tables; New note + Switch theme… go label-BLANK on web (no surviving emacs slot; replacement chords = logged v2 taste call — web also has no config file to persist a user pick yet). Tier 3: ONE truthful-label owner (`join_slots_truthful` supersedes `join_slots_resolved`; `LINUX_DISPLACED_LETTERS` shared with the keymap collision table — can't drift); displaced emacs chords blanked everywhere. Honest epistemics logged: CDP-injected input can't prove/disprove true reserved-ness — Tier 2 rests on documented browser behavior. 17 tests added incl. 2 real-wasm.

**🌙 NIGHT CLOSED (2026-07-10)**: local `main` @ `098c5ad`, **22 commits ahead of origin, UNPUSHED** (push is the user's). Suite **1737 green** (was 1241 at the last pushed state), **0 warnings native + wasm**, websmoke **L2 10/10** (rebuilt from broken this same night). All session worktrees janitored; `git worktree list` still shows ~25 PRE-EXISTING worktrees from OTHER sessions (jp-faces, jp-typo, misc wf_*) — stale-worktree housekeeping banked for an audit day. Cosmetic history note: the outline merge appears twice-ish in the log (`c97f89d` no-edit merge + `4dce3ba` descriptive merge — an amend that stacked instead of replacing); harmless, gates green atop both. LIVE-CHECK LIST for the user (flagged live-only across rounds): ⌥⌘I hold on a physical keyboard; table swap-reveal + pill/panel taste; welcome/tour read-through on web; Linux-convention feel in a real browser (labels, displaced chords, blank New note/theme chords); table pan gesture on web; the deploy refresh of /editor when ready. Two tiers: (1) INTERCEPT everything interceptable — audit winit web `prevent_default` + canvas focus so Ctrl-S/F/G/P/B/, reach awl instead of the browser (WEB.md's "⌘P observed swallowed" suggests leaks we can win back); (2) ROUTE AROUND the truly reserved set (Ctrl/⌘-N, -T, -W + Shift variants — no page can intercept these, ever): mark browser-reserved per convention in #7's tables; affected commands (New note, Switch theme — Finish file/Quit already web-hidden) get an alternate web-advertised chord or fall to slot 2/palette; LABELS must render the web-effective chord (never advertise a chord the browser eats). Tier 3 banked: installed-PWA note.

## 🏦 BANKED FOLLOW-UPS (from this merge train)

- **Table Meta-parse unification** (agent-flagged in #3): `compute_table_layout` vs `prepare_table_grid` still duplicate table-block Meta parsing off the same `md_spans` source — small, same merge-don't-align spirit as every other "same behavior ⇒ same code" cleanup in this codebase. Not urgent; fold in next time either function is touched.
- **Web table-pan gesture check** (from #3): per-table pan already ships (`table_pan_*` + the indicator bar) — no new feature needed, but nobody has confirmed the drag/scroll gesture actually reaches the canvas on a real web build. If it turns out dead on web, queue input wiring.
- **`.app` bundle chore** (pre-existing, re-surfaced by #5's App-menu work): a proper `.app` bundle (`CFBundleName = "Awl"`, `CFBundleExecutable = "awl"`) is still the only way to get the menubar's own leftmost App-menu title reading "Awl" instead of the raw binary name — banked as packaging work, not a code round.

## ✅ LANDED (2026-07-05 second wave — all pushed; origin/main @ 92c7c28, 1241 tests)
- **WYSIWYG v1.1** `38be97d` — TRUE zero-width conceal (near-zero font-size metrics spans; ghost spacing gone; heading flush-left) + one-piece fence panel/wash tiling (no row seams); exposed+fixed 2 latent bugs (refresh_rule_conceal now invalidates row_geom AND runs before set_caret_target). ☀️ live: the reveal-reflow feel when the caret enters a styled line.
- **gutter** `d481714`+`693c110` — filename never wraps (shared rowlayout elision owner, extension preserved); USER CORRECTION applied: both lines elide independently, project always visible until the hard floor hides the whole gutter.
- **cursor shapes** `542f6a5` — I-beam over text / ColResize on the page edge / arrow on overlays+margins; one tested priority table; fires on change only; composes with pointer-hide. Taste call: arrow (not hand) on picker rows.
- **test hermeticity** `aca51b9` — App::new_hermetic seals session/stash/config doors; structural guard test source-scans for unaccounted App::new; poisoned-env verified twice.
- **page-width split** `f88b127` — PageClass (prose 70 / code 100 = rustfmt), per-kind sticky writes + reset, legacy page_width RETIRED (inert if present), sidecar page.class, schema /98-100.
- **parallel-suite fix** `050116f`+`e36c344` — the REAL racer was the Goto replay's unlocked page-global writes (not a GPU race); every page-global writer now self-locks under cfg(test) (page::test_lock, reentrant, page LAST in lock order); a live ABBA deadlock found+fixed; the law test made parallel-safe itself. PROCESS LESSON RE-LEARNED: I pushed a red suite once by gating on a piped tail — pushes now gate on cargo's own exit code (pipestatus).
- **macOS menu bar** `92c7c28` — muda 0.19.3; App/File/Edit/View/Window, every item a third door to catalog Actions via ONE law-tested table; routed (not predefined) Quit so exiting() hooks fire, routed Edit items because raw wgpu NSView has no responder chain; v1 ships without menu accelerators (NSMenu would intercept the keymap's chords — logged tradeoff). MAC-CITIZEN ARC COMPLETE (multi-buffer → daemon → session restore → menus; tab strip consciously killed). ☀️ live: the bar appearing, items firing, About/Quit behavior.

## 🔜 REMAINING (2026-07-05 evening)
- **banked: RELEASE READINESS (user asked what must be defined pre-release, 2026-07-05) — UPDATED 2026-07-10 night, most infra now BUILT, gates now user-decision-shaped not code-shaped:** (1) ~~LICENSE~~ — code still has none (RELEASING.md §4 logs the gap explicitly: Hunspell dict notices + a NOTICE/copyright file both missing) — now tracked as part of **#13 Mac release execution**, above; (2) ~~.app bundle + codesigning + notarization~~ — **BUILT this round** (`scripts/package-macos.sh` + `release.yml`'s mac job; signing/notarizing gated on 5 Apple secrets, unsigned fallback otherwise) — only the SECRETS + ICON are still missing, tracked in #13; (3) the COMPAT FREEZE line in PHILOSOPHY.md — still open, not addressed by this round; (4) the CRLF normalize-on-load call — RESOLVED separately (see "Line endings" in CLAUDE.md, the VS Code model shipped); (5) a human-facing README — still open; (6) version scheme — `AWL_VERSION` in `scripts/package-macos.sh` already reads `Cargo.toml`'s `package.version` via `cargo metadata`, and `release.yml`'s tag-triggered path derives the release version from the `v*` tag itself — mechanism BUILT, just needs a first real tag. Bundle-vs-packs already settled (bundle; packs = never-download break-glass). CI (`ci.yml`) and the fly-site deploy (`deploy-web.yml`) were NOT part of the original 2026-07-05 ask but landed alongside this infra and are documented in CLAUDE.md's new pipeline section.
- **chore: cargo-audit (from the 2026-07-05 supply-chain briefing)** — the one actionable gap: no advisory scanning exists. Install cargo-audit, run it, fix/acknowledge advisories, add a note to CLAUDE.md to re-run it each merge-train day. (Full audit findings: 22 direct / 178 reachable crates on mac, ZERO network deps anywhere, 33 build-time-code crates = the real vector, lock committed, wgpu exact-pinned, assets vendored.)
- **banked (user Q, answered no-changes): full-bleed page hides the gutter** — verdict: coherent (full-bleed = user asked for zero chrome), identity stays available on demand; micro-follow-ups if ever wanted: filename in the Cmd-I HUD + verify the native window title carries the filename.
- **banked (user product Q): NO TOASTS for user actions** — feedback taxonomy settled: user-action feedback = in-world juice on the thing itself (the caret-juice language); system interventions with no in-world anchor = the existing quiet bottom-center notice (clobber guard); visible results (paste) need no announcement. Candidate if copy ever wants feedback: a subtle selection pulse on the juice seam — banked pending a nod, shipping nothing is also fine.
- **TASTE GATE (user + boyfriend): JP world-faces gallery** — branch worktree-wf_02196eb4-bb8-2 (157f6c5, +13.5MB, all 14 worlds assigned characterful ja faces); captures in that worktree's gallery/jp-worlds/. Merge on the nod; cherry-pick per face is fine (each is independent data).
- **☀️ LIVE-REVIEW session (--release)** — the accumulated list on this board: multi-buffer feel, daemon two-process handoff, session restore relaunch, WYSIWYG reveal feel, cursor shapes, pointer first-keystroke hide, menu bar, Saltpan/light-world code colors, zh WenKai on Mopoke/Quokka, JP bundled look, Enter/boundary juice, ==highlight==, dictionary picker (flip to en_AU).
- Banked: fast-follow cjk_priority→capture ViewState; goto-picker MRU ranking (the strip's replacement); zh-Hant subset; WYSIWYG v2 links; conscious version mark; ~20 worlds arc (with per-world JP/zh faces); sound layer; CRLF product call; Shift-PageUp/Down; winit-warning re-check on bump; rename/move vs registry-collision design call; ~25 test-backlog items.


## ✅ LANDED (2026-07-05, merge train) — japanese round: bundled Noto Serif/Sans JP as first CJK candidates — `9bb8ca6`, PUSHED to origin/main
- **japanese round (bundle-first, option C) — MERGED + PUSHED.** USER NOD RECEIVED ("lets go", 2026-07-05) — the bundled-first candidate order ships. Merge commit `9bb8ca6` (merges `worktree-wf_2aaad8c7-1ef-2` @ `e7d65ef` into `main`; branch cut from `c8b1cf1`, so it crossed the WYSIWYG round + web-fix + trunk commits that landed on main since — `f8180c8`/`565c2a3`/`30bb111`/`ffa16e0`/`19d3409`, 8 commits). Embeds Noto Serif JP + Noto Sans JP (Google Fonts, OFL, JIS X 0208 subset via fonttools/pyftsubset) as `render::FONT_CJK_FACES`, listed FIRST in `theme::CJK_MINCHO`/`CJK_GOTHIC` ahead of the system Hiragino/Noto-CJK fallback (which stays trailing, never removed) — a Japanese run now resolves the SAME bundled family on every machine regardless of installed system CJK fonts.
- **Merge conflicts (3, all mechanical schema-string collisions, resolved by hand):** `CAPTURE.md`, `src/capture.rs`, `src/capture/sidecar.rs`. Both this branch and the WYSIWYG round (already on main) independently bumped the sidecar schema `80→83` for their own additive field (`font.cjk` vs. the `wysiwyg` block) — same target number, different meaning. Resolved by keeping BOTH fields (font block gains `cjk`, and `wysiwyg` stays in the top-level shape) and bumping one further, to `/86` (timeline `/87`, held `/88`), with every doc comment / CAPTURE.md prose / test doc-comment updated to match (grepped for stray `/83`/`/84`/`/85` references after resolving — none left). `theme.rs`/`render.rs`/`render/text.rs`/`src/caret/tests.rs` auto-merged clean (disjoint regions from main's changes).
- **Gate:** `ViewState {` initializer grep — clean, no new field this round. `cargo build` clean; full `cargo test` foreground — **1061 passed, 0 failed, 4 ignored** (~170s). Known flake `capture::tests::double_capture_is_byte_identical` did not fail in the full run; isolate-reran it 5× standalone → 5/5 clean passes — did not reproduce this round (pre-existing intermittent flake, not a regression).
- **Byte-check (the critical regression gate):** built the pre-merge parent (`19d3409`) in a throwaway `/tmp` worktree with an isolated `--config`. Froze the CURRENT (post-merge) `CLAUDE.md` as a fixed Latin-only fixture and captured it with BOTH the pre-merge and post-merge binaries — **PNG byte-identical** (`cmp` clean); sidecar JSON-key diff showed ONLY the expected additions (`schema` string bump, new `font.cjk` block) — every other field (cursor, text, theme, page, focus, wysiwyg, md_spans, …) bit-for-bit equal. Confirms the JP bundling touched nothing about how existing Latin content renders.
- **JP capture:** rendered `samples/japanese.md` under Undertow (serif→mincho) — sidecar `font.cjk` = `{ "family": "Noto Serif JP", "bundled": true }`; visually confirmed kanji/hiragana/katakana all shape correctly mixed with Latin text (screenshot reviewed). The dedicated capture test `capture::tests::japanese_fixture_resolves_bundled_cjk_face_deterministically` (Undertow + Currawong, asserts `bundled: true` on each) passed in the full suite.
- **Binary size delta:** release build before (`19d3409`) = **15,921,312 bytes (15.18 MB)**; after (`9bb8ca6`) = **22,315,072 bytes (21.28 MB)**; delta = **+6,393,760 bytes (+6.09 MB)** — matches the branch's own reported ~6.0 MB estimate for the two JIS-subset JP faces (~3.5 MB + ~2.5 MB).
- **Web/trunk sanity (cheap check per the "tofu now fixed" note):** `trunk build --release` on the merged tree — clean, `dist/awl-*.wasm` emitted (~17.7 MB, grew as expected from the bundled JP fonts going into the wasm binary too). Not deep-tested in-browser this pass (not requested), just confirmed the build itself is unbroken post-merge.
- **Push:** `git push origin main` → `19d3409..9bb8ca6`, clean, no force needed. Working tree left exactly as found (only `.gitignore`/`Dockerfile.linux`/`scripts/build-linux.sh` dirty, untouched); throwaway `/tmp` worktree + baseline build removed after use.
- **STILL TASTE-GATED, not yet the final call:** the bundled-first order SHIPPED, but system Hiragino/Noto-CJK stay as trailing fallback candidates until a live eyeball between bundled Noto and system Hiragino (see the live-review item below — revert is a one-line candidate reorder in `theme.rs` if it reads worse live). The kana→kanji IME session (preedit rendering, conversion, no caret weirdness mid-preedit) is LIVE-ONLY and still unconfirmed — also flagged below.

## ✅ LANDED (2026-07-04, merge train finisher) — web-fix: wasm canvas cfg-gate — `565c2a3`, PUSHED to origin/main
- **web-fix (no 1200x800 canvas pin on wasm) — MERGED + PUSHED, browser-verified.** Merge commit `565c2a3` (merges `worktree-wf_f9689a0f-881-2`, parent commit `30bb111` "web: stop 1200x800 pinning the canvas; characterize the cursor-reset hypothesis", into `main`) was already committed when this finishing pass started — re-ran the full gate + a real Playwright browser proof rather than re-merging.
- **Gate:** `cargo build` clean; full `cargo test` foreground → **1060 passed, 0 failed, 4 ignored** (~161s). The known parallel flake `capture::tests::double_capture_is_byte_identical` was re-run in isolation → passed clean (flake, not a regression; joins the existing banked flake note).
- **Browser proof (`trunk serve --release` + Playwright MCP, http://127.0.0.1:8080):**
  - **1600×900 → PASS.** `canvas.getBoundingClientRect()` = exactly `{0,0,1600,900}`, matches `window.inner{W,H}`, `canvas.width/height` = 1600×900. NOT pinned to 1200×800 — the fix holds. Console: 0 errors (only benign `integrity` preload + locale-fallback warnings).
  - **800×600 → PASS.** Canvas rect = exactly `{0,0,800,600}`, tracks viewport 1:1. Console clean.
  - **375×667 → PASS, with an intentional floor (not a bug).** Canvas settles at `464×288` (bigger than the 375×667 viewport → deliberate horizontal overflow), inline style `min-width:464px;min-height:288px`. Traced to `src/app.rs` (`resumed`, ~line 650-683): `MIN_COLS=30`/`MIN_LINES=8` × font metrics, applied via `with_min_inner_size` on BOTH native and wasm — a documented readability floor ("the window can never be dragged below roughly one readable line"), not a revival of the old fixed 1200×800 pin. Typed a full line at this size with no panic, correct glyph layout, live spellcheck squiggle rendering. Console clean of errors across all three sizes (only the pre-existing `integrity`-attribute and locale-fallback warnings, present at every size).
  - **CURSOR PROBE — reproduces the cursor-reset hypothesis 30bb111 asked to re-confirm live.** Sequence: click canvas → `Meta+ArrowDown` (buffer-end chord) → type `AAA` → wait 2s idle (crosses the ~1s autosave debounce) → type `BBB` (separate action, no re-click) → read `/welcome.md` back out of `localStorage` (`awlfs:F:/welcome.md`). **Result: final text was `"BBBAAAsmall viewport typing test# 日本語テスト…"` — BBB landed BEFORE AAA, both prepended at absolute document position 0**, not appended after AAA as continuous typing would produce, and not at the buffer end despite the Cmd-Down chord. Settled caret rendered directly over the first `B` (position 0), confirming the cursor is genuinely at start, not a display artifact. This is a live, browser-only reproduction of exactly the symptom 30bb111 flagged ("typing resets to buffer position 0... needs browser re-confirmation… does not reproduce from this seam") — code inspection there cleared `autosave_flush`/`stash_scratch_now` as the cause, so the real trigger is still open. **Not investigated further here** (out of scope for this finishing pass — the push gate was the canvas fix, not this pre-existing, separately-tracked symptom) but now has a concrete, minimal repro recipe for whoever picks it up: type → idle ≥1s (autosave boundary) → type again with no click → check insertion point.
- **Push:** canvas proof passed (viewport tracking + no panics) → `git push origin main` → `f8180c8..565c2a3`, clean, no force needed. Working tree left exactly as found (only `.gitignore`/`Dockerfile.linux`/`scripts/build-linux.sh` dirty, untouched); scratch screenshots + `.playwright-mcp/` cleaned up after use.
- **Remaining user gate (unchanged, still open):** JP font eyeball (Hiragino-vs-Noto side-by-side captures) — lives on `worktree-wf_2aaad8c7-1ef-2`, per the japanese round's taste-gate below. Not touched this pass.
- **New follow-up candidate (not queued yet, flagging for triage):** the cursor-reset-to-buffer-start-after-idle symptom above is now LIVE-CONFIRMED (previously only suspected/uncleared by code inspection) — worth a dedicated investigation branch once picked up; repro recipe is in this entry.

## 🔜 QUEUED (user, 2026-07-04 evening) — 12 of 16 items landed this round (9 build round + 3 merge-train); see ✅ LANDED below
- **TAB STRIP — KILLED (user, 2026-07-05: "nah lets get rid of the file strip lol... with recent files we probably don't need it")** — Mac-citizen arc item 4 is dead; PHILOSOPHY.md's "no tab strip" line stands unamended. Multi-buffer stays invisible: the pickers + Finish Buffer's last-buffer flip are the doors. OPTION (not built, user's "recent files" instinct made concrete): the go-to picker could rank OPEN buffers first (MRU), giving a zero-chrome buffer switcher through an existing surface — propose when convenient. Native macOS menus promotes to arc item 4.
- **page-width split: prose vs code (user, 2026-07-05)** — the 70-char Butterick measure is a PROSE number; code gets its own default. DECISION (recommended, user asked): code default = 100 (rustfmt's max_width; 80 is the classic-terminal number but genuinely too narrow for modern Rust/idiomatic code — "too optimistic" confirmed). Two config keys: page_width_prose (default 70), page_width_code (default 100), both user-configurable; the active buffer picks by kind (is_markdown/no-syn_lang → prose; syn_lang → code). INTERACTIONS to wire: the sticky page-width write-on-drag/C-x {} persists to the key matching the ACTIVE buffer's kind; PageReset clears the matching key; buffer SWITCH between prose↔code re-applies the right measure (through the existing set_measure seam — mind RowGeom invalidation, the stuck-hunt lesson); NO legacy migration (user call 2026-07-05: "there's no users only me") — the old page_width key is simply RETIRED: delete the key handling, remove it from the config template, a stale line in an existing config is silently inert (the standard lenient-parse behavior); the user just re-drags or sets the new keys once. Sidecar page block reports which measure class is active. Tests: per-kind default resolution, sticky write targets the right key, switch re-applies, reset clears the right one, stale legacy key is inert.
- **gutter fix — USER TASTE CORRECTION (2026-07-05, supersedes part of the in-flight round):** the PROJECT line STAYS visible under the filename even under width pressure (the user wants both lines; the project vanishing was the complaint, not a feature). Filename still never wraps mid-word (elide with extension preserved — that part stands). Yield order becomes: elide filename first; the project line drops only below the hard floor where the gutter hides entirely. APPLY AT MERGE TIME of the in-flight gutter round (worktree wf_41e51502-2f1) — adjust before landing.
- **native macOS app menus (user q, 2026-07-05: "do we have the native app menus?") — NO, today awl has only winit's bare default (app name + Quit, no File/Edit/View)** — QUEUED as Mac-citizen arc item 5 (after tab strip): real NSMenu bar via the muda crate (the winit-compatible tauri menu lib): File (New Note/Open/Save/Finish Buffer), Edit (the clipboard/undo standard items routed through the existing actions — free correctness win: some macOS text services expect menu items to exist), View (theme/page/focus toggles), Window standard. Philosophy fit: the menu bar is INVISIBLE until summoned — the platform's own chrome, not ours; same "meeting the platform" precedent as the tab strip amendment. Every item routes keys→Action through apply_core (menus are a THIRD door to existing actions, never new behavior); menu accelerators display the native slot-1 chords. Native-only (cfg), wasm/linux unaffected (linux: skip v1, note it).
- **test-hermeticity blanket pass (from the session-leak find, 2026-07-05):** several tests build bare App::new against the real FS without session_restore=false (app.rs:1724, app/input.rs:893/907/921/934, app/daemon.rs:157/217) — not failing today but exposed to ambient ~/.local/share/awl/session.toml state. Add the kill-switch to every real-FS App::new test construction; consider a test-helper constructor so future tests can't forget (one owner).
- **BUG: gutter filename wraps mid-word at narrow width (user screenshot, 2026-07-05: "DESIG / N.md")** — the bottom-left filename/project gutter lays text directly (render/chrome.rs) instead of through a shared elision owner — the exact merge-don't-align violation RowLayout fixed for pickers. FIX: (1) filename NEVER wraps — one line, middle-ellipsis with extension preserved, via the SAME elision door the picker path truncation uses (extract/share rowlayout's fit_primary/middle-ellipsis rather than copying it — module-private bypass so chrome can't drift again); (2) project name = the SECONDARY: yields (disappears) first under pressure — current vanishing behavior is CORRECT, keep it, but it must yield BEFORE the filename ever elides; (3) below some hard floor (gutter column < a few chars) hide the gutter entirely rather than showing confetti. Add the gutter to the elision law test's sweep if the owner is shared. Unit at the layout seam + a narrow-width capture scenario.
- **✅ MULTI-BUFFER CORE — LANDED + PUSHED (2026-07-05, `ccc264d`, 1085 tests)** — arc item 1 done: BufferKey/BufferRegistry (MRU cap 16, dirty never evicted), open-already-open switches to the live buffer (edits/cursor/scroll survive), BufferExtra folds all version-keyed App caches, --keys-drivable + sidecar buffers block (schema /89). Post-merge adversarial review caught 2 blockers pre-push, both fixed in `ccc264d`: path canonicalization at the BufferKey boundary (relative/absolute/symlink = one identity; lenient ancestor-canonicalize for unborn files) + headless NewNote now parks through the registry (live/headless parity). Nit fixed: leaving-flush clobber notice no longer wiped. Logged follow-up: rename/move vs registry-collision (design call). ☀️ live-review: open a few files, switch around, unsaved edits must survive.
- **🔄 IN FLIGHT: daemon round (arc item 2)** — single-instance awl.sock + CLI handoff + --wait + C-x # finish-buffer; worktree wf_e92e59c2-8ac.
- **DECIDED (user q, 2026-07-05): NO Git LFS for fonts** — fonts are write-once immutable blobs (LFS solves churning binaries, not these); `include_bytes!` + LFS = the pointer-file footgun (un-hydrated clones embed 130-byte pointers → builds that succeed then fail to parse fonts at runtime — CI/Docker/fresh machines); scale is trivial (largest font 3.7MB vs GitHub's 50MB/file warning line). If repo size ever matters: native partial clone (`--filter=blob:none`), which hydrates on checkout and keeps include_bytes! working — no LFS, no new tooling.
- **JP world-faces round (user, 2026-07-05: "we probably want some japanese-only fonts — lots of good open source ones")** — per-world CHARACTERFUL Japanese faces, riding the i18n round's FontId resolver (DEPENDS ON: i18n round merged — themes reference per-script font IDs; this round is then pure data + assets). Candidates (all OFL, Google Fonts): Shippori Mincho / Zen Old Mincho (bookish serifs → Undertow/Saltpan/EB-Garamond-class worlds), Zen Kaku Gothic New (clean sans → Galah/Magpie), Klee One (warm, handwriting-adjacent → Mopoke/Quokka), BIZ UDMincho/UDGothic (utilitarian → workhorse worlds), M+ 1 / M+ 1 Code (techy → the mono worlds Tawny/Potoroo alongside their Latin monos). PIPELINE exists from the Noto round: instance at wght 400 → pyftsubset to JIS X 0208 (~2-4MB/face); expect 3-5 distinct faces shared across the 14 worlds, +8-15MB — REPORT the real delta per the bundle principle; worlds without a distinct face keep the Noto pair (the resolver default). TASTE-GATED like the Noto flip: a capture gallery (each world × the same JP fixture, current-vs-proposed) for the user's eyeball before merging; pairings are proposals, not law. Also: the never-tofu law + JP regression tests must stay green; size the per-face subsets honestly.
- **chore: startup "Window move completed without beginning" (user, 2026-07-04)** — macOS AppKit warning at launch, terminal-only, harmless; VERIFIED not ours (zero set_outer_position/with_position calls in src/) — winit 0.30.13's window creation trips AppKit's move-session bookkeeping on recent macOS. Re-check on the next winit bump; if still there, one upstream-issue link in a code comment near window creation. Not worth chasing otherwise.
- **def-tint round 2: LIGHT worlds — LANDED, see ✅ LANDED below (`c8b1cf1`, merged + pushed 2026-07-04).**
- **japanese round — LANDED, see ✅ LANDED at top (`9bb8ca6`, merged + pushed 2026-07-05).** (Original brief, kept for history: JP is FIRST-CLASS, audience of one; NO JA dictionary ever — the IME is where correctness lives; option C bundle-first shipped on the user's "lets go" nod. Two follow-ups remain OPEN, not closed by this land: the Hiragino-vs-Noto taste-gate eyeball — see the ☀️ live-review item below — and the kana→kanji IME writing-session confirm, also below. LATER, unstarted: per-world characterful JP faces (Shippori Mincho / Zen Kaku / BIZ UD…) for the ~20-worlds arc.)
- **WYSIWYG markdown — LANDED, see ✅ LANDED below (`f8180c8`, merged + pushed 2026-07-04).**

## ✅ LANDED (2026-07-04, merge train) — WYSIWYG reveal-on-cursor conceal — `f8180c8`, PUSHED to origin/main
- **WYSIWYG markdown (philosophy amendment) — MERGED + PUSHED** — merge commit `f8180c8` (merges `worktree-wf_2aaad8c7-1ef-1`, 3 commits `29b096a`+`9f2f0a1`+`6c11d52`, into `main`; clean merge, no conflicts, branch was cut from `c8b1cf1` = main's HEAD at merge time). Generalizes the pre-existing hr-fleuron + list-bullet reveal-on-cursor conceal to headings/emphasis/inline-code/highlight/fences per the PHILOSOPHY.md amendment ("if the caret is on that line, show the actual markdown; otherwise show the preview"): `markdown.rs` gains `MdKind::ConcealMarkup(ConcealKind)` (line-scoped for heading/emphasis/code/highlight, block-scoped for fences) and `MdKind::Code{inline}`; `render/spans.rs`'s `add_wysiwyg_conceal_spans` + shared `wysiwyg_reveals`/`line_has_code_span` decision fns drive both the renderer and the sidecar off one truth; a quiet `base_200` value-step PILL (inline code) and PANEL (fenced blocks) ride the same proto-cache pattern as the existing wash buckets. Sticky `wysiwyg` config bool (default on, mirrors `writing_nits`/`spellcheck`); sidecar `wysiwyg: { on, concealed }` block, schema bumped to `/83` (timeline `/84`, held `/85`).
  **This branch had NO independent verify — the merge train was it.** Performed here, not on the branch:
  1. `ViewState {` initializer grep across all 6 sites (`render.rs`, `bench.rs`, `capture/modes.rs`, `app/viewstate.rs`, `render/framebench.rs`, `render/perfbench.rs`, `render/tests.rs`) — clean; the branch's two new pipeline fields (`fence_panel_pipeline`, `code_pill_pipeline`) live on `TextPipeline`, not `ViewState`, so no initializer needed updating.
  2. `cargo build` clean; full `cargo test` — **1059 passed, 0 failed, 4 ignored** (matches the branch's own reported count exactly).
  3. **OFF-SWITCH byte-compare** (the critical regression gate): built the pre-merge parent commit (`c8b1cf1`) in a throwaway worktree in `/tmp`, captured CLAUDE.md + samples/welcome.md with an isolated config (`wysiwyg = false`) on both the parent build and the merged build — `cmp`/md5 **byte-identical** PNGs on both fixtures, and a JSON-key diff of the sidecars showed only the new `wysiwyg` block + `schema` string differing (every other field bit-for-bit equal). `wysiwyg = false` reproduces pre-round rendering exactly, as designed.
  4. **Default-on sanity**: a capture of CLAUDE.md itself (1683 concealed ranges across heading/fence/code/emphasis kinds) and a purpose-built fixture (heading/bold/italic/inline-code/`==highlight==`/fence) both showed correct reveal-on-cursor behavior — the heading ON the cursor's own line (line 0) revealed its raw `#`, while the second heading and every other span off-cursor concealed to styled content; the fence panel visibly spanned the whole block; sidecar `wysiwyg.on: true` and `concealed` ranges agreed with the pixels (visually confirmed via a rendered PNG).
  5. **Adversarial diff skim** — no product collisions found: (a) conceal leak — `add_wysiwyg_conceal_spans` only ever iterates `md_spans`, which is an empty `Vec` for any non-markdown/non-code buffer (`self.md_enabled` false in `text.rs`), so there is no leak path into code/prose buffers even before the `wysiwyg_on()` gate; (b) fence panel perf — `ensure_fence_panel_protos` is a proper proto-cache keyed on `(row_geom.generation(), reshape_count)` exactly like `WashCache`, rebuilt only on invalidation; the per-frame `fence_panel_rects()` call is O(visible) offset+cull over the cached protos, not O(doc) per frame; (c) schema docs vs emitted JSON — CAPTURE.md's documented `{ on, concealed: [[start,end,"kind"]] }` shape, schema string `awl-capture/83`/`/84`/`/85`, and the kind vocabulary (`heading`/`emphasis`/`code`/`highlight`/`fence`) all match `sidecar.rs`'s `wysiwyg_json` + `capture.rs`'s `SCHEMA_*` constants exactly.
  Worktree cleaned up (`git worktree remove --force` + temp dir deleted). **Pushed to origin/main**: `c8b1cf1..f8180c8`.

## ✅ LANDED (2026-07-04, merge train) — theme-QA / def-tint round 2 + THEMES.md — `c8b1cf1`, PUSHED to origin/main
- **theme-QA: light-ladder ground-contrast retune (def-tint round 2, FULL PASS) — MERGED + PUSHED** — merge commit `c8b1cf1` (merges `worktree-wf_cf75a17b-6e3-1` @ `b5b5972` into `main`; taste-gate PASSED by the user 2026-07-04 before the merge was authorized). Brings the light-world Def/Const/Str tints through a SECOND retune: round 1 (`7ed6315`) fixed the navy-vs-luminance bug (law h, ink-luminance floor) by pushing `T_LIGHT`→`[0.84,0.90,0.94]`/`S_FG_LIGHT`→`0.28`, but that pushed tints toward the pale `base_100` ground and a live taste-gate caught Saltpan reading "too hard to read" — measured Saltpan Str contrast only 4.62:1 against ground (Quokka 3.66:1), under WCAG 4.5:1. Round 2 (`b5b5972`) adds law (i) — every tinted role's fg must clear ≥4.5:1 WCAG contrast against `base_100` alongside the existing ink-luminance floor — and re-sweeps to `T_LIGHT=[0.76,0.78,0.80]`/`S_FG_LIGHT=0.18` (lower t + lower sat clears BOTH floors with margin: worst ground 4.84:1 Quokka Str, worst ink dY 0.056 Gumtree Def/Const). Dark worlds untouched and re-asserted (9.4–13.5:1 measured). No world needed a `role_overrides` escape hatch. Also lands **THEMES.md** (the world contract doc — what a world is, every measurable law + its enforcing test, ink-ladder/role-tint derivation, add-a-world process) and a PHILOSOPHY.md "what gets bundled" section; CLAUDE.md's contract-doc list gains the THEMES.md reference (merged alongside main's own new "Branches & worktrees" section — both survive, no collision).
  Merge mechanics: auto-merge was CLEAN on all three touched files (CLAUDE.md, src/render/spans.rs, src/render/tests.rs) — no conflict markers; main's `==highlight==` wash routing (landed since the branch's cut point) and the branch's retuned tint constants/laws coexist in both files. `ViewState {` initializer grep: clean (neither side added a struct field). `cargo build`: clean. `cargo test`: **1051 passed, 0 failed, 4 ignored** (a transient parallel-run flake on `capture::tests::double_capture_is_byte_identical` in the first run did not reproduce on a full rerun or in isolation — pre-existing test-infra flake, not merge-introduced; see the FLAKY TEST banked note, now joined by this second instance).
  **Pushed to origin/main**: `e3a37b9..c8b1cf1` — `git log origin/main..main` empty, fully synced.
  **UNBLOCKS**: the japanese round's bundle-first font decision (Noto Serif/Sans CJK JP) — it was explicitly waiting on the theme-QA branch landing first (shared THEMES.md/spans.rs territory); it can now proceed independently.

## ✅ LANDED (2026-07-04 evening round, local — awaiting push) — main ahead of origin/main by 22, 1050 tests
_Independently closed 2026-07-04: GATE (build+test+determinism), PROBE (per-item live re-verification), and REVIEW (0 blockers, 2 nits) all read-only, all green. `cargo build` clean; `cargo test` = 1047 passed / 0 failed / 0 ignored (~142s). Isolated-`--config` captures confirmed byte-identical/self-identical where expected (only the schema string + env-derived fields differ vs the pre-round baseline — zero unexpected deltas)._
- **merge train: caret ink-box alignment + morph bar-trail anchor** — `a3b1c7f` (merges `worktree-wf_cf75a17b-6e3-2` @ `3f29bca` into main): closes BOTH of the queued caret items above in one branch — the block caret now ink-aligns on kerned glyphs (`caret_anchor_ink_box`, reading the shaped glyph's own swash box instead of the naive advance cell whenever the anchor maps 1:1 onto a single glyph on a proportional world; mono worlds + multi-char ligature clusters keep the old cell math) and Morph's line-start bar-degrade trail now anchors on the bar's left edge instead of the cell centre (`caret_is_bar_form`, the one owner of "is the caret's current form a bar"). This branch was cut from `origin/main` and so crossed ~20 unpushed local commits, including `c27d4ca` (Enter-land squash + boundary recoil) which ALSO touches `src/render/caret.rs` — the auto-merge was clean with zero textual conflicts (the two commits land in disjoint regions of the file: ink-box/bar-form helpers vs. the new `caret_line_land` delegate). Verified: `ViewState {` initializer grep clean (neither side added a struct field), `cargo build` clean, full `cargo test` 1050 passed / 0 failed (+3 net from the caret branch's own new tests).
- **merge train: CLAUDE.md branches/worktrees rider** — `9660bca`: closes the MERGE-TRAIN RIDER queue item — a short "## Branches & worktrees" section documenting local `main` (not `master`/`origin`) as the dev branch, the worktree ff-check-your-base convention, and the merge-train integration gate (build+test per merge, `ViewState {` grep after any struct-field add).
- **focus-mode crash: stale line index** — `e007912`: `render/focus.rs`'s `clear_focus_spans` no longer indexes `buffer.lines[li]` raw off a stale `focus_lines` snapshot after the buffer shrinks (Cmd-A select-all + type-over blew past `.len()` mid-`key_down`, unrecoverable panic — `len is 1 but the index is 757`); now guards with the same `.get()` pattern line 116 already used, and the rest of focus.rs audited for other raw `lines[li]` sites (none found). 2 regression tests. Live-verified: `--keys "C-x d s-a x"` on a 10-line file → exit 0, no panic, sidecar reflects the shrunk 1-char buffer.
- **focus-mode drag feedback loop + shift-click extend** — `239432c`: extracts pure `follow_scroll_strategy(mode, dragging)` gating the typewriter auto-recenter on "no live press" (a mouse press was recentering the view under the stationary pointer → phantom drag-selection → new focus unit → repeat-jump — "scroll really quickly"); autoscroll-cap sub-item reasoned out as unneeded once the gate landed (per user's "drain, no deadzone" call). Wires shift+primary-press to extend the existing mark in `on_press`'s click_count==1 arm. REVIEW NIT: a shift-click landing within the multi-click time/pixel window falls into the SAME click-count-bump path as a plain double/triple-click and silently drops the shift-extend instead of extending by word (pinned deliberate by `double_and_triple_click_arms_ignore_shift`) — confirm intended feel vs the common TextEdit/Xcode/browser "shift+double-click extends by word" convention.
- **caret-style picker: Morph preview never painted the silhouette** — `f0c0e4f`: fixed the choreographed demo panel below the caret-style picker (scope = the picker's own `CaretGlyphPipeline`/mask cache only — the live document caret was never affected). Sidecar gains `caret_preview.silhouette` for headless assertion.
- **writing-nits polish + JP pinning** — `7d8549e`: reveal-on-cursor — nits suppress the whole CARET LINE (a line is judged only once you've moved off it); spell squiggles suppress only the ONE word at/adjacent to the caret, not the whole line (the flagged taste call, decided as suggested — other misspellings on the same line still flag). Code-buffer nits now scope to the lexer's own prose spans (comment+string) via new `span_in_prose_ranges`, mirroring `spell::misspellings_for`'s scoping; confirmed the double-space rule already existed (only the code-buffer exemption was missing). Folds in the japanese round's pinning sub-item: unit tests pin that BOTH the spell scanner and the nits detector never false-flag pure Japanese prose, while a real mistake embedded in JP prose still flags — against the real bundled en_US dictionary. 10 new tests. REVIEW NIT (pre-existing, not introduced this round): the double-space rule still false-positives on markdown TABLE cell-padding (`"| Name  | Value |"`) since `nits::line_nits` is purely whitespace-shaped with no table awareness, and this round's code-buffer scoping doesn't cover markdown table rows in a prose buffer — banked below for a follow-up table-row exemption.
- **file-picker freshness** — `d0f5ac5`: one owner `rescan_file_index` replaces 4 copy-pasted call sites, wired on `Action::OpenGoto` — a summoned go-to-file overlay re-scans the project tree on every summon instead of serving a stale cached listing (measured ~8-9ms/173 files on this repo — cheap enough that no watcher/cache machinery is needed).
- **caret juice: Enter landing squash + boundary bump** — `c27d4ca`: Enter now rides the EXISTING typing-impact/recoil primitive for the caret's landing on the new line (caret-only, no row-reflow animation — rows don't dance); a motion that CANNOT move (C-a/C-e already at line edge, buffer start/end, M-</M->, PageUp at top, …) now plays the same quiet recoil bump instead of a silent no-op, enumerated via the `is_motion` completeness sweep (Shift-PageUp/Down stay documented non-movers). Determinism: settled capture is unchanged either way — pixel-diff-verified `--keys "C-a C-a"` (repeated boundary bump) == `--keys "C-a"`. Feel/depth/duration flagged live-only by the round itself.
- **pointer-hide v2** — `e1d419f`: deletes the 3s-countdown machinery outright (net −166 lines) — a real keystroke hides the OS pointer IMMEDIATELY (macOS/TextEdit convention), any mouse motion still shows it instantly, and window BLUR now un-hides too (never leave the pointer hidden across a focus change). REVIEW NIT: the IME preedit guard in `app.rs` returns *before* the pointer-hide call, and composed IME characters land via `WindowEvent::Ime` which never calls it either — so typing through an active IME (kana→kanji, etc.) never hides the pointer, an undisclosed carve-out from the "every real keystroke hides it" framing (possibly a reasonable call — a candidate window may want the pointer visible — but worth a deliberate confirm or a doc note).
- **markdown `==highlight==`** — `cd4bcd9`: the de-facto Obsidian/Typora/iA `==text==` convention (single `=` rejected — false-positives on `x = y`) renders the existing warm comment-wash BEHIND full content ink (a highlighter stroke, reusing the wash pipeline/bucket — no 3rd pipeline); `==` markers recede to muted like other markup. Hand-rolled inline scan over `markdown.rs` text events (pulldown-cmark has no native `==`). Sidecar `md_spans` gains a `highlight` tag, schema bumped to `/80` (timeline `/81`, held `/82`); CLAUDE.md doc updated in the same commit.

_Deferred, still queued (see 🔜 above): the full theme QA pass + THEMES.md (branch held — see the HELD note above), the japanese round's larger font-bundling decision (taste-gated), WYSIWYG markdown (awaiting user scope nod). The morph line-start bar trail anchor and block-caret kerning items landed via the merge train (`a3b1c7f`, above) and are no longer queued._

_The orchestration board. You talk design with me; this holds the plan + status.
Read it anytime; you won't see merge/push mechanics in the chat anymore._

> **🌙 OPERATING MODE — OVERNIGHT AUTONOMOUS (user asleep, 2026-07-02).** Drain the queue without blocking. Standing directive: *"if you need me to make a product call, drain the other ones first."* So — for any item that needs a TASTE/PRODUCT decision (mono-per-world assignment, theme-family tags, exact contrast value, etc.), MAKE A SENSIBLE DEFAULT, log it under ☀️ MORNING REVIEW, and keep going — never stall waiting. Chain rounds as background jobs complete: fire disjoint work in parallel, sequence anything sharing files (render.rs / spans.rs / sync_theme / overlay+chrome). Merge+push each landing, base-pin every worktree to the CURRENT main hash + verify the decomposed layout (the base-pin miss lesson). Keep the byte-identical / test / wasm discipline.
> ⚠️ MERGE DISCIPLINE (learned the hard way): (1) ALWAYS gate the push on `cargo build && cargo test` SUCCESS — never push a merge whose build failed. (2) A branch that ADDS a `ViewState` field must ALSO update src/render/perfbench.rs `bench_view()` (perf added that initializer; pre-perf-based branches don't see it → git auto-merges with NO conflict but compile-breaks — happened on the picker merge, fixed in 45b41ff). Grep for `ViewState {` initializers after any struct-field add.

## ✅ Done (on main, pushed)
- keymap: 2-binding model (native + Emacs slots) + native Cmd defaults + Option fix — `86cb1c3`
- rebind menu (game-style, Cmd-P → Keybindings) + right-click spellcheck — `8e5afad`
- caret-juice: recoil primitive + search press-twice-to-wrap + deletion squash/gulp + typing impact — `51782f8`
- type system: ink ladder (content/muted/faint) + size ladder (h3→1.25) + DESIGN.md §4 — `5aa26f9`
- themes → 14: cull Frogmouth/Bracken, add Kingfisher + Currawong; faint authored per theme — `95d7719`
- backgrounds: `Background` enum; add Stripes (N++) + edge-Dots; Potoroo→Stripes, Mangrove→edge-Dots — `c89fef4`
- sticky prefs: theme/zoom/page/caret persist to config.toml + restore on launch; zoom default 0.8 — `c3ce40a`
- caret-style picker with Smash-style live preview (Block/Morph/I-beam) — `c1e319d`
- FS trait: sync `FileSystem` trait + NativeFs + InMemoryFs; seam complete (is_dir + all bypass sites routed); clean build — `1220912`
- tweaks: default zoom 1.18→1.0, gutter→bottom-left, removed old status strip, HUD hold-only + macOS modifier-release dismissal — `65cae72`
- stats/gutter: panel hierarchy (label+muted hints) + dim-doc-overlay + filename/project gutter + held-key stats HUD (Cmd-I) + dropped always-on readout — `b7569eb`

- web demo: wasm32 + WebFs(localStorage) + crate::clock (web_time wasm-only) — VERIFIED in-browser (boots, survives interaction), merged — `672037b`
- perf: cursor-line wrap-geometry memo (RowGeom single-slot) — long-line frame cost 9.0ms→0.11ms (~82x), byte-identical — `c979a10`

- decomposition v2: 7 monster files → submodule dirs, byte-identical (caret 3606→626, app 2917→1145, buffer 2562→487, capture 2052→60, main 1849→119; render/actions test-extract finishing) + ARCHITECTURE.md refresh — `5b42de8`

- symbols+ornaments: AwlSymbols subset (DejaVu) via span seam — ⌘⇧⌥⌃ render + align fix; per-theme hr_ornament/end_mark; fleuron hr + end mark — `ac944e5`
- responsive layout: min window width + responsive page column (wide→centered, narrow→edge-to-edge) — `ac944e5`

- chrome round 2: cached blur backdrop (theme/caret pickers crisp, hues intact) + debug mode (zoom/viewport/ln:col/md:syn, fps→debug) + amber-% fix — `fcbd49e`
- hr-conceal: reveal-on-cursor — `---` concealed to centered fleuron, dashes reveal when caret on the line — `fcbd49e`

- fonts+ornaments: 4 swaps (Mopoke→iA Writer Quattro, Potoroo→Monaspace Xenon, Saltpan→Fraunces, Undertow→EB Garamond); AwlSymbols+⁂; per-syntax breaks ---→❧ ***→⁂ ___→❦ (reveal-on-cursor) — `d61342f`
- zoom+page: zoom decoupled (gutter survives zoom, bigger text same page) + settable sticky page-width + C-x {/} command + drag-to-resize — `d61342f`

- caret-preview panel: reusable `prepare_float_panel` primitive + choreographed caret demo below the picker; small polish (auto end-mark removed, ornaments 1.45×, eprintln dropped) — `522b2e5`

- panel-consumers: all pickers left-click-selectable (hover+click); HUD blurred+trimmed to writer stats; spell-correction panel AT the misspelled word — `64c7c59`

- markdown lists: nested bullet glyphs •→◦→▪ by depth (reveal-on-cursor) + Tab/Shift-Tab indent/outdent + Enter continues/ends list (+ ordered auto-increment) — `2df79c6`

- replace redesign: Cmd-R → discoverable labeled find/replace panel + teaching key-hint line (button-free); + button-free principle in PHILOSOPHY — `2602181`

- Cmd-A select-all + writing-nits (quiet muted underline, genuine-mistakes-only, distinct from spell) — `241f140`

- track-changes: local snapshots (FS trait, native+web) + git-presence gate + summoned History timeline (restore, +N/−M diffs) — `9b439f4`
- capstone-cleanup: dead-code sweep (fs::Metadata::created+plumbing, capture::capture, TextPipeline::metrics, paragraph/sentence/slugify wrappers) + de-amber markdown (LinkText→content ink, Code→muted; amber now caret-ONLY, pixel-verified) + HUD float-primitive elevation + Clipboard/Select-all into COMMANDS catalog + docs reconciled (syntax highlighting officially IN scope, which-key promoted, CLAUDE FPS→Debug/stubs/link) — `2b76c76`
- docs: no web artifacts for awl (native app — prototype in-app via headless capture; persisted to CLAUDE.md) — `854c272`
- fix: instance-buffer OVERRUN CRASH (the long-standing wgpu_core.rs:1952 write_buffer panic — NOT an OOM): selection + spell-underline pipelines grew via create_buffer_init sized to CONTENTS while bumping instance_cap to next_pow2, so a later frame whose count fell in the gap wrote past the buffer ("Copy 4656 bytes … Destination buffer of size 4608" = 97 vs 96 × 48-byte SquiggleInstance). Now grow sized to instance_cap×stride + fall through to write_buffer. +regression test. 833 tests — `23061b8`
- perf round (item 2): viewport-window the per-frame O(doc) work + before/after benches (hidden --bench-perf harness, long fixtures). ORNAMENTS (long-md palette-open) 41ms→0.26ms (~158×, the exact screenshot lag); THEME reshape 36ms→5ms (~7×); MOTION (settings ↑/↓) 11.7µs→1.2µs (~10×); CONCEAL 6ms→gated. BYTE-IDENTICAL verified (incl. live font-changing theme switch). 833 tests — `74644a0`
- picker fixes (item 8): overlay-owned scroll offset (hover only HIGHLIGHTS the row under the pointer, never rip-scrolls; root cause was top_idx derived from selection) + wheel scrolls the LIST not the doc; single-line path truncation (middle-ellipsis, filename+ext preserved, muted dir / content filename) which also fixes the card overflow; right-click a 2nd misspelling dismisses the 1st menu + re-targets (on_right_press fires Cancel then OpenSpellSuggest). 839 tests (+6). — `45b41ff` (merge needed a perfbench.rs overlay_scroll fixup — see MERGE DISCIPLINE above)
- line-measure (item 7): DEFAULT_MEASURE 80 → 70 (Butterick sweet spot; default page column ~1152px → 1008px). — `b415523`
- fence highlighting (item 9): fenced code blocks in markdown now syntax-highlight their body by the info-string language (rust/sh/…) — Alabaster value roles in the MONO face, fence markers + info stay muted, never amber; unknown-lang + indented fences stay plain; byte-identical for non-fence content (cmp vs base binary). 846 tests. — `a002588`
- motion-fix (the "stuck" report): a PRE-EXISTING vertical-motion bug (NOT the perf round — pre-perf build sticks identically) at shared wrap boundaries — UP (C-p) hard-stuck + DOWN silently SKIPPED a row when goal_x was past the wrapped row's end (that row-skip is what read as "down gets stuck"). New col_on_row() pulls the landing column back to the row it actually owns; wired into all visual_line_up/down sites, stays O(line). Exhaustive sweep now clean (every up ascends, every down descends, all positions). +2 GPU regression tests, byte-identical. 848 tests. — `28295c5`
- narrow-frame margin (item 6): NONPAGE_INSET=32 (separate from TEXT_LEFT, so the page-collapse floor is unchanged); non-page writing column insets 32px/side (was 16) → more side background. PAGE mode byte-identical. 848 tests. — `5834f52`
- themes-menu (item 5): the theme picker is now a FACETED LENS-SWITCHER — ←/→ cycle Time·Register·Voice·Temperature·All (All parked far right, clamped at ends), faint section headers per lens, live preview on move (whole editor re-themes ~5ms), Enter keeps, Esc reverts to the opening world; keeps the highlighted world across lens switches. Per-world ThemeTags derived on theme.rs. One-amber respected (active lens by value + content-ink underline, never amber). 853 tests. — `36ceb2c`

## ✅ LANDED (drain, local — awaiting push) — main ahead of origin/main by 5, 991 tests
_Gate-verified 2026-07-04: `cargo build` clean, `cargo test` = 991 passed / 0 failed / 0 ignored (130.6s). Byte-comparison + sidecar diff confirmed every feature below is default-off/absent and does not perturb an unrelated capture (CLAUDE.md + src/page.rs baselines identical; sidecar gained only `dictionary`, `debug.autosave_state`, `debug.autosave_since_s`, and the schema-version string). Feature probe re-verified all 5 items individually against the built binary. Code review: 0 blockers, 3 nits (see ☀️ LIVE-REVIEW LIST + notes below)._
- **syntax sample gallery** — `77bab9b`: `samples/syntax/<lang>.<ext>` for all 20 lexers; headless-verified via `gallery/syntax/<lang>.json` sidecars (syn_lang + role tags present; spot-checked rust/python/sql/yaml against a fresh regen — counts matched exactly). Answers the user's fn-name question: fn NAMES are `Definition`-tagged, calls stay plain (by design).
- **dictionary picker (en_AU / en_GB / en_US)** — `793a067`: bundled LibreOffice Hunspell en_AU/en_GB alongside en_US; `SpellChecker::new(DictVariant)`; sticky `dictionary` config pref (absent = en_US, byte-identical default); Cmd-P → "Dictionary" picker (3 rows, no live-preview — reparse is a real ~100–250ms cost, applies on Enter only); switch invalidates the spell debounce/squiggle cache through the one owner seam. Sidecar `dictionary` field, schema `/70` (timeline `/71`, held `/72`). Verified live via `--keys "Cmd-P d i c t Enter Down Down Enter"` → `dictionary: "en_AU"`.
- **page-measure reset (+ drag readout)** — `7d43d19`: `Action::PageReset` snaps `set_measure(DEFAULT_MEASURE)` and clears the sticky `page_width` config override entirely (format-preserving removal, not a written-back default); reachable via palette "Reset Page Width" (no default chord) and double-click on the draggable page edge; companion quiet muted char-count readout floats near the pointer while dragging (live-App-only, never in ViewState/sidecar). Verified: `--measure 40` then `--keys "Cmd-P r e s e t Enter"` → `page.measure: 70`.
- **debug-panel autosave indicator** — `98f9516`: a quiet `autosave …` line in the debug pane stamped exclusively through `App::autosave_flush` (+ clobber-guard sub-paths) — "autosave saved · Ns ago" / "autosave on" / "autosave held — disk changed" / "autosave off"; the clock figure renders the fixed `"autosave —"` placeholder in a capture (mirrors the perf-triad pattern). Sidecar `debug.autosave_state`/`debug.autosave_since_s` (both null in capture), schema `/73` (timeline `/74`, held `/75`). Verified: `--debug` capture shows the placeholder line, both fields null; default (no `--debug`) capture unaffected.
- **mouse-pointer auto-hide while typing** — `58123e0`: `window.set_cursor_visible(false)` after ~3s idle typing, any mouse move instantly restores; live-App-only pure state machine (unit-tested without a window), rides the existing single-`WaitUntil` seam. Structurally absent from the headless path (`grep` confirms `pointer_hide` appears only in `src/pointer_hide.rs` + `src/app.rs`, never capture/apply_core) — no sidecar field, default capture byte-identical.

## ✅ LANDED (fix round, local — awaiting push) — main ahead of origin/main by 12, 1013 tests
_Verified 2026-07-04 by an independent closer round (GATE + PROBE + REVIEW, all read-only, tree untouched): `cargo build` clean, `cargo test` = 1001 passed / 0 failed / 0 ignored (~132s, two independent runs). GATE isolated the one non-byte-identical capture (CLAUDE.md's own embedded `sh` fence re-saturates under the dark-world Def-tint fix's shared `S_FG_DARK` constant — confirmed EXPECTED, not a regression) and confirmed light-world tints + all other captures self-identical across repeated runs. PROBE live-verified all 5 items below against a freshly-built binary. REVIEW: 0 blockers, 1 nit (noted under item 3)._
- **(6) zoom misplaces the caret on heading lines** — `94f3cbe`: `set_caret_target` in `set_view` now runs AFTER `restyle_all_lines`, so a zoom-only restyle on a heading-bearing buffer no longer latches the caret spring target against stale pre-restyle row geometry. New regression test `render::tests::zoom_on_heading_line_keeps_caret_target_aligned` independently recomputes the target via the pure `caret_target_xy()` post-restyle and asserts it matches the latched target within 0.5px (non-tautological — doesn't just re-read the same field it's checking). Also added `markdown::tests::tilde_fence_highlights_body_same_as_backtick_fence` pinning that `~~~lang` fences syntax-highlight identically to backtick fences (pulldown's `CodeBlockKind::Fenced` is delimiter-agnostic by construction; now pinned by a real span assertion, not just "verified by construction"). PROVABLY LIVE-ONLY BY CONSTRUCTION: `src/capture/modes.rs` calls `pipeline.set_view` TWICE per render with unchanged zoom between calls, so any transient mis-ordering inside one `set_view` is always overwritten by the second call before the frame is captured — a `--screenshot`/`--capture-timeline` repro is structurally incapable of exposing this bug either way, which is why the unit seam (not a capture) is the correct and only reachable proof.
- **(7) spell polish: string prose gate + global toggle** — `e754039`: (a) code-buffer string literals now spellcheck only when they read as prose (≥2 space-separated alphabetic tokens, mirrors `looks_like_code`'s default-to-skip shape) via the one-owner `spell::looks_like_prose_string` gate inside `misspellings_for`/`misspelled_spans_scoped` — single-token strings (`"struct"`, `"const"`, `"en_AU"`, format strings, selectors) never squiggle again (the Currawong DEF_KEYWORDS acceptance case is now silent), while genuine multi-word prose strings and prose comments still flag real typos. (b) new global `spellcheck` config pref (default ON, mirrors the `writing_nits` boolean pattern exactly) + palette "Toggle Spellcheck" command wired through a real `Action::ToggleSpellcheck` (apply_core → config persist → immediate rescan → `reload_config`); sidecar gains a top-level `spellcheck` boolean, schema 73→76. (c) per queue decision, NO per-surface (md-on/code-off) split shipped — banked as a future escape hatch only. 12 new/updated tests across `spell.rs`/`config.rs`/`actions/tests.rs`, all green; live-verified via `--keys` palette-toggle capture + pixel-diff (squiggle band under a typo vanishes on/off, prose otherwise identical) and a targeted single-token-vs-multi-word string pixel diff (only the single-token squiggle disappears). NIT (code review, left as-is — not a blocker, a real boundary case for a future round): `looks_like_prose_string`'s ≥2-alphabetic-token threshold silently skips the common "word + bare placeholder" shape (e.g. `"Vlaue: {}"`) because tokens with no alphabetic character (`"{}"`) don't count toward the threshold — this exact shape already exists throughout awl's own `src/` (`actions.rs`, `debug.rs`, `app/apply.rs`, `app/files.rs`: `"wrote {}"`, `"budget {}"`, etc.), so a typo in one of those message strings would go permanently unflagged under the new gate; the round's regression test only covers a full multi-word sentence, not this boundary.
- **(1) blur backdrop stale on page-width change** — `02901b3`: `blur_signature` now folds page/wrap geometry into its key, so reopening Cmd-P after a width change re-renders the CURRENT column instead of serving the cached frosted backdrop. Live-verified: narrowing via `C-x {` then reopening the palette shows 96% of sampled backdrop pixels differing between the two palette-open frames (proves a fresh render, not a stale copy).
- **(2) pointer-hide anchor at typing start** — `f02f270`: the 3s countdown now anchors on the FIRST key of a typing burst (`Visible→Armed`, guarded by `armed_at.is_none()`) instead of re-stamping on every key; a mouse move still resets to `Visible` and the next keystroke re-anchors fresh. All 8 `pointer_hide::tests` green.
- **(3) samples above the fold** — `29e4d98`: every `samples/syntax/<lang>` gallery file restructured so a `Definition` span lands within the first ~22 raw source lines (≤17 wrap-aware visual rows on the 1200×800 canvas) — confirmed across all 20 sidecars. NIT (code review, left as-is — a taste call, not a bug): `samples/syntax/json.json`'s commented-out-code example was downgraded from a multi-line block comment to a single-line one, so the gallery no longer visually demonstrates block-style `CommentCode` classification; no test depends on it.
- **(4) dark-world Definition tint imperceptible** — `bf3a55b`: raised dark-world Def presence (deepened `T_DARK[0]` + `S_FG_DARK`) plus a new perceptibility law test; Currawong Def now redmean ≈96 from plain ink (was ≈43, under the 40-floor law). Side effect (expected, not a bug — flagged for taste): `S_FG_DARK` is a role-shared constant, so Str/Constant also re-saturate slightly on every dark world, not just Definition; light worlds (`T_LIGHT`/`S_FG_LIGHT`) are provably untouched (diff-confirmed, only the two dark constants changed).
- **project restore (sticky project)** — `8fa8151`: the active project root (`C-x p`) now persists as a sticky config pref; a bare relaunch restores it. Live-verified round-trip: a config with `project_root` set restores that exact root in the sidecar on a no-`--root` launch; an explicit `--root` still overrides it.

## 🔜 QUEUED (user, 2026-07-04)
_(all 5 items below have landed — see ✅ LANDED above; section kept empty pending the next round)_

## 🔜 Queued / running  — FEATURE QUEUE DRAINED ✅
- ~~thesaurus~~ DROPPED
- which-key: prefix (C-x) + 500ms pause → panel of catalog-derived continuations; button-free, idle-safe — `5dce9c0`
- gpu-harden: blur drop-before-alloc + doc-capture cap (3200px) + live gpu:N MB debug line (mac Metal); NO leak found — defensive — `f86fa6e`
- decompose-finish: extracted render.rs+actions.rs test modules — actions 1657→498, render 3520→1739 (GPU-core floor) — `add74ca`
- ~~decompose-finish~~ [done] — extract render.rs + actions.rs inline test modules (byte-identical) → actions ~500, render ~1740. Last of the decomposition.
- **chrome round 2** [after decomposition] — blur backdrop, debug mode (zoom + md/syn), amber-% fix.
- **symbols + ornaments** [after decomposition] — RESEARCH-DECIDED: add `add_symbol_spans` (mirrors `add_cjk_spans` seam) + bundle a subset "awl Symbols" face from DejaVu Sans Mono (in-repo, PD; has ⌘⇧⌥⌃ ❧ ❦ † ‡). Fixes modifier glyphs + alignment + ornaments in ALL 14 worlds at once, no per-theme work, no font swap, web-correct. Theme `hr_ornament`/`end_mark` fields (default ❧ / ❦ — DejaVu-covered, skip the ⁂ gap). Fleuron hr-rules + end-of-doc mark.
- **responsive layout** [RUNNING ∥ symbols] — min_inner_size + responsive page column.

- **HUD blur + trim** [with the chrome/panel pass] — route the Cmd-I stats HUD through the BlurBackdrop (was still grey-scrim); keep it, trim to writer stats (words · read time · % through doc), drop file-created/session-time.
- **caret-preview panel** [after fonts-ornaments + zoom-page land] — NEW independent-panel primitive (summoned discrete box, reusable). The caret-style picker keeps its familiar list; below it a ~2-line preview box runs a choreographed loop: type out "watch me glide, jump, and morph" → back×5/fwd×5 → C-a/C-e (+boundary recoil) → backspace×3 + C-k (delete/gulp) → rest → loop. Driven by apply_core + a scripted key-timeline. Live-only feel; settled capture deterministic. Touches overlay/chrome/caret-preview.

- **all pickers left-click-selectable** [with panel/overlay work] — hover-highlight + click-to-pick across ALL overlays (palette/go-to/theme/keybindings/spell), not keyboard-only. Approachability.
- **spell-correction panel** [after the panel primitive] — contextual panel AT the misspelled word; right-click opens, left-click a suggestion applies; establishes the word→alternatives→replace pattern the thesaurus reuses.

- **small polish** [fold into next chrome pass] — bump ornament glyph size (breaks look a bit small); remove the `eprintln!("page width…")` stderr chatter on C-x {/}; note: zoom-decouple makes the DEFAULT (0.8) page a bit wider — settable/sticky now so tunable, but may want a nicer default width.

- **markdown lists** [PENDING your confirm on defaults] — nested bullet glyphs by depth (•→◦→▪, reveal-on-cursor); Tab/Shift-Tab indent/outdent (2-space, list-only; ordered lists indent no-renumber); Enter-continues-list. Touches render (bullet glyphs) + apply_core/keymap.

- M-d = delete-word-forward (+ ∂ resolves, row_x_span hardening) — `796ea8c` — bind M-d → new DeleteWordForward (mirror delete_word_backward); ALSO makes the composed ∂ resolve to a real action. Fixes the 'Option-D does nothing' gap. (+ optional hardening: make render/geometry.rs row_x_span use .get() — belt-and-suspenders, all callers currently safe.)
- **CRASH (Option-D) — UNPINNED**: awl's reachable code is clean (∂→no-op, 755 tests, all panic-sites audited). If real, it's live-only outside awl (winit macOS IME / non-US dead-key layout / GPU). NEEDS: the panic backtrace + keyboard layout to localize.

- **Cmd-A select-all** [RUNNING ∥ writing-nits]
- **writing nits** [RUNNING ∥ Cmd-A] — quiet muted STRAIGHT underline (distinct from spell's wavy red), flag GENUINE MISTAKES only: multiple spaces between words, space-before-punctuation, trailing whitespace (EXCEPT md 2-space hard-break). SKIP intentional/stylistic (!!!, ???, multiple blank lines, quote style). Toggleable (palette command, sticky). NO full grammar (out of SCOPE). Style-nits (passive/wordy) banked separately.

- **GPU OOM investigation** [RUNNING diagnostic] — wgpu out-of-memory (user, old build). Prime suspects: blur backdrop textures re-allocated/not-freed per overlay-open/resize; glyph atlas unbounded growth on theme switches. Diagnostic measures GPU-region footprint (footprint/vmmap) across stress cycles on CURRENT main. FIX (when pinpointed) will ALSO add a live `gpu: N MB` line to the DEBUG readout (Metal currentAllocatedSize via wgpu-hal; mac-gated, others show —) so the leak is observable + fixes verifiable.

- panel-polish: spell panel fits widest suggestion (no short-word overflow) + click-away-dismiss (general) + ↵⇥ hint glyphs — `5c402e1`

## 🔜 IN FLIGHT / NEXT (post-drain, user-driven 2026-07-02)
- ✅ **frame-profile round LANDED — 60fps REACHED, 4× headroom** — `256b294`. The flamechart (--bench-frame, src/render/framebench.rs, per-stage median ms @2910×1720 with real spell load) CONVICTED: spell-squiggle rect build = 23.2ms = **80.4%** of the 28.8ms frame (visual_rows per misspelling per frame — the pre-fix ornament pattern; CLAUDE.md has 155 squiggles); nits rect build another 1–3.9ms (O(doc) rescan/frame). EXONERATED: the whole-buffer glyphon prepare = **0.46ms** → the "drastic" full_shape_height/windowing cull would buy ~0.4ms (~2% of budget) — the data says NEVER do it (keystone officially closed as unnecessary). FIX: proto-cache + visible-cull for squiggles + nits (keyed on RowGeom generation + spell/text version). RESULT: CLAUDE.md 28.9→**3.8ms** (7.3×), CAPTURE.md 12.1→**3.5ms** — both ~4× under the 16.6ms budget. Independently verified 3 runs/side; 31 byte-identical capture scenarios; 856 tests.
- ✅ **caret-sliver fix LANDED** — `21547eb`. Block caret collapsed to a 1px sliver on the SPACE at a soft-wrap boundary (user screenshots). THREE root causes: (1) cosmic-text collapses the wrap space → degenerate xs cell → advance=max(0,1)=1px; FIX in col_x_and_advance: mid-line cell under 0.1×char_width falls back to char_width (blast radius provably caret-only — motion/hit-test read raw xs); (2) font_is_mono only listed "IBM Plex Mono" (8-world era) → JetBrains/Monaspace worlds had silently lost the block floor AND the documented Block auto-default — list fixed; (3) caret_block_w keyed on theme font not the SHAPED face → code buffers in serif worlds now caret correctly (doc_family seam). Byte-identical except the 3 slivered captures; motion sweep green; 857 tests.
- ✅ **honest debug readout v2 LANDED** — `ad1909e`, 865 tests. The pane schedules ZERO frames (keep_hot no longer pins on debug; a one-shot `still ·` stamp then full silence); lines: `frame N.N ms · worst N.N · budget NN.N` (prev-frame cost excl. Fifo acquire wait; adaptive budget per monitor; textual `· over` past budget) / `key→px N.N ms` (dispatch→present-return, keys+mouse, first-unrendered-input) / `redraws N` (monotonic, FROZEN while idle = the hot-loop detector). Sidecar debug block gains machine-readable perf fields (null + still:true in capture); schema /64-/66. LIVE-CONFIRM for user: ms ticking, the still stamp, the frozen counter, key→px.
- ✅ **stuck-hunt LANDED** — `71a0f37`, 869 tests. H1 motion fixed point: NO, decisively (742 width/DPI combos over CLAUDE.md + the exact bullet/bold fixture, both directions, zero stuck walks — pinned by 2 new sweep tests). H2: **REAL BUG FOUND+FIXED — TextPipeline::set_size never invalidated row_geom**: every window RESIZE / page TOGGLE / PageWider-Narrower / page-edge DRAG left scroll-follow, hit-tests, max_scroll, doc_top and the caret row answering from PRE-RESIZE geometry until the next text edit (live-only by construction — captures size before text, replay can never see it; probe: 24 stale vs 50 true rows after 1200→600). The held-repeat/pure-move short-circuit seam itself proven clean (+ regression test). H3: no build window sticks going DOWN; user build predates the sliver fix so the caret can LOOK dead at a wrap boundary. USER FOLLOW-UP: rebuild at ≥71a0f37; if it still sticks — did you resize/drag the page column earlier that session? and does `ln` climb while the caret sits still?
- ✅ **test round LANDED** — `a6e28de`, 895 tests (+26). Autosnapshot-in-git FIXED (record_periodic bypasses only the git gate; save-hook still defers); NaN-zoom guards at all 3 seams (base binary ACCEPTED nan and wrote a poisoned frame — fix proven real); CRLF divergence characterized (normalize-on-load still the user's call); undo-roundtrip invariant; shift=true selection + is_motion completeness (Shift-PageUp/Down documented non-movers — possible follow-up); catalog↔keymap sweep; spellunderline grow test; UnderlineCache invalidation; code-mono reshape; double-capture determinism. FLAKE TRULY FIXED: 19 render tests read page globals without page::TEST_LOCK — all now take both locks; suite PARALLEL-STABLE (3× green acceptance; CLAUDE.md attribution corrected). Product observations parked: history timeline read-model prefers git_list (periodic awl snapshots inside a repo don't surface in the timeline — separate question).
- ✅ **file-open "doesn't appear" FIXED** — `c8be50d`, 898 tests. Root cause: the perf-round rope-clone cache (sync_text_cache) keyed by buffer.version() ALONE — a freshly-opened Buffer restarts at version 0, colliding with an un-edited old buffer (also v0) → sync_view served the OLD document's text; redraw painted stale content until the first keystroke. Headless immune (captures rebuild text per frame) — hence the clean replays. Fix: clear the cache at both live swap sites (load_path, new_note) + extract view_text for GPU-less unit tests; regression test failed pre-fix. LESSON (cache-key discipline): a cache keyed by buffer VERSION must also key by buffer IDENTITY (or be cleared on swap) — version numbers restart across buffer swaps.
- ✅ **theme-burst LANDED** — `82527ec`, 899 tests. MEASUREMENT CONFESSION: the old "~5ms theme reshape" bench number was an ARTIFACT — it faked shaped_font while the active face stayed the same, so cosmic-text's set_attrs_list equality no-op'd and nothing reshaped (corrected bench: a REAL family change = ~26-33ms release, 300-650ms DEV — the user runs dev `cargo run`, hence their 230.9ms worst). Atlas-trim thrash ACQUITTED with data (glyphon trim only clears the in-use set; LRU eviction never fired); resolve_cjk ~2µs. FIX: sync_theme split into sync_theme_colors (O(1) retints, applied INSTANTLY on every picker arrow/hover/filter/lens-move) + sync_theme_font (the reshape, DEFERRED behind a 150ms debounce consumed in about_to_wait — the zoom-persist WaitUntil pattern, no hot loop); Enter/Esc/click-away retint synchronously + cancel any pending deferral. AFTER: arrow step 2-7ms release / 5-55ms dev (was 40-60 / 400-1000); settle pays ONE ~29ms reshape. Byte-identical captures (replay applies fonts synchronously). LIVE-CONFIRM (user): colors track arrows instantly, glyph SHAPES snap in ~150ms after pausing — does it read calm? Tunable: THEME_FONT_DEBOUNCE in src/app.rs. NOTE for user: daily-driving `cargo run --release` gives the true feel (~10-20× faster frames than dev). — the greenlit audit top-10: LIVE-BUG FIXES (autosnapshot-in-git contract via record_periodic; NaN-zoom finite guards at clamp_zoom/--zoom/config) + CRLF characterization (buffer AND render halves; normalize-on-load decision DEFERRED to user) + undo/redo roundtrip invariant + shift=true selection + catalog↔keymap sweep + spellunderline growth (the pipeline that actually crashed) + proto-cache invalidation + code-mono reshape + double-capture determinism + THE REAL FLAKE FIX (theme_font_switch test reads page globals without page::TEST_LOCK — not a font-cache race; CLAUDE.md attribution corrected; acceptance = 3 consecutive green PARALLEL runs). Backlog: ~35 lower-priority audit items (full list in the wkgl2bz3p output) for later rounds; atlas-VRAM soak PARKED. Kills the fps line + the hot-loop pin (app.rs:954 keep_hot loses `|| debug_on()` — the pane stops creating the work it measures). Four honest metrics: (1) `frame N ms · worst M · budget 16.6` — prev-frame COST (prepare+encode+submit, EXCLUDING the Fifo acquire wait = pacing not work, per PresentMon's busy/wait split), `· over` flag past budget; (2) worst-recent = max over last 120 drawn frames (sparse-frame analog of 1%-lows); (3) `key→px N ms` — Zed's definition wholesale: stamp at event dispatch, end at present-submission, first-unrendered-input-per-frame = worst case under coalescing; (4) `redraws N` monotonic + `still ·` prefix when settled — one stamp frame then ZERO redraws; a climbing count while idle = hot-loop bug signal (the VS-Code-#22900 detector). Capture renders numberless placeholders (existing fps-placeholder pattern), schema bump. DECISIONS LOCKED (user 2026-07-02): over-budget = textual `· over` · key→px SHIPS in v1, keys+mouse · budget = ADAPTIVE per monitor refresh (16.6 @60 / 8.3 @120, via winit) · NO sparkline v1. REFRAME (user): the pane is DIAGNOSTIC INFRASTRUCTURE FOR THE AGENT, not a user feature — the user only needs "is there an issue"; the workflow is screenshot→agent triage→headless repro. So: keep the pane dense with triage signal, simplest styling, and ALSO emit the v2 stats into the capture SIDECAR as machine-readable JSON (a `perf` block: frame_ms/worst/budget/key_px/redraws) so headless runs hand the agent the same data programmatically. The deep diagnostics live machine-side: --bench-frame (per-stage), --bench-perf, the soak pattern.

## 🔜 NEXT ARC — "awl as a real Mac citizen" (user-approved direction 2026-07-02; FIRES AFTER the autosave round lands — same core files)
Sequence (each its own round): 
1. **MULTI-BUFFER CORE** (the load-bearing refactor): N open buffers, each with own cursor/scroll/undo/history state, one active; switch preserves everything. Everything below hangs off this.
2. **SINGLETON + CLI HANDOFF**: bind a Unix socket (~/Library/Application Support/awl/awl.sock) as the single-instance lock; later `awl file` connects → sends open /abs/path → exits (ms-fast); stale socket = connect-refused → delete + become the instance; in-app listener thread posts into winit via EventLoopProxy::send_event (event-driven, idle stays 0%) → open buffer + raise window. `--wait` flag (blocks until buffer closes) = EDITOR=awl works for git. CAPTURE GATE: --screenshot/bench flags ALWAYS standalone, never hand off. cfg-gated native-only (wasm unaffected).
3. **SESSION RESTORE**: relaunch reopens last files + window frame (dovetails with the autosave round's scratch stash).
4. **TAB STRIP + SELECTOR** (philosophy amendment, like syntax highlighting — "meeting the platform is the philosophy"): in-canvas, value-based tab strip that EXISTS ONLY when 2+ buffers are open (one file = today's chromeless awl); Cmd-Shift-[/] cycle + summoned buffer switcher + click-a-tab (pointing, not action-buttons); PHILOSOPHY.md "no tab strip" line amended. DEFAULT PROPOSED to user (auto-hide at 1 buffer) — adjustable before build.

## ✅ LANDED 2026-07-04 (the whole in-flight set; main @ e3a37b9, 969 tests, pushed)
- **autosave round** — `8c63e48`: autosave default-ON (idle ~1s/blur/switch/quit, atomic writes, clobber guard + calm notice), scratch persists across quits, git files untouched version-wise (timeline = git log), session snapshots + AGED RETENTION LADDER (prune resolution not memory; oldest always survives), History = live preview in the document (Esc = exact present, Enter = undoable restore) + when/which rows (commit subject / edited-§heading / clock-suffix disambiguation). Adversarially verified: headless is provably stash-blind + autosave-free (sandbox tree-diff empty). NITS parked: ladder cap can transiently overshoot under clock rewind (self-heals, documented); .git-FILE worktrees read as loose (follow-up); colliding-row clock suffix is UTC (taste call).
- **morph rounds** — `94d7c43` + `74de730`: morph inhabits the char BEFORE the insertion point; col-0/empty = the I-beam bar (melt/snap); EDIT-MOVES SNAP, navigation glides (all looks).
- **picker RowLayout** — `dca9cbf`: one layout owner + no-wildcard law test; root cause was a cross-face width-budget bug firing at EVERY width.
- **alabaster-true** — `e3a37b9`: per-world quiet role hues + washes (14×4 tint table verified hex-for-hex + on-pixel; min pairwise redmean 51.6; ≥38° from every world's primary), COMMENTS PROMINENT (prose = full ink + warm wash; commented-out code = muted via looks_like_code, 1.1% false-grey on this repo's own corpus), spell scoped to comments+strings in code buffers, DESIGN §3 amended, schema /67 (shared with autosave's preview_id — both landed in one push).
- ⚠️ MERGE LESSONS: gate on the merge EXIT CODE, not piped tail; never delete remote branches before push-verification (autosave branches were deleted prematurely — recovered via FETCH_HEAD).

## ☀️ LIVE-REVIEW LIST (user, in --release)
1. TYPING in morph: snap + cross-fade + impact, no slide. 2. C-a melt-to-bar, type to snap back. 3. Idle autosave (~1s) + 0% CPU after; blur/quit flushes. 4. Clobber notice on an external edit; Cmd-S clears. 5. Scratch survives relaunch. 6. History: arrow rows = the doc time-travels; Esc = exact present. 7. CODE LOOK: role hues across worlds — esp. the dark-world Def/Const pastel pair (subtlest legal pair) + the warm comment wash on Currawong OLED; washes read a step stronger on-screen than the table hexes (GPU blends linear). role_overrides in theme.rs = the per-world escape hatch. **REFRESHED (2026-07-04, `c8b1cf1` merged + pushed):** the LIGHT worlds now carry the SECOND retune's tints — `T_LIGHT=[0.76,0.78,0.80]`/`S_FG_LIGHT=0.18` (down from round 1's `[0.84,0.90,0.94]`/`0.28`), specifically to fix Saltpan reading "too hard to read" against its pale ground (a new WCAG ground-contrast law now guards this — see THEMES.md). This is the taste-gate-passed live-eyeball round; re-confirm on your own screen that Saltpan/Quokka/Magpie/Gumtree now read comfortably (not just Potoroo, which already passed) and that no light world lost its role distinguishability along the way. Dark worlds are unchanged by this round. 8. UTC clock-suffix taste call.
9. **Dictionary default = en_US** — the picker ships all three (en_AU/en_GB/en_US) but defaults to en_US; Frank wants en_AU personally, so flip the sticky pref once (Cmd-P → Dictionary → en (Australia)) and it persists — a one-time action, not a code change needed unless you'd rather the DEFAULT itself be en_AU.
10. **SETTLED (2026-07-04, pointer-hide v2):** the 3s-countdown-vs-native question below resolved in favor of native — the pointer now vanishes on the FIRST keystroke and returns on any mouse nudge (`setHiddenUntilMouseMoves`), countdown machinery deleted outright. Remaining live-only check: confirm the immediate hide reads calm rather than twitchy in real typing, and that a window blur reliably shows the pointer again on a real focus change.
11. **Page-edge drag readout has no band hint** — the live muted char-count readout while dragging (e.g. `68`) ships bare; the spec offered an optional faint hint at the Butterick 45–90 sweet-spot band edges as a "keep calm" taste call, and it was left OUT (readout-only, no band markers). Confirm whether the band hint is wanted or the bare number is preferred.
12. **Debug-panel autosave wording** — the four states read `"autosave saved · Ns ago"` / `"autosave on"` / `"autosave held — disk changed"` / `"autosave off"`; confirm the exact phrasing reads well against the other debug-pane lines (frame/key→px/redraws/gpu) — it's diagnostic-pane prose, easy to bikeshed, worth one glance.
13. **SUPERSEDED (2026-07-04, pointer-hide v2)** — the anchor-at-typing-start countdown this item asked about is gone; see item 10 above (now hides on the first keystroke, native-convention, no countdown to anchor).
14. **Dark-world Definition presence (taste)** — Currawong (and other dark worlds) now show a clearly visible blue Definition tint (redmean ≈96 from plain ink, up from ≈43); confirm it reads as "clearly visible, still quiet" and not too loud, and that the correlated slight re-saturation of Str/Constant (shared `S_FG_DARK` constant) still looks like one coherent quiet family rather than a mismatch.
15. **Blur backdrop after a width change** — reopening a picker (Cmd-P/theme/etc.) after resizing the page column (`C-x {`/`C-x }` or an edge-drag) now re-renders the frosted backdrop against the CURRENT column instead of a stale cached one; confirm it looks right live (no flash/stutter) across a few resize→reopen cycles.
16. **Project restore on relaunch** — the active project root (`C-x p`) now persists and restores on a bare relaunch (like the existing scratch-buffer restore); confirm this matches the intended feel ("reopen and I'm back where I was") and that switching projects then relaunching lands you in the LAST one, not the first.
17. **Zoom-on-heading caret placement** — zoom in/out on a buffer with `##` headings; confirm the amber block caret now tracks the heading glyph row exactly (no floating offset above the row) through a zoom sequence and across repeated zoom in/out. The harness proved this at the pure row-geom unit seam (structurally can't repro live-only bugs of this shape in a capture — see the LANDED note) so a real-window eyeball is the only remaining check.
18. **Spell toggle feel** — Cmd-P → "Toggle Spellcheck" should flip squiggles off/on instantly across the whole document (code + prose); confirm the palette label/wording reads well and the toggle feels immediate, not laggy, on a long file.
19. **Single-token strings no longer squiggling** — a code string like `"struct"`/`"en_AU"`/a lone identifier should NEVER show a spell squiggle now, while a genuine prose string (`"this has a typo teh"`) still does; confirm this reads right on your own code and doesn't feel like it's hiding real typos (the one open nit: a short "word + `{}`" format-string typo, e.g. `"Vlaue: {}"`, still won't flag — a real gap, banked for a future round, not expected to bite often).
20. **Crash gone: focus + select-all + type** — open a longish file, toggle focus mode (`C-x d`), Cmd-A select-all, type a replacement char; awl must not crash (this used to abort with a stale-index panic). Harness confirmed the exact repro is now clean; the live eyeball is just "does it feel/read fine", not a crash hunt.
21. **Focus-mode click/drag feel** — with focus mode on, press-and-drag near the top/bottom of the view; the typewriter auto-recenter should no longer fight the drag (no more "scroll really quickly" runaway). Also: shift-click should extend the selection from the cursor's prior position (but see nit above — a shift-click landing right after a very recent click may instead word/line-select; try both a fresh shift-click and a rapid double-shift-click to feel the difference).
22. **Morph preview demo** — Cmd-P → Keybindings/caret-style picker → highlight Morph; the little choreographed preview panel below the list should now show the actual glyph silhouette morphing, not a blank/wrong shape.
23. **Caret-line nit quiet + double-space nit** — type a sentence with a stray double space or trailing whitespace; nits should stay silent on the line your caret is CURRENTLY on and only light up once you move off it; spell squiggles, by contrast, should only go quiet on the one word at the caret, not the whole line — confirm this split feels right. Separately, type `"word  word"` (two spaces) in prose and confirm the double-space nit now fires (previously silent). NOTE: a markdown table row's alignment padding will currently ALSO false-positive this rule (banked below) — expected for now, not a regression to chase live.
24. **Picker freshness** — with a project open, drop a new file into the project directory from Finder/terminal, then summon go-to-file (`C-x g`/similar); the new file should appear without needing to reopen awl or the picker's containing window.
25. **Enter + boundary-bump juice** — press Enter a few times and confirm the caret's landing on the new line now has a small felt "settle" (not a slide/reflow — rows must stay still). Then run the caret into a wall on purpose (C-a twice at line start, C-e twice at line end, Home/End at buffer edges) and confirm a quiet bump/recoil plays instead of dead silence — should read as "acknowledged, can't go further," never as an error flash.
26. **Pointer hides on first keystroke** — start typing anywhere in the document; the OS pointer should vanish on the very FIRST character (not after a delay), any mouse nudge brings it back instantly, and switching windows (blur) should also bring it back. NOTE (nit above): typing via an IME (e.g. composing Japanese) currently does NOT hide the pointer — confirm whether that's fine or should also hide.
27. **`==highlight==` look** — type `this is ==marked text==` in a markdown/scratch buffer; the marked span should render with a warm highlighter-style wash behind full-ink text, while the `==` marker characters themselves recede to the muted markup color (not full ink, not amber).
28. **Markdown table double-space false positive (known, not fixed this round)** — typing an aligned markdown table (`| Name  | Value |`) will currently show the double-space nit on the padding between cells; this is a known pre-existing gap in `nits.rs` (whitespace-shaped rule, no table awareness) surfaced by this round's adversarial review — confirm how annoying it is in practice; a follow-up table-row exemption is banked below.
29. **WYSIWYG reveal/conceal feel (merged `f8180c8`, harness can't judge motion)** — open a markdown file with headings/bold/italic/inline-code/`==highlight==`/fences and move the caret line to line: confirm the reveal-on-cursor swap (raw markup ↔ styled preview) reads as an intentional, calm "look, it's the same text" moment rather than a flicker/pop — this is the core Obsidian-live-preview-style feel the harness only proved state-correct, not felt-right.
30. **WYSIWYG wrap-shift on entering/leaving a styled line** — since concealing markup shortens a line's rendered width, moving the caret onto a heading/bold/code line changes that line's width (markup reveals → line grows) and moving off shrinks it back; confirm this "micro-jitter" (the documented, accepted cost — see the WYSIWYG queue note) doesn't feel distracting in real typing/navigation, especially on a long wrapped line or when arrowing quickly through several styled lines in a row.
31. **WYSIWYG panel/pill look per world** — open the same fenced-code + inline-code fixture across a few worlds (esp. a light world like Saltpan/Quokka and a dark world like Currawong) and confirm the `base_200` value-step panel/pill reads as a quiet, present "surface" — not invisible (too close to `base_100`), not loud (too close to `primary`/amber) — on every world, not just the one used for taste-gating during the build. `CODE_PILL_INSET_X/Y` and `FENCE_PANEL_INSET_X` are flagged taste defaults in the code, not confirmed live per-world.
32. **`wysiwyg = false` escape hatch** — set `wysiwyg = false` in `~/.config/awl/config.toml`, relaunch, and confirm markdown renders exactly as it did before this round (raw `#`/`**`/`` ` ``/`==`/fence markers always visible, no panel/pill, no conceal ever) — the merge train byte-compared this against a pre-merge build and found zero pixel difference, but a live eyeball on your own config/theme is still worth one glance since the harness can only check the fixtures it was given.
33. **JP look: bundled Noto replaced Hiragino everywhere (japanese round, `9bb8ca6`)** — open `samples/japanese.md` (or any JP text) under a serif world (Undertow/Gumtree/Saltpan/Bilby/Outback/Magpie → mincho) and a sans world (Currawong/others → gothic); the kanji/kana now render in the BUNDLED Noto Serif JP / Noto Sans JP face on every machine, not whichever system Hiragino/Noto-CJK happened to be installed. Confirm it reads as good as (or better than) the system Hiragino face you're used to — weight, spacing, how it sits next to the Latin text on the same line. **If it reads worse live, say so — revert is cheap:** a one-line candidate reorder in `theme.rs`'s `CJK_MINCHO`/`CJK_GOTHIC` (put the system Hiragino/Noto-CJK entries back first; the bundled Noto faces stay registered either way and the system candidates already trail, never removed, so nothing else has to change).
34. **Kana→kanji IME writing session (japanese round, unconfirmed since the original brief)** — a real IME composition session: type kana, watch it convert to kanji candidates, commit a conversion, and confirm the preedit renders sensibly, the candidate window behaves, and the caret never does anything weird mid-composition (jumps, disappears, misplaced block/morph shape). This is genuinely LIVE-ONLY — the headless harness has no IME to drive — so it's still an open confirm from the original japanese-round brief, not something this merge could verify.

## 🔄 IN FLIGHT (2026-07-04)
- **autosave round** [wf_99afb85b: plan→engine→UX→verify] — autosave default-on + scratch persistence + git-files-untouched + session ladder + live-preview timeline.
- **picker RowLayout** [agent] — the narrow-width overlap bug fixed as a SYSTEM: RowLayout primitive (Primary never dropped/Secondary yields), private bypass, no-wildcard law test over OverlayKind, responsive card. (User principle: similar components must share behavior structurally, never case-by-case.)
- **morph-anchor** [agent] — morph caret inhabits the char BEFORE the insertion point (the glyph you just typed); block/ibeam unchanged; line-start fallback = current behavior.
- **alabaster-true** [wf_679f8b5f: plan→build→verify] — tonsky essay absorbed (user approved both calls): per-world quiet role HUES (strings→green-lean, constants→violet, definitions→blue, comments→warm; derived from each world's palette, pairwise-distinguishable = the MEMORY TEST as a law test) + background WASHES on dark worlds; COMMENTS INVERTED to prominent (prose comments full ink; commented-out code stays grey via a light heuristic); spell-check scoped to comments+strings in code buffers (no identifier squiggles); DESIGN §3 one-line amendment (quiet role hues on code surfaces; amber stays the caret's alone); syn_role_color = the single role-style owner (fences inherit). Morning review: the 14-world × 4-role tint table lands with the verdict.

## 🏦 Banked (designed, not yet building)
- **markdown table double-space false-positive** (review nit, 2026-07-04 evening round — pre-existing, not introduced this round): `nits::line_nits` flags the interior alignment padding in a markdown table row (`"| Name  | Value |"`) since the double-space rule is purely whitespace-shaped with no table awareness, and this round's code-buffer prose-scoping only exempts recognized CODE buffers, not markdown table rows in a prose/markdown buffer. Needs a table-row exemption in `nits.rs` or the render-side prose scope.
- **pointer-hide v2 skips IME composition** (review nit, 2026-07-04 evening round, undisclosed carve-out): the preedit guard in `app.rs`'s `WindowEvent::KeyboardInput` handler returns BEFORE the pointer-hide call, and composed IME characters land via `WindowEvent::Ime` which never calls `pointer_hide` either — so typing through an active IME (kana→kanji, etc.) never hides the OS pointer, while plain Latin keystrokes hide it immediately. Plausibly a deliberate call (a candidate window may want the pointer visible) but reads as unstated rather than decided; needs a one-line doc note or a confirm either way.
- **shift-click vs the multi-click counter** (review nit, 2026-07-04 evening round, taste call pinned by a test): `bump_click_count` keys purely on time+pixel proximity, never on the Shift modifier, so a shift-click landing within the multi-click window falls into the SAME double/triple-click word/line-select arms and silently drops any in-progress shift-extended selection rather than extending it by word (the common TextEdit/Xcode/browser "shift+double-click extends by word" convention). Pinned deliberate by `double_and_triple_click_arms_ignore_shift`; confirm intended or amend.
- **conscious version mark** (user-shaped): NOT forks — "I am about to make a major change, pin a version FIRST." A deliberate checkpoint command; its snapshots are the EXEMPT tier of the retention ladder (never auto-pruned). Slots into the autosave-round seam comment.
- **more theme worlds** (toward ~20, LATER): the 3 added fonts (iA Writer/Fraunces/Monaspace) first ENHANCE existing themes; brand-new worlds (Commit Mono, Hanken Grotesk, Departure Mono, etc.) come later.
- web CJK degrades to tofu (no browser system CJK) — bundle a Noto CJK subset OR document graceful degradation.
- text-selection tint audit (per-theme `selection` token — ensure tasteful value-ish tints, not saturated)
- **web demo** (after FS trait): wasm32 + OPFS sandboxed storage, scratch-buffer playground — wgpu/winit/glyphon already web-ready
- sound layer (audio engine + event hooks + default-OFF toggle; assets TBD — CC0/synth)
- theme "oomph" pass (distinctive grounds per theme) → then grow roster toward ~20
- caret-juice live-feel tuning (after you've played with it)
- chore: drop the cosmetic `LayoutOracle` unused-import warning
- **FLAKY TEST (known — NOT a real failure):** `render::tests::theme_font_switch_reshapes_document` intermittently fails under PARALLEL `cargo test` load (system-font cache race); passes single-threaded / in isolation. Merge-gate uses a single-threaded retry. Chore: serialize it (a font-load lock / `#[serial]`) so it stops flaking. **SECOND INSTANCE (2026-07-04, merge train `c8b1cf1`):** `capture::tests::double_capture_is_byte_identical` failed once under full parallel `cargo test` (1050 passed/1 failed), passed in isolation immediately, and passed clean on a full rerun (1051/0) — same "parallel run, passes alone" shape as the theme-font flake above, though this test already takes 7 TEST_LOCKs (theme/page/caret/focus/nits/debug/hud) so the race is in some OTHER shared global not yet covered by those locks. Not merge-introduced (test predates the merge, last touched by `cd4bcd9`). Worth folding into the same serialize-flaky-tests chore rather than chasing as a one-off.

## 🏁 CAPSTONE — ✅ DONE (audit → user greenlit → cleanup landed `2b76c76`)
- 3 read-only reviewers → synthesis; verdict: awl largely coherent + lean; signal concentrated in stale docs (syntax/which-key outran spec after a mid-build direction change) + 2 markdown one-amber violations + clipboard/select-all catalog gap + HUD elevation. ALL greenlit + landed.

## ✅ NEXT BATCH — DRAINED (all landed on main; only the keystone is deferred for you)
_Overnight autonomous drain complete. main @ `14b33ba`, 856 tests. Landed: crash-fix, perf round, motion-fix, picker fixes, line-measure, fence highlighting, narrow-margin, faceted themes menu, per-world mono + contrast, docs. DEFERRED: the keystone freeze-fix (see item 1 / MORNING REVIEW — perf already killed the freeze; the remaining full_shape_height cap is architecturally risky + a live-feel call)._

- docs pass: perf-measurement convention + known-flake note added to CLAUDE.md ("remember for future"); Theme::mono documented in Fonts; config.rs measure comments 80→70. — `14b33ba`

## 🔜 (original batch — all items now ✅ except the deferred keystone) — was main @ `854c272`
_Locked decisions: syntax highlighting officially IN scope (serves "light editing"; minimal value-based Alabaster, NOT IDE machinery) · binary ceiling <100 MB (now 13 MB) · font count gated by CURATION not bytes · no web artifacts._

1. **GPU 6 GB memory** [the recurring CRASH was SEPARATE — FIXED `23061b8`; the 6 GB is still OPEN, soak RUNNING]. IMPORTANT: the recurring PANIC/freeze the user kept hitting was a wgpu write_buffer OVERRUN (instance-buffer off-by-one in the selection + spell-underline pipelines) — NOT an out-of-memory — now fixed + pushed (the overrun was only 4656 bytes, so it does NOT explain 6 GB). The 6 GB MEMORY reading is a DISTINCT, still-open question (do not assume the crash fix resolves it). Full GPU-alloc audit found NO unbounded accumulation in awl's own textures: blur/doc-capture is viewport-bounded, 3200-capped (both dims), drop-before-allocate, NOT recreated on theme switch; every texture viewport-sized + single-instance; glyph atlas trim()'d every frame; zero create_texture on any summon/switch path. The ONE doc-scaled lever: full_shape_height() (text.rs:540) = lines×8 rows (~900k px on CAPTURE.md), re-shaped on EVERY theme font-change + zoom/DPI. SOAK RESULT (decisive NEGATIVE): 800 iters each of reshape / theme (727 whole-doc font-reshapes) / overlay / hud / baseline → ALL dead-flat ~5.5 MB, slope 0. The reshape/theme/atlas path is memory-EXONERATED. So the 6 GB (IF real + recurring) is in the interactive-only winit SURFACE/SWAPCHAIN/present path a headless device structurally can't exercise (surface.get_current_texture / present / configure under theme churn — a retained SurfaceTexture drawable or a reconfigure storm), OR was a transient tied to the now-fixed overrun crash. Caveat: the soak harness was built on a STALE base (I forgot to base-pin the Agent — my miss), so its CODE is throwaway (branch discarded); the finding still holds (reshape/atlas memory behavior is fundamental + the decomposition since was byte-identical). NEXT — only if 6 GB RECURS now the crash is fixed: (1) drop the gpu-readout into the LIVE App::redraw + rapid theme-switch a long file to confirm surface-path; (2) audit SurfaceTexture drop + the Outdated/Suboptimal/Lost reconfigure arms. Otherwise: likely crash-tied, leave it.

   ⚠️ **KEYSTONE — theme-preview reshape on long files (FREEZE + lag + likely the 6 GB OOM all converge here).** User FROZE awl by arrowing ↑/↓ through themes on a complicated long file. Root: the theme picker LIVE-PREVIEWS on every arrow via sync_theme, which on a font change RESHAPES THE WHOLE DOCUMENT at full_shape_height() = lines×8 rows (~900k px on a long file). ~14 worlds × one huge whole-doc reshape each, queued on the main thread → freeze. Same lever drives the fps lag AND is the GPU audit's prime OOM suspect (atlas/glyphon churn under repeated whole-doc reshape). FIX (ONE round, AFTER the perf round lands — both touch sync_theme): (1) DEBOUNCE preview — flip theme COLORS instantly per arrow (O(1)), DEFER the font reshape until the selection SETTLES ~N ms (or reshape on commit); keeps live-font preview when you pause, kills the per-arrow reshape storm. (2) CAP full_shape_height (text.rs:540) to visible+overscroll instead of lines×8 (medium risk — row-geom/scroll math reads it; validate). The --soak-gpu theme run (in flight) confirms whether bounding the reshape also stops the memory climb — if so, this ONE fix closes freeze + lag + OOM. NOTE: the perf round F4 only drops ONE redundant reshape (~2×); it does NOT by itself cure this freeze.

2. **PERF — viewport-window the per-frame O(doc) work** [both traces DONE; all simple, non-drastic]:
   - MOTION ORACLE (the settings ↑/↓ 40fps): visual_line_up/down/_start/_end/visual_x_of call visual_rows(line±1) → full-doc layout_runs walk per keypress (single-slot memo always cold-misses the destination). FIX: a line_rows_local(line) reading buffer.lines[line].layout_opt() (O(line)); point ONLY the motion oracle at it (render.rs:2030-2081; geometry.rs:654). Byte-identical via capture.
   - SCROLL: gate refresh_rule_conceal on cursor-line change (skip on pure scroll) — text.rs:428, focus.rs:41.
   - SCROLL: cull rule_marks/bullet_marks/rule_lines to VISIBLE row range + cache line-lists by text-version — rects.rs:26,71,106; layers.rs:297.
   - THEME: drop redundant buffer.set_text in sync_theme font branch (restyle_all_lines already reshapes) — render.rs:1480 (halves per-preview-step cost).
   - short-circuit whole-rope clone + shaped_key compare on pure move/scroll — viewstate.rs:39, text.rs:480,508.
   - RESIDUAL (LEAVE): whole-buffer glyphon prepare_text_layer O(doc)/frame — only removable via DRASTIC visible-window shaping cull. NOT doing.
   - watch: same-font palette switch skips restyle → stale md/syn colors; if adding recolor-without-reshape, handle it.

3. **Per-world code mono** — Theme::mono companion; prose keeps display face, CODE (syn_lang.is_some()) renders mono. Share monos by temperature (mono worlds reuse own face); ~5-6 distinct for ~14-20 worlds, mostly already bundled (Plex/JetBrains/Monaspace/Fira). Uses the per-run family-span seam (like add_cjk_spans). + subset the whales (EBGaramond 613K, MonaspaceXenon 800K → Latin+code-punct ~90-120K, OPTIONAL polish) + verify & remove dead ./fonts/ dir (nothing include_bytes!'s it).

4. **Alabaster contrast nudge** — syn_attrs roles base_content→muted at Definition 18% / Constant 34% / Str 52% (spans.rs:387-392); Str quite faint. Once code goes mono, re-evaluate + bump the value separation so code highlighting reads. Small.

5. **Faceted themes menu** — theme picker → lens-switcher. LENSES (←/→, "All" parked at the far end): Time (Dawn/Day/Dusk/Night) · Register (Humble/Everyday/Refined) · Voice (Literary/Technical/Modern) · Temperature (Warm/Cool/Neutral) · All (flat, search by name). Each world tagged per dimension; a lens = group-by-dimension with FAINT section headers; same world recurs across lenses. ONE lens at a time (switch, don't stack filters). Live preview on ↑/↓ (whole editor re-themes; ⏎ keep, esc revert) — needs the perf fix so preview is instant. Data-driven (tags + group-by), NOT nested menus. Keyboard-first (←→ lens, ↑↓ world, type filter, ⏎ keep, esc revert) + click-selectable. Toward ~20 worlds.

6. **Narrow-frame margin** — show a tad more background on the sides in NON-page mode. Knob TEXT_LEFT (render.rs:107) is coupled to PAGE_MIN_PAD (geometry.rs:115) → add a SEPARATE non-page inset constant so bumping it doesn't raise the page-collapse floor.

7. **Line-measure nudge** [APPROVED] — DEFAULT_MEASURE 80 → ~70 (Butterick sweet spot ~66; 80 is just past 3 alphabets). Update any page-geometry tests asserting 80. Small, sticky/settable already.

8. **Picker fixes** [bugs, from the file-picker screenshot] — a cluster on overlay.rs + render/chrome.rs + app.rs/input.rs:
   (a) HOVER AUTO-SCROLL (the real bug, corrected): hovering the mouse near the TOP (or bottom) edge of an open menu makes the LIST auto-scroll toward that edge RAPIDLY. Hover should ONLY highlight the row genuinely under the pointer among the visible rows — NEVER auto-scroll; hovering in the header/padding/outside the row band must not move selection or scroll. (Companion: give the wheel to the LIST + block doc scroll while an overlay is open — app.rs:718 currently scrolls the doc behind.)
   (b) OVERFLOW: the list caps at MAX_ROWS=12 LOGICAL items but sizes the card by item COUNT assuming 1 visual row/item; long paths WRAP to 2 rows → the card overflows (chrome.rs:417-422). Fixed for free by (c).
   (c) PATH TRUNCATION: single middle-ellipsis that PRESERVES the filename+extension (never the ext) + as much leading dir as fits → ONE line per item. Color the dir prefix MUTED + the filename CONTENT ink (figure/ground). Fuzzy-filter narrows in practice; truncation is the wide-view fallback.
   (d) RIGHT-CLICK RE-TARGET: right-clicking a SECOND misspelling while the first spell menu is open should DISMISS the first + OPEN the second word's menu. Currently the right-press is likely blocked/consumed while an overlay is open (same as left-click). FIX: a right-press always dismisses any open overlay, THEN hit-tests + fires OpenSpellSuggest on the new word (re-target). app.rs / app/input.rs on_right_press.

9. **Fenced-code syntax highlighting inside markdown** [APPROVED] — a ```lang fence in a .md should syntax-highlight its BODY by the info-string language (GitHub-style), composed with the markdown styling. markdown.rs already parses CodeBlock (Tag::CodeBlock(_) at :350) but ignores the info string; read CodeBlockKind::Fenced(lang) → map to syntax::Lang → lex the fence body → apply Alabaster syn spans over the body (the fences + info string stay muted Markup). Reuses the existing 20 lexers + the span seam. Same value-based roles, never amber. Note: this is per-FENCE lexing inside an md buffer, distinct from the whole-buffer syntax gating by file extension.

☀️ **MORNING REVIEW — autonomous judgment calls made while you slept (confirm / adjust):**
- _(accumulating — each round appends the taste-defaults it chose: truncation style, mono-per-world assignments, theme-family tags, contrast value, etc.)_
- **Picker truncation:** used the recommended single middle-ellipsis (filename+ext preserved) + muted-dir/content-filename coloring. Reversible if you want a different style.
- **Line-measure:** DEFAULT_MEASURE = 70 (from 80). Bump to 66 for Butterick-exact, or leave at 70.
- **Narrow-frame margin:** NONPAGE_INSET = 32px/side (was 16). Toward 40 = more ground; 24 = tighter.
- **Themes-menu lens tags** (world → Time / Register / Voice / Temperature — DERIVED defaults, adjust freely in theme.rs ThemeTags): Tawny → Night/Humble/Technical/Neutral · Mopoke → Dusk/Humble/Modern/Warm · Currawong → Night/Humble/Technical/Neutral · Potoroo → Dusk/Humble/Technical/Warm · Gumtree → Day/Refined/Literary/Cool · Bilby → Day/Refined/Literary/Cool · Saltpan → Dawn/Refined/Literary/Warm · Quokka → Dawn/Everyday/Modern/Warm · Undertow → Night/Refined/Literary/Cool · Kingfisher → Night/Everyday/Modern/Cool · Outback → Night/Everyday/Literary/Cool · Mangrove → Night/Humble/Technical/Cool · Galah → Dawn/Everyday/Modern/Warm · Magpie → Day/Everyday/Literary/Neutral. Judgment calls to eyeball: Mopoke Register (iA Quattro as Humble but Voice=Modern since it's sans-class); slab worlds Outback/Magpie Voice=Literary (slab is serif) but Register=Everyday; Literata (Gumtree)=Refined; Tawny/Currawong Temperature=Neutral. (Section spread skews Night-heavy — 6 worlds; may want to rebalance by re-tagging a few.)
- **KEYSTONE freeze-fix — DEFERRED for you (not done autonomously):** the perf round already cut the theme reshape 36ms→5ms, so the freeze is largely gone. The remaining piece — capping full_shape_height (text.rs) — was flagged by BOTH the perf trace and the GPU audit as architecturally risky (the scroll/row-geometry/hit-test model assumes the whole doc is shaped), and the preview-debounce timing is a live-FEEL call. Recommend we do this together, live, so you can confirm the scroll feel. Or skip it — 5ms preview is smooth for normal long files.

_PERF-MEASUREMENT is now a STANDING PRACTICE (user: "remember for future"): the bench harness (cargo bench / benches/perf.rs, landing with the perf round) records BEFORE/AFTER numbers on the hot paths; the soak harness (--soak-gpu) does the same for GPU memory. Persist both as a CLAUDE.md convention once they land._
