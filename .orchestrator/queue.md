# awl — live build queue

> Live execution state only. Completed and superseded work is in git history
> (`git log -p .orchestrator/queue.md`) and `.orchestrator/reports/`. Protocol,
> claiming, worktrees, and execution hygiene live in `.orchestrator/README.md`.

## Ready — current user-visible wave

61. **Repair word-run frost geometry around punctuation and long labels (Wave 4 pasted-16).** Keep the measured word-run performance class and the one continuous field across Mangrove + Firetail, outline + gutter, but derive each run halo from actual ink/advance geometry with bounded end padding and punctuation-aware radius. An isolated `&` must not become a circular bump; “Button-free” must not carry excessive frost after its final glyph. Do not identity-branch a glyph/label or silently restore the +31% per-glyph path. Verify punctuation + short/long runs at 50/100/200/300% zoom, natural nearby-run merging, both lava-world galleries, standing vision smoke, and `--bench-frost` in release; reject >5% steady-frame regression or steady mask rebuilds. After the implementer generates the real before/after lava captures, Fable inspects those images and returns the taste call on halo shape/padding only; Fable does not edit or implement, and the implementer applies the verdict. **DISPATCH-READY after correctness bugs.**

62. **Currawong stars gain about 10% more chroma and mild deterministic size variation—not more brightness (user-decided).** Raise saturation/chroma of the low-saturation blue-white/white/champagne palette by roughly 10% while preserving luminance, the quiet visibility ceiling, and the one-accent/amber guard. Add a small hash-derived size spread around the current size; no true randomness, density increase, or extra clock. A star keeps tint, size, phase, and position across frames. Verify deterministic identity, a named narrow/nonzero size distribution, lifecycle/contrast laws, fixed-phase before/after gallery, and live twinkle/Retina density. The implementer generates the fixed-phase stills; Fable only judges their chroma/size taste and returns a call, never implementation. Live timing/feel remains a human check because still images cannot witness it. **DISPATCH-READY.**

63. **Exact `- ` + Enter preserves an empty bullet instead of destroying it (user-decided).** Preserve the current marker line byte-semantically as an empty Markdown list item, move the caret to a new plain line below, and render the off-cursor empty bullet. Do not create a second bullet or trim the marker’s required trailing space. Keep the gesture one undo group; nonempty continuation and Enter on an already-blank line stay unchanged. Apply the same rule only to unordered marker siblings the existing parser already recognizes. Verify exact text/caret, undo/redo, CRLF restoration, document boundaries, and a WYSIWYG capture of the preserved bullet. **DISPATCH-READY.**

64. **Spell picker is a compact context menu: top five corrections plus an always-visible separated Add-to-dictionary action (user-decided).** Preserve dictionary ranking/order, show at most its first five corrections, then a fixed `Add '<word>' to dictionary` row. No scrolling, scrollbar, More/ellipsis button, submenu, or hidden sixth suggestion. Suggestions retain initial focus; Add is immediately keyboard/mouse reachable after the last correction, survives filtering/zero-suggestion states, renders fully at ordinary desktop width, and elides only at the real narrow cap. Verify 0/1/5/6/20-result corpora, filtering, arrows/Enter, click/hover, Pane/Bars, all anchors, and narrow width. Implement through the shared row/surface owner; do not add another parallel metadata array. **DISPATCH-READY; coordinate with item 54.**

65. **Fold affordance + Outline state taste correction (user-decided; follows item 58).** Put the expand chevron immediately left of the heading, outside editable text advance; reveal it only on hover or when that heading owns the caret. Make `… N lines` quiet but plainly readable and baseline-aligned. In the persistent Outline, keep each collapsed parent visible with a small state marker while suppressing hidden descendants; the marker is not a second toggle and Outline remains click-to-jump only. The implementer prepares the requested flat/lava/one-bit gallery across heading levels, lengths, and rest/caret/hover states; Fable inspects only those generated images and returns the affordance/contrast taste call, never code or implementation, then the implementer applies it. Pixel laws cover no overlap, a named tail contrast floor, parent retention, descendant suppression, and identical restoration after unfold. **DISPATCH after item 58.**

66. **Date picker examples use one coherent ink value (user-decided).** Remove mixed bright/dim characters within each example date. Selection may change the row surface and a separate format description may use secondary ink, but every character belonging to `2026-07-23`, `23/07/26`, etc. uses one uniform legible value. Preserve deterministic capture date, selected-format identity, and theme palette. Verify glyph sampling across every selected/unselected example plus Pane/Bars and light/dark/one-bit/lava galleries. **DISPATCH-READY.**

67. **Left/center/right overlay placement uses one inset stage—side worlds compose inward, not against the window frame (user-decided from 2026-07-23 screenshot).** Keep the three theme-owned anchor choices and every world’s existing assignment, but redefine their shared placement law: Center remains exactly at the viewport midpoint; Left and Right are symmetric interior rails with card centers near the viewport’s one-third and two-thirds positions when the card fits comfortably. They are still unmistakably left/right, but gain generous outer breathing room instead of the current 28px edge-hug. Card width, internal text alignment, right-anchor content shrink, and mirrored Bars growth remain unchanged. This is one global resolver—not per-theme offsets, identity branches, or a fourth user-facing anchor. Narrow response is continuous: as the viewport can no longer seat the ideal thirds, both side rails move inward/clamp through the existing safe inset/fill regimes with no breakpoint jump, clipping, or asymmetric drift. The Theme picker still crosses to the complete destination world on deliberate movement, including snapping to its newly inset rail; passive hover still does not chase. Verification: wide-window card-center fractions near 1/3, 1/2, 2/3; exact left/right mirror law across widths and content widths; narrow sweep stays on-canvas and continuous; Pane/Bars, plain/secondary rows, zoom/DPI, and left↔center↔right Theme crossings; vision gallery includes the reported 1488px Mangrove-style left composition and its right mirror. The implementer generates the wide/mirrored/narrow placement gallery; Fable only judges the resulting composition and breathing room, never implements it. **USER-DECIDED + DISPATCH-READY.**

69. **Two quiet backgrounds gain a specific physical idea: Gumtree’s three broad grass-bands and Bombora’s layered waves (user-delegated world assignment).** Replace Gumtree’s uniform dot grid with exactly three large tone-on-tone diagonal bands across the canonical landscape viewport: roughly 30–35° like cut-paper grass, broad enough to read as three authored shapes rather than repeating wallpaper, using only its eucalyptus ground ladder, crisp-but-quiet edges, static, and continuous as one field behind the occluding page rather than restarted independently in each margin. Responsive views crop/scale that same large field; they never tile it into many narrow stripes. This is distinct from Potoroo’s existing energetic `Stripes`: Potoroo keeps its narrow bright page-edge band and outward dissolve byte-identical, while Gumtree’s bands span the full margin field at low contrast. Replace Bombora’s static `Starfield` with three stacked shallow wave tiers: wide scalloped crests, horizontally phase-offset so they layer rather than form a grid, drawn in its own violet ground steps, calm/static and continuous behind the page. Mulga becomes the sole shipping static Starfield; Currawong’s separate ambient lifecycle stars are unchanged. Implement both as reusable theme data through the one background renderer—closed variants/parameters and exhaustive matches, never world-name branches, assets, randomness, or clocks. The flat writing column, page-off disappearance, caret accent, one-organic-element law, ambient-motion behavior, and every unrelated world remain unchanged. Extend capture sidecar names/fields, background/WebGL2 shader validation, deterministic pixel laws, palette/contrast laws, and the full roster sweeps; assert Gumtree’s canonical mid-field witness crosses exactly three broad bands and Bombora exposes exactly three non-overlapping wave tiers at fixed phase/geometry. Verify wide/canonical/narrow page mode, DPI/zoom independence, left/right continuity through the hidden page, release frame cost with the existing fullscreen-pass witness, and item 68’s Room/Frame before-after gallery. The implementer generates the real current/bands/waves captures; Fable only makes the scale/angle/contrast taste call from those images, never writes the shader or theme data, and the implementer applies its verdict. Update `WORLDS.md`’s table/vocabulary plus `THEMES.md` in the landing round. **DISPATCH after item 68; one shared shader/theme round.**

70. **Quokka becomes awl’s deliberately playful printed-card world: Sour Gummy, slanted halftone material, and chamfered eight-edge cards (user-decided).** Replace Quokka’s Latin display face only (Fira Sans) with OFL Sour Gummy while retaining IBM Plex Mono for code and the existing Klee One/LXGW WenKai CJK companions. Follow the established source-verified instance/subset path: Regular 400 for prose plus a real heavy companion for bold/headings (implementation produces 700-vs-900 candidates rather than relabelling weight metadata), complete Latin/punctuation coverage, no runtime fetch, license/copyright attribution, native+wasm size delta, never-tofu and fallback laws. Add two closed `RenderCaps` fields with inert defaults: `CardTexture::Flat | HalftoneDots { … }` and `CardShape::Rectangular | Chamfered { … }`; Quokka alone initially assigns the non-defaults. Its card texture is a small rotated dot lattice at roughly 15–20°, strongest at the far/right decorative side and rolling off before the left-aligned content-heavy side, drawn only from derived card-surface ladder values—never raw color, amber, randomness, or motion. Its silhouette is a crisp 45° chamfered rectangle with a modest roughly 10–12 logical-px cut: fill, texture, border, and shadow share the same eight-edge boundary. The capability belongs to the one `ListBacking::Card` overlay-surface owner (including a Pane picker and its small card popup), stays inert on Bars/no-card worlds and non-overlay HUD/menu/which-key floats, and crosses completely in the live Theme picker like every other theme-owned property. Split Pane’s upper and lower cards are each valid eight-edge panels; their dots use one shared card-local coordinate field so the pattern aligns across the open world-gap instead of restarting. Narrow layouts reduce the chamfer before it can steal text room; texture never changes hit geometry or text layout. The implementer first generates real awl captures for current Quokka, type-only, type+halftone, and the complete chamfered treatment, including 700/900 heavy candidates, Pane split, small popup, Theme-picker crossing, wide/narrow, 1×/2× DPI, and Galah beside Quokka. Fable inspects only those generated images and returns the final heavy-weight, dot angle/density/rolloff, and chamfer-size taste call; Fable does no implementation or file editing, and the implementer applies the call before gates. Verify exhaustive cap rosters, Flat/Rectangular byte-identity for all other worlds, card/text contrast at every sampled dot phase (query, muted/faint rows, hints, selected band), clipping/border continuity, no pattern in the split gap, WebGL2 shader validation, deterministic captures, release frame cost/no idle CPU work, full suite/native+wasm, item 68’s Room/Frame gallery, and `WORLDS.md`/`THEMES.md`/font-license docs in the landing round. **DISPATCH after item 68; coordinate with item 69’s background shader work but do not merge them into one implementation round.**

71. **Bowerbird’s summoned cards gain a quiet woven jagged-wave material (user-inspired; world assignment delegated).** Extend item 70’s closed `CardTexture` capability with `JaggedWave { … }` and assign it to Bowerbird; its glossy blue-black bower is the natural home for the interlocking angular rhythm, while Quokka keeps its separate printed halftone identity. Draw two or three broad nested chevron/wave tiers across the complete card-local field, horizontally phase-offset so they read as one woven surface rather than tiny repeating zigzag wallpaper. Use only Bowerbird’s derived card-surface ladder at low contrast: no amber, raw colour, randomness, animation, new border shape, or change to text/hit geometry. The pattern is clipped by the existing rectangular card silhouette, shares coordinates across Split Pane’s two surfaces and open gap, and becomes quieter beneath the content-heavy middle so query, rows, shortcuts, muted/faint ink, and selection remain immediately legible; it stays inert on Bars/no-card worlds, page backgrounds, and non-overlay HUD/menu/which-key floats. The implementer generates real Bowerbird captures for current plus two- and three-tier variants at quiet/medium contrast, Pane split, small popup, Theme-picker crossing, wide/narrow, 1×/2× DPI, and Bowerbird beside Quokka/Mopoke. Fable inspects only those images and returns the tier count, tooth scale/phase, contrast, and content-rolloff taste call; Fable does no implementation or file editing, and the implementer applies the call. Verify the exhaustive texture roster, every non-Bowerbird world byte-identical, card/text contrast at representative pattern phases, coordinate continuity/no paint in the split gap, deterministic/WebGL2 captures, release frame cost/no idle CPU work, full suite/native+wasm, and `WORLDS.md`/`THEMES.md` in the landing round. **DISPATCH after item 70; keep as a separate taste/assignment round.**

## Ready — shared ownership and performance

10. **One shared single-line textbox model across all text-entry surfaces.** Own text, caret char-index, insert/delete, character motion, and the same word-boundary rules as document text—never rope/path/undo/syntax machinery. Route seven fields through it: picker query; Rename; Insert-link URL; Keep-version name; Settings value; Find query; Replace text. Typing/deletion work at the caret; word motion uses the document boundary owner; render the amber caret at the shaped prefix advance. Plain Left/Right become character motion where currently unused (Rename/Link/Keep/Settings/Find/Replace); picker query retains its lens/descend/list meanings, using distinct word-motion actions. Surface effects stay outside the model: picker refilters, Find recomputes, Replace does not, Settings validates, and each commit rule remains. Verify a no-wildcard seven-field roster, Unicode/boundary parity, per-surface effects, and caret pixels at beginning/middle/end. **DISPATCH-READY.**

54. **Replace `OverlayState` parallel row arrays with one typed `OverlayRow`.** A row carries accept/display text and typed optional metadata; the corpus owns `Vec<OverlayRow>`, and filtered/ranked views retain stable row indices. Filtering, reordering, removal, and breadcrumb rebuilds must preserve complete row identity across spell actions, headings, settings, history, files/folders/git, hidden rows, bindings, and times. Preserve current strings, ranking, selection, sidecar schema, and visuals. Verify a no-wildcard `OverlayKind` constructor roster and metadata-preservation laws. **DISPATCH after item 10.**

55. **Make Settings identity typed; display labels are never behavior keys.** Add a closed setting/target identity to the one registry and carry it through accept/effects while preserving `config.toml` wire keys, validation, consequences, and persistence timing exactly. Verify a no-wildcard roster maps 1:1 to the registry, label changes cannot alter behavior, compatible targets are exhaustive, old config keys round-trip, and both keymap conventions/captures stay green. **DISPATCH after items 10 and 54.**

56. **Move active buffer + buffer-scoped state as one owned slot, then remove the shadow active path.** In sequential phases: (A) move buffer identity, folds/view state, and caches atomically between active state and the registry, retiring manual snapshot/restore ownership; (B) make `Buffer::path` authoritative and remove duplicate `App.file`, retaining separately named metadata only for genuinely different concepts. Do not add a generic session framework or broad live/headless driver. Verify exhaustive A→B→A sentinels, version-zero cache isolation, fresh defaults, and path laws across open/new-note first save/autoname/rename/move/duplicate/close/autosave/history/session restore; native + wasm gates. Decompose `app/files.rs` along the new private ownership boundaries. **DISPATCH after the user-visible wave; high-risk sequential round.**

57. **Caret lookup cost must be independent of document position—measure first.** Add a release witness at document top/middle/tail that proves work ran and records visited runs/lines + frame cost. If already position-independent, land only the witness. If prefix growth is proven, compute one target-line-local caret glyph record per relevant geometry generation and share it across block/mask/descender/baseline/morph consumers; no retained document-wide cache. Verify constant visited work and appearance equivalence for proportional/mono, ligatures, wrap affinities, CJK, blank/combining text, descenders, and every caret look. **DISPATCH-READY and independent.**

68. **One roster-driven world-gallery command captures the Room and Frame for every current world (user-decided).** Add a small sibling to the existing sample-oriented `scripts/capture.sh`: build release once, enumerate the exact `theme::THEMES` roster through one code-owned source (no duplicated shell list), and render the same canonical taste specimen plus the same representative summoned overlay for every world. The specimen is restrained but actually Markdown: one H1, at least two H2s, short prose, italic + bold + inline code, a short list, and a section break so the heading ladder, world-owned bullets/ornament, and body voice all appear; it contains no unresolved product placeholders or spell warnings. Capture the writing view at a fixed wide canvas/DPI with page mode explicitly ON and a narrower fixed measure, leaving generous visible margins/page edges and enough rail for the default persistent Outline—an 80ch page that visually becomes edge-to-edge is not an acceptable gallery witness. Emit named PNG + JSON pairs and labeled light/dark contact sheets under a replaceable gitignored `gallery/worlds/` run, so a designer can compare the full roster without manually typing eighteen commands. Fail loudly on a missing, duplicate, unknown, or newly un-enrolled world; keep headless determinism and require no network, OS automation, or new external image utility. The run that motivated this item also exposed stale roster copy in `awl --help` (only ten names): route help output through the same owner so it cannot drift again. Document the one command in `CAPTURE.md`; leave the existing fixture sweep unchanged. Verify all 18 identities appear exactly once in both Room and Frame sets, sidecars name the requested world, page/Outline/margin geometry is present in the Room captures, contact-sheet labels match, and a roster-addition law fails until the new world enrolls. **DISPATCH-READY; independent verification tooling.**

## Timed — not blocked

20. **Pre-tag taste pass.** At the user’s explicit tag/release start, the implementation/release owner generates one current world screenshot export, then Fable judges only those images for per-world bullets, squiggle size/baseline including Bilby, dash padding, and Saltpan font outcomes; Fable never implements or edits. Ordinary pushes do not trigger it.

24. **Release-adjacent user-facing docs refresh.** After the current user-visible wave settles and before release preparation, update GUIDE, welcome/tour, and site guide for the current product, chords, and features. Matter-of-fact voice; facts verified. Site copy may change; deployment remains separately user-gated.

## Parked — explicit gate or future design

37. **Theme-picker latency experiment.** Add input→settled-present timing and a once-per-switch phase breakdown (font load, reshape, row geometry, atlas upload, first present). Compare normal debounce with zero debounce using both slow rhythmic single Down presses and rapid bursts, bracketing every press separately from visible highlight response to last changing compositor frame. The external witness is a foreground live app + OS screen recording + agent-driven input; it is not headless or CI. Report numbers and a recommendation, but the user retains the final tune/keep/kill decision. **PROBE RAN 2026-07-23 (agent-driven, live, --release) — NUMBERS IN, DECISION PARKED FOR THE USER.** Finding: the felt freeze is NOT a slow GPU reshape. A theme switch's WORST single frame is 8–15ms in every condition (under one 16.7ms frame — awl never drops a frame), atlas-upload-bound (~3–18ms by typeface), font/rowgeom ≈0, present ≈0.5ms. The freeze IS the debounce wait: app "theme settled" = **162–176ms at the 150ms debounce vs 29–32ms at 0** — the 150ms adds ~130–140ms of pure input→settle latency and defers nothing expensive. The user's rapid-Down case reproduced exactly: at 150ms the coalesced restyles land as one delayed ~167ms lurch (the reported "freeze on settle"); at 0ms every Down previews its own theme live (~30ms, 8ms worst frame), no dead air. Pixel-bracket + the item-37a in-app readout agree. **RECOMMENDATION (advisory): 150ms should not stay — set debounce = 0** (immediate, ~30ms settle; the only side effect is that fast scrubbing previews each intermediate world live, arguably desirable); **if that intermediate-theme flicker is unwanted, a 30–50ms cap coalesces without a perceptible freeze.** **DECISION (user, 2026-07-24): (a) debounce = 0 — LANDING.** The theme/font debounce DEFAULT becomes 0 (immediate theme switches — kills the felt freeze the probe pinned). The `AWL_THEME_FONT_DEBOUNCE_MS` env override stays as the recoverable escape hatch (precedent: convention/lava env overrides), so 150 or any value is restorable. Wiring templated from branch `worktree-wf_31709d4c-f78-1` @ ed83369; re-applied on current main with default 0 and integrated right after wave 7 (avoiding a main-tree merge race). Evidence (4 recordings + exploded frames + panel crops) is in the job tmp.

- **Export save-dialog scope:** macOS + Linux, one live-only cross-platform seam; capture uses an explicit path. Decided, not scheduled.
- **Per-world living-band choreography:** audition TwoShape/Slam/Soft against Morph; live feel is the oracle. Needs a design session.
- **Per-world copy-pulse differentiation:** possible future motion tweak; needs a design session.
- **Site deployment:** only on the user’s explicit word.

## Monitoring — non-blocking

- **CI soak acceptance:** item 53’s fix is landed. The first post-fix main run is green ([run 30006896158](https://github.com/Frank-P-Lu/awl-next/actions/runs/30006896158)); two more consecutive green main runs complete the promised 3-run acceptance.
- **Hands-on checks still useful:** Dawn/Bilby world feel; writer-diff panel/Tab + zoom readout; phantom image resize handle; upward scrolling past images in release; right-click Add-to-dictionary summon; 2px Wagtail stipple taste.
- **GPU memory:** no action unless the 6 GB symptom recurs; then probe the live surface with the window foregrounded.

## Release blockers and reminders

- App icon.
- Dictionary/font/license notices plus code copyright/NOTICE review.
- Apple signing secrets and Fly deployment token; see `RELEASING.md`.
- Tags and releases require the user’s explicit word. A dry run may precede them.
