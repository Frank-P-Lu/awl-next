# Long Wrapping Document

Paragraph 01: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 02: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 03: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 04: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 05: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 06: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 07: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 08: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 09: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 10: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 11: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 12: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 13: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 14: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 15: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 16: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 17: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 18: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 19: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 20: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 21: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 22: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 23: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 24: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

Paragraph 25: This is a deliberately long line that exceeds the window width so it soft-wraps into multiple visual rows, which is exactly the case where logical-line scrolling fails to reach the bottom. 

LAST LINE: if you can scroll here, visual-row scrolling works.