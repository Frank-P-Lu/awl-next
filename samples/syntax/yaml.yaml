# Syntax gallery sample — YAML.
#
# This paragraph is a prose comment: several stacked line comments that read
# as an explanation, not code, so they should render prominent rather than
# fading like the commented-out snippet below.

# retries = 3;
# connect(host, retries);

name: awl-gallery
greeting: "hello, awl"
verbose: false
enabled: true
owner: null
missing: ~
max_retries: 5
tau: 6.283185
tag: 'c'
hex_flag: 0x1F
legacy_yes: yes
legacy_off: off

mode:
  kind: read
  idle: false

endpoints:
  - https://a.example.com
  - https://b.example.com

description: |
  This is a literal block scalar. It runs across several lines and
  behaves like a multi-line string literal, not a mapping key.

retries_by_env:
  dev: 1
  prod: 5

authors:
  - name: awl
    verified: true
  - name: gallery-bot
    verified: false

negative_offset: -3
scientific: 1.5e+2
