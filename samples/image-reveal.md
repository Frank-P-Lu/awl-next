# Image reveal

Prose above the image stays put — nothing reflows.

![a soft gradient with a pale sun|360](photo.png)

Prose below the image stays put too — caption style: when the caret lands on
the image line the source reveals centred over the dimmed image, and the row
height never changes, so no line dances.
